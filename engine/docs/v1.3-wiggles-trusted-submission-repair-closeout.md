# Trusted submission timing repair — finite acceptance closeout

## Result

**PASS for B1 and the directly related deadline paths, within the existing offline, supervised/trusted-administrator scope.** No live capability or execution authorization is granted.

The canonical offline suite ran **once** after focused acceptance: **479 tests, 476 passed, zero failed, three unchanged intentional skips, zero cancelled/todo; exit 0**. The process completed in 312.37978808535263 seconds, within its 600-second allowance. No source or test edits followed this acceptance.

Final accepted source manifest:

- File: `engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`
- SHA-256: **`3b2ae4ee3130626f16e4af1f7b890e62f999b7721c83fccc5bbcc400ba44eb5b`**
- **188 members**: all 187 starting paths retained in order; one prior implementation member updated, one regression-test member appended; 186 prior members unchanged.

The historical starting identity was verified, not replaced with a newly inferred historical hash: **`3ed339f437e6c3b8f13c4135655df1d7a44a15136983ebdb984ecbbe41a18089`**, 187 members. The existing accepted manifest copy remains in `/root/artifact-wiggles-submission-evidence._0apxfgp/`. Existing implementation work, acceptance evidence, and the original read-only review were preserved.

Paths below use `A/` for `engine/src/verification-scope-v1-3/final-proof-agent/`.

## Exact repository files changed by this repair

1. **`A/wiggles-submission-v1.mjs`** — final transport timing gates; scheduler-origin observation; response acceptance deadlines; durable timing dispositions and validation; retirement of scheduler-timed-out calls; timing checks in terminal-only recovery.
2. **`A/wiggles-submission-timing-v1.test.mjs`** — new actual-composition deterministic regressions, including delayed real authority/fsync operations, boundary positives/negatives, late observations, durable consumption/reopen, evidence mutation/omission, and dispatch adjacency.
3. **`engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`** — updated the one changed implementation digest and appended the new test digest, without reordering or removing starting members.
4. **`engine/docs/v1.3-wiggles-trusted-submission-repair-closeout.md`** — this closeout, outside the source manifest to avoid self-reference.

No other repository file was edited by this repair. In particular, the reviewed scheduler/source identity, prior scheduler tests, runtime implementation/test, acquisition/disposal authorities, SQLite authority, original closeout/review, package script, and live entry remain unchanged from the starting accepted members. The no-op warning remained closed; no test was edited merely to clear it.

Final changed source hashes:

- `A/wiggles-submission-v1.mjs`: `17de0b1a5e18eef1e0854d30ce39381f1a858914048836de80538105163c25d7`
- `A/wiggles-submission-timing-v1.test.mjs`: `8a7d95e06ad18fb2e883f1b43e63bb8e4ff7b2f4f985f51dc81f54400cc062d1`

## Finding dispositions and targeted readback

### B1: final transport boundary — repaired

`A/wiggles-submission-v1.mjs:301-334` observes the **same clock reads** used by the pinned scheduler for its acknowledgment and resolution origins. The acknowledgment origin is captured on the scheduler's read immediately following retained initial `ACK_MATCH`; the resolution origin is captured on its read after terminal evidence reservation. These are not newly started, more permissive clocks.

The actual external invocation at lines **378-395** follows the authority inspection, verified wire reread, durable call reservation, and reservation readback. It checks:

- Runtime wall clock strictly before the configured deadline.
- Full request timeout fitting within the existing wrapper overall budget.
- For resolution calls, full request timeout fitting within the existing 180000-ms resolution budget.
- For both rebroadcast ordinals and their eligibility reads, the acknowledgment-relative ordinal window.

The unchanged windows remain **[2000, 3000)** and **[5000, 6000)**. Lower equality is permitted; upper equality is refused. Full-timeout-fit equality for resolution/overall initiation remains permitted; a one-ms overflow is refused. No new deadline constant or budget was introduced.

The final clock check and external `transport(externalRequest)` invocation occur in the same synchronous invocation body. The promise turn that enters that body is **before**, not after, its final gate. There is no intervening await, journal write, authority call, or promise turn. A new microtask-order regression checks this through the actual runtime composition.

The reviewed scheduler remains byte-identical at SHA-256 `9dfed7dd40e97da0f98c1eba1d374a58f98eff2a12c25df2bdf2ad6f54c5bb16`; its policy and tests were not redesigned or loosened.

### Durable ordering, expiry, exceptions and restart — preserved

The existing SQLite `recordSubmissionPossibleV1` owner election still consumes the whole schedule before any external effect (lines 293-297). Call/send accounting and the fsynced call reservation remain before dispatch (lines 336-360).

When post-reservation work exhausts a deadline, an exclusive, durable timing record retains **`NOT_DISPATCHED_DEADLINE`**, with a null dispatch time. The call reservation is not removed and the send budget is not refunded. Complete transmission validation will not certify that refused callback as a transmitted request. The submission remains unresolved, and reopen does not obtain any more sends or signing authority.

A crash or persistence exception can prevent a complete timing disposition. Such absence remains ambiguity; the earlier durable schedule consumption remains authoritative. It is not treated as proof of non-transmission. Existing SIGKILL-before/after-effect and cross-process owner-election tests passed unchanged; new fsync exception and expiry/reopen tests check the same conservative stage and no additional transport.

### Resolution and runtime response deadlines — repaired

Lines **401-414** check response acceptance against request, wrapper, resolution, and runtime wall deadlines. Equality at an acceptance deadline is late. A backward wall observation relative to dispatch cannot be accepted; the existing safe-integer/nondecreasing monotonic-clock discipline remains enforced.

A bounded response observed late is retained separately in its timing record as **`LATE_RESPONSE`**, including status and exact base64 body, but is not returned as an accepted scheduler response. A locally expired deadline is never converted into chain non-occurrence. Missing/late terminal evidence cannot manufacture finalized success, failure, or expiry proof; ordinary ambiguity/unresolved handling remains.

For real-time, abort-ignoring settlements, the reviewed scheduler's outer timeout can precede the wrapper timer. The wrapper now observes the scheduler's durable absent-response retention, retires that request, aborts its controller and retains the transport-error disposition before the next scheduler call (lines 306-326, 369-374). Retired work cannot start later or return an accepted result. Once the bounded observation has closed, later promise settlement remains inert: it does not append late evidence to a closed package or authorize another send. The preserved timeout/error record represents an unaccepted/uncertain observation, not proof that the transaction did not occur. Independently obtained, timely finalized evidence may still resolve escaped bytes.

### Evidence and terminal-only recovery — repaired

`validateCallTiming` at lines **181-215** checks a closed timing-record schema, exact call-record hash/request ordinal binding, dispatch/observation timestamps, applicable window/full-timeout fit, runtime wall deadline and acceptance classification. Completion now binds the ordered timing-record hashes, and closed root membership requires them (lines 232-270). The new regression rejects a fully rehashed out-of-window dispatch record and rejects omission.

The independent terminal-only finalization path also binds each terminal request to its journal/timing record and applies these checks before economic source capture (lines **455-469**). It does not require a complete transmission package merely to close real finality after revocation, but it cannot bypass terminal timing custody.

These are executor-custody observations, not provider-signed clocks or independent append-only attestation. Hashes detect alteration relative to retained identities; an administrator capable of fabricating an entirely consistent replacement observation tree remains outside the approved integrity promise. Historical records lacking the new timing/runtime binding are not silently upgraded or used to replenish authority; reconstruction remains a separate supervised gate.

### Preserved non-timing invariants

The targeted readback traced the unchanged retained-input reconstruction and exact wire/message/signature checks at lines 96-149, reread-before-callback comparison at lines 338-341, and unchanged ordinal-only input surface. No submission path gained a signer, new wire build, requote, blockhash refresh, transaction replacement, or caller-selected submission parameter.

The three-send limit, zero provider retries, consumption, revocation behavior, finalized status/transaction correlation and economic source recapture remain intact. The acquisition-validator repair and chain-derived disposal quantity code are among the 186 byte-unchanged prior members. Existing acquisition/disposal, wrong-basis, revoked-finalization, source-substitution, missing/conflicting evidence and exact-wire tests passed; timing work did not modify their semantics to obtain GREEN.

`A/live-executor-composition-v1.mjs:9-14` was read back and still unconditionally fails with `bounded_agent_live_executor_not_released`. No live activation, RPC, real credentials, wallet/ATA operation, broadcast, infrastructure change, Git mutation or delegation occurred. Tests use the existing disposable offline fixtures and injected transport, not live signing/submission authority.

## Executed regression and acceptance evidence

Retained directory:

**`/root/artifact-wiggles-timing-repair-evidence.3_vz2lwf/`**

Each listed run has its own TAP and `*-result.json`, with command, exit, wall duration, TAP SHA-256 and full before/after source hashes. Prior failing results were not overwritten.

| Label | Tests | Pass | Fail | Skip | Exit | Meaning |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `red-window` | 2 | 0 | 2 | 0 | 1 | Both real authority-read and journal-delay reproductions transmitted once where zero was required. |
| `green-window` | 2 | 2 | 0 | 0 | 0 | First repair stopped both disallowed rebroadcasts. |
| `red-deadlines` | 7 | 2 | 5 | 0 | 1 | Three resolution-initiation overflows, runtime initiation and late runtime response reproduced. |
| `green-deadlines` | 29 | 29 | 0 | 0 | 0 | Deadline repairs plus existing submission tests. |
| `red-eligibility-and-matrix` | 25 | 23 | 2 | 0 | 1 | Additional status/height eligibility-window crossings reproduced; boundary matrix otherwise passed. |
| `focused-final` | 137 | 133 | 1 | 3 | 1 | Broader run exposed scheduler-first timeout retention ordering; repaired, not hidden. |
| `focused-accepted` | 141 | 138 | 0 | 3 | 0 | Final timing/submission/recovery/runtime/reused-scheduler focused acceptance. |
| `canonical` | 479 | 476 | 0 | 3 | 0 | Root `npm run test`, once after focused acceptance. |

All runs have zero cancelled/todo. The final new test file contributes 29 tests. They cover both ordinal windows at lower/inside/equal/after positions with a nonzero origin; both real authority and persistence delay; status/height eligibility expiry; all three resolution request kinds at the exact full-fit boundary and one ms beyond; terminal response before/at/after the resolution deadline; wall deadline crossing and deadline-minus-one positive; overall initiation exact fit/overflow; retained late response bytes; rehashed invalid timing/omission; fsync exception/reopen; and synchronous final-gate adjacency. Disallowed-send tests first verify the scheduler already prepared the send, then require zero transport for that send and no restart budget.

Final focused TAP SHA-256:
`05c044700fc78d40089a1d0142f38a08a7a3e2c201ec39361ae4ab22abe671be`

Canonical TAP SHA-256:
`84f56fa86e0dc9c3221a503aed1a60f2ff8dccf820ac1e2de6e8827e50bf4ad0`

Final-focused pre/post and canonical pre/post source snapshots are identical at the final manifest identity. All 188 current members were checked again after acceptance. The 40 test files selected by the unchanged canonical script exactly match the manifested maintained verification-scope test set.

`git diff --check` passed, and explicit whitespace checks cover the changed/new implementation and test, including untracked files. The external final-verification helper initially compared the canonical selection against every manifested test, including an intentionally out-of-scope wallet-acquisition adapter test. That helper-only set assertion was corrected to the actual unchanged package-script scope; no repository source, manifest or tests were changed, and no successful suite was rerun.

Exact final source bytes are retained in **`accepted-source.tar.gz`** and were read back member-by-member against the accepted manifest. Archive SHA-256:
`55d96a095d701aeaf685aa8ab71bcf7fe97ed44ffad4012b32bac92c6ea8ae1a`

`final-verification.json` records member preservation, process results, archive identity and whitespace verification. The original review remains SHA-256 `cff434da1d83dc4d33652d812db491dcb34011017b7541c2fccd138695ace2f7`. External checksum inventory is retained as `SHA256SUMS`.

## Read-only acceptance clarification: external helper selection assertion

**CHECKPOINT_READY** at accepted manifest SHA-256 `3b2ae4ee3130626f16e4af1f7b890e62f999b7721c83fccc5bbcc400ba44eb5b`.

This clarification inspected retained evidence only; it did not rerun tests or the write-producing verification helper. Its sole write is this addition to the existing closeout.

The external helper's original assertion (visible in the retained conversation's patch diff) was:

```python
assert {str(Path(p).relative_to(R)) for p in tests}=={p for p in members if p.endswith('.test.mjs')}
```

The corrected assertion at `final-check.py:41` is:

```python
assert {str(Path(p).relative_to(R)) for p in tests}=={p for p in members if p.endswith('.test.mjs') and p.startswith('engine/src/verification-scope-v1-3/')}
```

The left-hand side was not changed: it enumerates the three exact test globs in the unchanged root package script. The original right-hand side incorrectly required **all 41 manifested test files**, including `engine/src/wallet-acquisition/helius-target-account-snapshot-adapter-v1.test.mjs`. That one file is outside the package script's verification-scope directory and was not part of this repair's agreed canonical or focused commands. The corrected right-hand side requires exact set equality with **all 40 manifested verification-scope test files**. It does not use a subset check or remove a required selected test. The wallet-acquisition adapter remains manifested and hash-verified; this acceptance does not claim it was executed.

Evidence checked for this clarification:

- `canonical-result.json` retains command `['npm', 'run', 'test']`, exit 0, and equal before/after source snapshots. `canonical.tap:2-3` records npm expanding it to `node --test engine/src/verification-scope-v1-3/*.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/*.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/reused/*.test.mjs`. The archived `package.json` has exactly that script.
- All 188 current members and every corresponding archived source member match the accepted manifest and canonical snapshot. The archived manifest bytes have the accepted hash. Final-focused pre/post and canonical pre/post snapshots are identical. The timing test source is thus bound to the accepted run, not inferred from a later mutable edit.
- **All 29 required new timing regression results are explicit, non-skipped `ok` records in `canonical.tap:1744-1912`, tests 286-314.** These include authority/journal delayed rebroadcasts (286-287); all three resolution-initiation overflow paths (288-290); runtime initiation/response deadlines (291-292); both ordinal boundary matrices (293-300); eligibility read deadlines (301-302); exact-fit resolution positives (303-305); terminal response boundaries (306-308); timing evidence mutation/omission (309); fsync exception/restart (310); overall exact-fit/overflow (311-312); wall positive (313); and final dispatch adjacency (314).
- The retained focused command explicitly names the timing, submission, submission-recovery, trusted-runtime, and reused-scheduler test files. All five are selected by the canonical globs. A multiset comparison found **all 141 focused result names, including their unchanged skip annotations, in the canonical TAP**. Focused acceptance remains 138 passed / 0 failed / 3 skipped; canonical remains 476 passed / 0 failed / 3 skipped, 479 total, exit 0. The three skips are the already-declared live-seam, live-source operator-contract, and live terminal-failure/expiry cases—not timing regressions.
- Canonical TAP SHA-256 remains `84f56fa86e0dc9c3221a503aed1a60f2ff8dccf820ac1e2de6e8827e50bf4ad0`; focused TAP SHA-256 remains `05c044700fc78d40089a1d0142f38a08a7a3e2c201ec39361ae4ab22abe671be`. All external `SHA256SUMS` entries also verified. Neither process result nor TAP was edited by the helper correction or this clarification.

**Disposition:** the correction fixed the helper's expected selection set after the successful canonical execution. It did not change a test command, test source, source manifest, assertion inside a test, process exit, TAP result, or required coverage. The helper's own set-assertion failure was not a failing test and was not reclassified as a passing test. Earlier genuine RED/failed-focused test runs remain retained as failures. No required acceptance coverage is missing from the retained final run.

## Remaining gates and stop boundary

Separate authorization/reconstruction/deployment gates remain: real principal/episode authority; release/configuration/dependency binding; credential-safe direct transport and admitted provider completeness/finality; zero hidden retries and egress confinement; host/filesystem durability and supervision; revocation delivery; known restoration/state-loss STOP procedures; approved exact-signature reconciliation of unresolved attempts; real economic source authority; and final release/child-artifact reconstruction.

None is granted by this offline repair. Local expiry, a lost acknowledgment, an incomplete package or remaining unused send slots cannot authorize replacement signing or another schedule.

This was one focused repair followed by finite acceptance and one targeted readback, not another broad independent review. Changes are left **uncommitted and unpushed**. No next-stage implementation is started.
