# Artifact v1.14 Release Notes

## Wallet-Wide Bounded Acquisition v1

Artifact v1.14 adds the deterministic, read-only acquisition boundary that completes this path:

```text
wallet + permitted fixed lookback
→ finalized Solana anchor
→ canonical finalized wallet-signature history
→ exact Enhanced Transaction enrichment
→ exhaustive five-outcome transaction dispositions
→ wallet_wide_acquisition_result_v1
→ candidate_evidence_bundle_v1
→ wallet_candidate_set_v1
→ digest-only selection
→ exact existing Slice 7 dry-run request
→ unchanged receipt_package_v1
```

Finalized Solana `getSignaturesForAddress` history is the completeness index. Enhanced Transactions enrich the exact canonical signature set; Enhanced address history is not accepted as the completeness authority. Caps, truncation, timeout, uncertainty, stale-head detection, set mismatch, or protocol contradiction fail closed without an acquisition result.

v1.14 introduces no hosted storage, authentication, jobs/workers, API, UI, publication, package-store commit, upload, signing, minting, deployment, live marks, or trading-fund custody or control.

The first pre-hardening bounded provider-attested controlled live validation and the distinct later post-hardening controlled live validation each passed the implementation current at the time. The final post-remediation controlled live validation then **PASS**ed at finalized anchor slot `437570354` and finalized anchor block time `1786013791`. That result remains an accurate historical stage.

After patch 3 changed the production operator, deadline, and telemetry boundaries, the final post-patch-3 controlled live validation **PASS**ed as the final authoritative release-gate run for one approved public Solana mainnet-beta wallet using `lookback_7d_v1`. It bound finalized anchor slot `437600788`, finalized anchor block time `1786026671`, and lower timestamp bound `1785421871`. It examined two pages, terminated with `historical_bound_reached`, observed 76 canonical signatures, excluded zero post-anchor signatures, and reconciled five in-window transactions with five Enhanced transactions.

The final disposition partition was 1 `supported_normalized_event`, 0 `unsupported_activity`, 1 `ambiguous_activity`, 3 `unrelated_activity`, and 0 `failed_transaction`. The result contained one normalized event, one finding, one localized finding, one blocked summary, zero candidates and zero selectable candidates. Completeness was exact: `acquisition_complete: true`, `normalization_complete: true`, `classification_complete: true`, `pagination_complete: true`, `historical_bound_proven: true`, and `chain_boundary_proven: true`; `capped`, `truncated`, `partial`, and `provider_uncertain` were all false. Telemetry was `retry_count: 0` and `timeout_count: 0`. Under patch-3 semantics, no retry attempt actually began and no HTTP attempt terminated because of its effective transport timeout. Patch 3 changed no aggregate classification relative to the prior run; the newer finalized anchor naturally changed the content-addressed digests.

Zero candidates was a valid result, not a validation failure. Live candidate resolution and Slice 7 were not exercised. Slice 7 was not invoked because there was no selectable candidate, and no receipt or package digest was issued. No package-store write, publication, upload, signing, minting, or deployment occurred.

The final sanitized report remains outside Git. Its SHA-256 was `d2973f4f97745880d05dd75100071d22b339ac12366db798730f7b7eea8b1ef5`, its size was 1,951 bytes, and its permissions were `0600`. Credentials, URLs, headers, provider prose, response bodies, and raw provider bodies were absent. The final canonical digests were:

- acquisition result: `d1ae883f9b41566fd547a79f2dfcb7894e8e7e827e7229abfba1a5c0606f93d3`;
- evidence bundle: `f463e5cd140b97fba00ad25eb86244ab7bad03dd97412f70d2613aa4ca809dfd`; and
- candidate set: `86400e78a0bc28b0367dcd4a8787abf49e58a68939590e8e0e5a449bf6084a61`.

This was the final post-patch-3 live validation. The live release gate is complete, and no further live rerun is required before tagging v1.14.0. Wallet-history completeness remains provider-attested, not trustless. v1.14.0 is not yet tagged.

## Deterministic retained replay and integration proof

The complete direct-Node wallet-acquisition suite proves:

```text
exact retained Helius bodies
→ v1.14 Enhanced projection
→ wallet-wide acquisition
→ evidence bundle
→ candidate set
→ candidate resolver
→ Slice 7 dry-run request
→ targeted package dry run
```

The replay uses no package store. Targeted orchestration remains `dry_run`, and no package-store capability is supplied.

The tracked tree intentionally contains exactly five retained Helius fixture bodies for deterministic replay: `jup_buy`, `jup_sell`, `ray_buy`, `ray_sell`, and `jupiter_close_account_swap`. The retained RAY bodies carry the Helius source label `JUPITER`; provider source metadata is projected out and does not enter canonical candidate or package identity. Controlled-live raw responses are not retained. No exact retained finalized RPC transcript exists; synthetic finalized RPC pages are used in these tests. Orca, UNKNOWN, failed, mixed-disposition, ambiguity, and provider-error bodies are synthetic fixtures unless a test explicitly says otherwise. Synthetic provider-shaped tests are not retained live evidence.

## Golden identities

### JUP

- Receipt: `5fb5732d248af4e8f9214a3b074c3bf711a776e8445bf14eae735ddf02a0bbca`
- Package: `5b8d2241a70eb68b4bc1b43f3d471dbd677b6d89ba47dc0569f7af7d34e71278`
- `archive-record.json`: `d28c5a58b920f526c5ed9e08e4e5b034d99285cd7182a1374f1eb9c10697c6ac`
- `canonical-receipt.json`: `c636cfda958eb87341d3225d33b53b7dc9dcf157def5cc3a054eb56cd4e9eb61`
- `economics.json`: `d8d716459707f3b8c7f95b2f6e64a3c1f1faf91e62629e0477213e4b4ed9ffbd`
- `manifest.json`: `2ce234ccedcb52ac555f49129de7a3b6660506b04ed452c02503ec626646f1f6`
- `verification.json`: `851c283e7e321bee61a939f1b39dbfb1f09ec038cdd078ceca50c8f7167c6ad0`

### RAY

- Receipt: `4d33969c45a041837070dbc83730862325ff989772712aae285384d4570e4341`
- Package: `25e6820d0ac45e8347375eadd824fde2c6ec528b56b637a0144c013da33d5fa2`
- `archive-record.json`: `777987cf14a3e41034923a6acc0e87ce15ec7affef68b0e3fb32890ad24bd695`
- `canonical-receipt.json`: `94717ca77018826e88bf39313c7b4b810ade1d42ed9f507809c649f1f6f3f2cb`
- `economics.json`: `4664d29a151bba54051c4a8ef6044990a2ca474a4b45a421536106e9fa5d0ea8`
- `manifest.json`: `9fffd0746b49b5e3b89dbf113675c76290c7ae10f99542a23b1c385e3c75b41e`
- `verification.json`: `808c2d03cd54bb13ed418ea034075dc8b523cb01e6a9ce3359d2959498141e6d`

These identities remain exact across Enhanced response permutation and permitted canonical page-layout changes. Wallet-wide `raw_index` values are assigned densely in canonical order, and the authoritative source set equals the disposition set exactly. Unrelated later evidence may change evidence-bundle and candidate-set identity while preserving an unaffected candidate-local digest, receipt-scoped evidence digest, receipt, package, and member identities.

## Safe release verification

Run:

```text
node engine/src/run-v114-regression.mjs
```

The runner executes the safety-adapted v1.13 baseline, every wallet-acquisition test directly with Node's test runner, syntax checks for wallet-acquisition modules/tests/fixture helpers, and a runner/documentation safety self-check. It does not invoke an npm wrapper or a live operator path.

Architecture and trust details are in [v1.14-wallet-wide-acquisition.md](v1.14-wallet-wide-acquisition.md). Operational boundaries are in [v1.14-operations.md](v1.14-operations.md). Release limits are in [v1.14-limitations.md](v1.14-limitations.md).

## Final release-blocker remediation patch 3

Patch 3 makes every exported acquisition-result constructor, validator, and alias authoritative through complete shared disposition accounting. It adds shared native Solana grammar (64-byte Base58 signatures; 32-byte Base58 public-key-like identities), makes the registered acquisition-wide deadline starter mandatory before any provider call, uses monotonic production control time, parses each discovered wallet-acquisition test file's complete top-level TAP independently, and defines retry/timeout counters at the actual attempt boundary.

The provider-neutral core remains free of global fetch, direct environment access, filesystem credential reads, package store, publication, signing, minting, and deployment. The controlled-live executable remains the sanctioned edge that reads `HELIUS_API_KEY`, invokes global fetch through its production client, and writes one sanitized mode-0600 temporary report. Deterministic regression does not execute a live operator command; offline operator tests use injected ports and may write isolated temporary sanitized report fixtures.

Deterministic remediation is complete only after the tests pass. The final post-patch-3 controlled live validation passed and completed the live release gate; no further live rerun is required before tagging v1.14.0. The preserved chronology still identifies the prior final post-remediation run as accurate for its implementation and the post-patch-3 run as the final authoritative release-gate run. v1.14.0 is not yet tagged. Exactly five retained Helius bodies remain tracked for deterministic replay; controlled-live raw responses and exact finalized RPC transcripts are not retained.
