# Construct readiness capture — incomplete prerequisite checkpoint

## Status

**Not implemented; no readiness-capture acceptance claimed.** This session stopped at dependency preparation. No capture module, challenge integration, authoritative opening/age validation, disposal capture or new tests were produced. Existing implementation and historical reports were preserved.

Baseline manifest verified: `8a2095da1bec93e2a0052902cfc8e50c0941f1d3a032bab87b08ae4ed6e8cd81`, with zero member mismatches. Git branch was `main`; initial status contained only the two pre-existing untracked `.hermes/plans/` files. No applicable instructions were found in inspected ancestors/relevant directories.

## Concrete dependency problem and limits of the diagnosis

The acquisition calibration uses `getWhirlpoolDecoder` and `getTickArrayDecoder` from `@orca-so/whirlpools-client@7.0.0`, and `swapQuoteByInputToken` from `@orca-so/whirlpools-core@3.1.1`. Resolution-only probes returned MODULE_NOT_FOUND for both packages from the repository and the calibration directory. Neither runner was imported or invoked.

A root installation dry-run using `npm install --offline --ignore-scripts --dry-run --package-lock=false --no-save --audit=false --fund=false @orca-so/whirlpools-client@7.0.0 @orca-so/whirlpools-core@3.1.1` exited 1 with ENOTCACHED for `@coral-xyz/anchor` registry metadata. No network fallback was permitted.

Alternative checked: the calibration package's offline installation dry-run exited 0 and planned 117 packages. The two requested Orca tarballs are present in npm's cache. An inventory of the donor lock found missing cached tarballs for other-platform optional TypeScript packages; this is **not** evidence that Linux runtime installation is impossible. A dry-run does not establish installed decoder/WASM availability or a reproducible repository dependency closure.

Thus the observed blocker is the attempted root offline dependency-resolution path, not a demonstrated incompatibility with protected economic semantics. The alternative isolated dependency packaging path remains unimplemented and unverified. Stopping here leaves the user's requested implementation incomplete; this note does not claim that all permitted alternatives have been exhausted or that additional network access is inherently required.

Smallest next step: establish a pinned, offline, repository-contained decoder/quote dependency closure from retained packages, without changing unrelated installed versions or importing the calibration runner. Verify runtime loading and replay against retained raw route evidence; then implement the capture integration and requested regression matrix. Do not substitute caller-provided quotes for this dependency.

## Inspected reusable mechanisms

- Calibration acquisition `wiggles-one-shot-acquisition.mjs:845–949`: direct finalized opening accounts, bounded concurrent whole-pair owner enumeration, expected history heads and explicit controlled-quiescence/non-atomicity wording. Its limit-one head checks alone must not be presented as the requested complete age/history proof.
- Same runner `:1086–1158`: initial pool read; directionally derived tick PDAs; same-response pool/tick/vault read and derivation recheck; mint/vault/fee-tier checks; sparse tick handling; quote engine; finalized context-pinned blockhash. B1 requires deriving a strict integer-ceiling minimum rather than blindly adopting the old rounded SDK minimum.
- `engine/src/wallet-acquisition/boundary-contract.mjs`: finalized anchor validation and exact lookback-bound derivation.
- `engine/src/wallet-acquisition/full-transaction-history-acquisition.mjs`: bounded page reconciliation and source-matching enrichment. It requires an independently established canonical source list and returns immediately for an empty list; that empty result is not standalone history-completeness proof.
- `engine/src/wallet-acquisition/helius-target-account-snapshot-adapter-v1.mjs` and `target-account-enumeration-port-v1.mjs`: raw decoding, complete per-lane population validation and distinct controlled-capture authority. Equal slots alone do not establish an atomic combined snapshot.
- Existing readiness challenge and adapter: retain the closed challenge/source fields, executor-owned disposal state, strict B1 inequality and exact-message fee binding. A new source port must own the evidence acquisition rather than merely hash supplied facts.

## Proposed budget envelope — not implemented or live-approved

A concrete initial proposal for later offline evaluation, explicitly not a default or approved budget hash:

| Item | Proposed hard maximum per phase capture |
|---|---:|
| Overall monotonic capture duration | 60,000 ms |
| Per-call timeout | 5,000 ms, capped by remaining overall time |
| Total transport attempts across all helpers | 64 |
| getGenesisHash | 1 |
| getSlot | 2 |
| getBlock, including anchor search | 8 |
| getAccountInfo | 1 |
| getMultipleAccounts | 4 |
| getTokenAccountsByOwner | 4, at most two complete pairs |
| getSignaturesForAddress | 12, at most four pages for each of wallet/JUP ATA/USDC ATA |
| Signature page size | 100 |
| getTransaction | 16, fail closed if required evidence exceeds this cap |
| getLatestBlockhash | 1, no refresh |
| getFeeForMessage | 2, at most one identical-message correlated -32016 retry |
| getBlockHeight | 2 |
| Other retries | 0 |

All retries would consume method and overall totals. The proposed minimum-context fee retry delay is 1,000 ms, subject to the same deadline. Late/abort-ignoring transport completion must not issue readiness or begin another attempt. This table still needs implementation qualification; it is not an assertion that these bounds suffice for every historical population.

Proposed freshness policy to qualify, not implemented: finalized economic/history and opening evidence; execution-time route/blockhash/fee observations retained with their own finalized contexts; nondecreasing required context floors; equal owner-lane contexts only under explicit controlled-quiescence admission; a maximum 30-second retained capture age at use, within an explicit challenge expiry and human acquisition deadline. A finalized anchor time must establish strict `anchor_time - latest_setup_time > 604800`, backed by complete relevant history, not by reaching the candidate wall-clock timestamp. Disposal must reconcile retained finalized acquisition evidence and quantity rather than reuse zero-JUP opening checks. No final acquisition not_after, live budget hash or production freshness approval is supplied here.

## Verification and preserved boundaries

No focused or canonical tests were run in this session because no implementation was completed. The prior 300 tests / 297 passes / zero failures / three skips remain historical, not fresh capture verification. No updated source manifest was necessary: this checkpoint is an unmanifested after-the-fact document, and executable/member bytes were not changed.

Only this checkpoint document was created. No wallet or credential access, RPC, signing, broadcast, deployment, scheduler, Git mutation, delegation or review cycle occurred. The two npm commands were offline dry-runs, not installations. The intended live factory remains unchanged and disabled. Trusted administration/supervision, exclusion of automatic administrator-snapshot rollback detection and deferred external-head infrastructure remain unchanged.
