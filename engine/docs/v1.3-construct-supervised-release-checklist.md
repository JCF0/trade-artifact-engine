# Construct supervised bounded-agent release checklist

## Approved release contract: option 1

The user has approved one human-authorized, supervised bounded-agent episode under trusted deployment administration. Hermes directs the two bounded decisions; Wiggles independently enforces the immutable mandate and owns signing authority. Executor-owned durable state survives ordinary process crashes/restarts. Controller provenance is not economic authority.

**Automatic detection of administrator restoration of an older internally valid snapshot is NOT a guarantee of this release.** External generation/head infrastructure is deferred. This is an explicit scope change, not implementation of rollback protection. Preserve the external-authority design in `v1.3-bounded-agent-rollback-deployment-decision.md` for any future stronger release.

A known restore, reprovisioning, loss of authoritative state, or ambiguous recovery requires stopping the episode. No automatic reenrollment, execution reset, requote, blockhash refresh, replacement signing, or replacement transaction is permitted. Ordinary restart reopens existing authority; missing authority must never trigger provisioning. An operator must preserve the database, retained wire, and evidence and seek separate authorization for any reconciliation. The system cannot automatically detect every restore. This operator policy relies on trusted administrators and supervision, not a hidden freshness detector.

The live entry point remains mechanically disabled. Option 1 does not approve live execution, wallets, credentials, deployment changes, scheduler activation, or an execution window.

## Implemented offline adapter slice

- `orca-message-boundary-v1.mjs` validates the frozen mandate and constructs a one-signer legacy message containing exactly one direct classic Whirlpool swap. It fixes the wallet, token accounts, pool, vaults, oracle, token program, account privileges, direction, exact input, minimum output, and direction-specific sqrt-price limit. It derives tick-array PDAs using the calibrated acquisition/disposal rules. No Compute Budget, ATA, cleanup, memo, additional instruction, ALT, or alternate route can enter the generated message.
- The prepared semantic digest covers the complete private message plan, including the serialized message hash. Byte equality, not merely independently supplied hashes, binds signer output to that message. All bytes are covered by alteration tests.
- `orca-signing-composition-v1.mjs` exposes only the existing authenticated Wiggles control-plane API. Its execution and signing ports are private closures. It uses executor-owned durable state for disposal quantity/evidence, checks the challenge-bound build input and exact-message fee binding, and requires the matching `KEY_LOAD_STARTED_AMBIGUOUS` row before invoking the injected signer once. A process-local consumed plan supplements, but never substitutes for, durable consumption.
- `orca-signed-wire-boundary-v1.mjs` verifies the exact message and Ed25519 signature, exclusively creates the ordinal-specific file, fsyncs file and directory, and reopens with no-follow and metadata/readback checks. The enclosing existing control plane commits `SIGNED_INTENT_DURABLE` after its independent retained-wire verification before returning an intent. No wire is released by the new API, and no transport is added.
- Known signing uncertainty cannot regenerate a plan after reopen through this composition. This guarantee assumes no administrator rollback of authoritative state, as explicitly approved above.

### Evidence limitations and explicit blocking inputs

The message builder is pure, not a live readiness authority. Its raw quote, tick state, blockhash, and fee inputs must be supplied by trusted executor-owned capture. The composition binds those inputs to an issued challenge and the fee to the exact constructed message. A hash is not proof of the truth or freshness of those inputs. The repository does not yet supply the operational capture port that independently validates live pool/tick/vault/token ownership, reconstructs the quote, establishes finalized context and opening balances, and captures the message fee under frozen RPC budgets. Missing inputs fail closed; no live defaults are invented.

The pure builder's retained-quantity argument is not independently authoritative on its own; only the composed path loads that value from the durable episode and checks the acquisition-evidence binding. Similarly, the generic signed-wire utility establishes cryptographic/retention identity, not admission or economic authority.

Tests use inert signer callbacks for the final-proof-shaped composition and a fixed published test key for positive cryptographic retention. They do not sign for the final-proof wallet. A positive operational wallet-signing test is not claimed; broadening the frozen mandate to a test wallet or loading the real key would invalidate this distinction. Existing authenticated-control-plane tests separately cover durable signed-intent completion and revocation races. No new claim of complete end-to-end live signer acceptance is made.

## Remaining implementation work

- [ ] Implement and offline-qualify the executor-owned build/readiness capture port from bounded raw finalized evidence. Reuse calibration validation/quote construction without importing live runners. Its dependency packaging, finite input inventory, freshness criteria, and RPC limits must be explicit; no network execution is authorized here.
- [ ] Freeze production composition/configuration and signer-capability wiring, without a caller-supplied alternative path. The separate live factory remains blocked until all release gates are satisfied.
- [ ] Bind exact retained bytes to the existing reviewed submission/terminal-evidence boundary. Preserve maximum three identical-byte client sends, maximum two rebroadcasts, provider retries zero, and no replacement construction. Do not redesign the scheduler.
- [ ] Independently validate live transmission child artifacts before economic evidence promotion. Preserve existing reconstruction, Orca projection, production bridge, candidate selection, and immutable claim semantics.

## Deployment isolation and permissions

- [ ] Identify and enforce separate Hermes, Wiggles runtime, and trusted provisioning/administration identities. Hermes must not read signer material, modify executor code/state, call provisioning, or reach a raw signer service.
- [ ] Provision a private executor-owned state root; runtime may open/mutate that existing authority but must not silently initialize or restore it. Separate provisioning permissions and audited supervised stop/restore procedures remain necessary.
- [ ] Restrict signer access to the reviewed Wiggles process and protect its code/configuration. Same-UID malicious code is outside local filesystem protection. Private exports, dependency injection, and unit tests are not OS confinement.
- [ ] Establish supervision and evidence retention for ordinary crashes, signing uncertainty, and known restores. Trusted administration must stop rather than reset an episode. No external freshness service is required for option 1.

## Human-supplied identities and authorization

The five missing live-readiness values remain:

1. real human authorization public key;
2. real agent-control public key;
3. acquisition `not_after` Unix second;
4. live RPC budget-table SHA-256;
5. final Wiggles executor release SHA-256.

- [ ] Freeze these values in the mandate and obtain the exact human authorization for one episode. Fixed test identities and the source manifest hash are not substitutes for the real authorization or release identity.
- [ ] Confirm the supervised two-decision scope and human revocation channel. No automatic delegated execution or extra economic authority is introduced.

## Execution window and RPC budgets

- [ ] Final-proof public wallet remains `5CJdSbz9d5CifzFcWL5NcbicgpSAEuDGpSZBgaLHN1tA`; no activity was authorized in this slice.
- [ ] Earliest candidate is `1789216029` (user-provided approximate local time: 12 September 2026, 20:27:09 SGT). Runtime must independently establish the strict seven-day predicate from finalized chain boundaries. Reaching a wall-clock timestamp alone does not satisfy the gate.
- [ ] Freeze acquisition not-after and bounded RPC method/count/time budgets. Actual blockhash/height, quote/fee capture and simulation evidence are blocking execution-time inputs, not reusable synthetic values.
- [ ] Preserve the frozen 5,000,000 raw USDC acquisition, maximum 50 bps slippage, exact per-leg fee/rent equation, full retained chain-derived JUP disposal, and prohibited extra instructions. No Helius Sender, tips, priority fees, or Compute Budget are authorized.

## Reconstruction and demo evidence

- [ ] Retain raw finalized responses and exact signed wire, complete reviewed scheduler/terminal evidence, and checksum-bound inventory through reconstruction.
- [ ] Demonstrate final proof through the unchanged source-authoritative v1.3 economic pipeline and explicit candidate selection; provenance hashes cannot promote claims.
- [ ] Distinguish the previously reported real controlled Orca VERIFIED/CLOSED reconstruction from synthetic bounded-agent fixtures and from the not-yet-performed final-proof episode. This slice does not reverify or repeat live calibration.
- [ ] Present the supervised/trusted-administration limitation explicitly in demo claims. Do not claim automatic restore detection, OS confinement proved by exports, model-reasoning attestation, or synthetic onchain occurrence.
