# Private Helius provider qualification — closeout

Date: **2026-09-10 UTC**. Repository: `/root/artifact/trade-artifact`.

## Disposition

**BLOCKED BEFORE EXTERNAL DISPATCH. Actual Helius qualification remains unresolved.**
The separate local qualification harness passed **16/16 synthetic checks**, and its
external-supervisor cleanup probe retired an escaped-session descendant. **Zero
external RPC requests, zero simulations, zero signatures and zero broadcasts occurred.**
These results establish local harness behavior, not actual provider support.

The concrete credential prerequisite is missing: OPERATOR specifies the private
FD-5 object and custody requirements, but the inspected documentation does not
establish an actual existing custodian/location-to-FD delivery mapping on this host.
A presence-only check found no inherited FD 5 and no documented legacy
`HELIUS_API_KEY` environment input. No credential files or secret stores were
searched/read. Historical Windows `.openclaw/.env` documentation is not a Linux
private-capability delivery mapping. No key was requested in chat.

The user explicitly directed preparation followed by STOP if this mapping or
confinement was missing. This closeout follows that branch. It does **not** claim a
completed live launcher: the implemented supervisor remains local-only; an actual
custodian/DNS/TLS launch binding is still unimplemented. Setup-body and representative
message prerequisites are also unresolved below. The task's desired real-provider
observations were **not obtained**.

Evidence root (**E**): `/root/artifact-private-helius-provider-qualification`.
The frozen prospective specification and zero-call ledger are under
`E/external-session/`. The full exact parameter plan is
`engine/qualification/helius-provider/RUN-PLAN.md`.

## 1. Recovered and preserved committed baseline

- HEAD: **`9c52f30b23e63bf46e32cb5a71611c476c94d279`**, branch `main`.
- Local `origin/main` resolves to the same commit; ahead/behind **0/0**. No fetch
  or remote-state verification occurred.
- Initial worktree: no tracked/index changes; only the two existing September 2
  plans were untracked. They were not modified.
- Applicable ancestor/repository instruction discovery found no `AGENTS.md`,
  `CLAUDE.md` or `.cursorrules` in the checked instruction locations.
- The requested query closeout, provider assessment and OPERATOR were read, along
  with accepted exchange, private FD loader, I/O, source inventory, confinement,
  readiness, finalized-source, RPC budget, simulation, mandate and builder contracts.
- Accepted source manifest: `engine/docs/v1.3-private-helius-query-binding-source-manifest.sha256`.
  **253 members**, all verified; manifest SHA-256
  **`094bedf06a1d8c41b207731afe268b80cc45e049496c9ee21c7b4b5d6a6b27ad`**.
- Accepted runtime: `/root/artifact-private-helius-query-binding/runtime-frozen.json`.
  **5,394 members**, verified for resolved path, regular-file type, bytes, mode and
  SHA-256; exact inventory/R
  **`1686d69ff98d821ee84a3d58722ea9842c04e49e4d2e4438203608913d91bb6c`**.
  The inventory was not regenerated or relabeled to cover this harness.
- Retained focused result was inspected and its TAP rehashed: **106/106**, exit 0,
  SHA-256 `fe7eb439d7db39d98c1e6dec87758f50fe083cc7ba299476eea23abf74dece22`.
- Retained canonical result was inspected and its TAP rehashed: **604 tests, 601
  passed, zero failed, three existing skips**, exit 0, SHA-256
  `848d43ecd40f0ffa04d509dc9aa48961ebdb91aa92869ace4c88d3a432bc5e93`.
  **Neither historical selection was rerun. No canonical suite ran.**

`E/baseline-before.json` and `E/baseline-after.json` bracket preservation. They bind
all accepted source/runtime members, existing documentation and plans, retained
result/TAP identities, and metadata inventories of the query-binding, FD-repair and
original private-binding evidence roots. Metadata covers paths, mode, size, mtime,
ctime and inode, excluding access time. No historical evidence/report is overwritten.

## 2. Separate harness, dependency identity and actual confinement

New code lives under `engine/qualification/helius-provider/`, **outside** the
accepted private-binding source scan. Its dependency surface is deliberately small:
byte-identical copies of accepted `exchange.mjs`, `io.mjs`, `openssl.cnf`, the JSON
contract, supervised profile and supervised RPC module, plus the accepted Node/ELF
runtime. The qualifier never imports a production composer, signer, authority DB,
readiness/episode-admission constructor or submission adapter.

`bounded.mjs` uses the accepted query constructor and FD I/O contract. It adds a
closed read-only method inventory, total/per-method pre-dispatch accounting, request
and aggregate-response ceilings, synchronous durable request/reservation records,
a post-retention deadline check and a one-shot exchange call. The remaining aggregate
allowance is passed into the accepted streaming limit. It retains original response
bytes only after that exchange admits them. Failure closes the session; reservations
remain consumed, including interrupted/ambiguous requests. There is no retry,
automatic continuation, endpoint selector or submission path.

### Executed confinement facts

The actual local Node process ran in a newly created chroot with private mount,
PID and network namespaces, under the **existing UID/GID 65534**, empty groups and
**zero CapInh/CapPrm/CapEff/CapBnd/CapAmb**. `no_new_privs` was set, core limits were
zero, and seccomp denied ptrace, process-memory I/O, io_uring and namespace/root/mount
escape operations. Regaining UID 0 and writing the copied runtime were refused.

Only named public runtime/source copies were present. Host wallet/key/credential
stores, authority databases and trading-state directories were not mounted or
copied; `/proc` exposed the private PID namespace, not host signer processes. An
outside, publicly readable **synthetic** file was accessible before chroot and
inaccessible afterward, including through `/proc/1/root`. No real signer path was
opened to demonstrate denial. The root parent only prepared/collected temporary
fixtures and supervised processes; the HTTP client ran after privilege dropping.
This is actual signer **inaccessibility in the tested qualification jail**, not a
root parent's promise not to sign and not production-host qualification.

Inherited descriptors were closed with `close_range(3,4)` and
`close_range(6,UINT_MAX)`, retaining only controlled 0–2 and the intended FD 5.
An inherited file and connected local socket at FDs **1000/1001** were confirmed
closed after lowering the FD limit to 256. FD 5 carried only a synthetic canonical
provider object; the accepted bounded loader wiped its read buffer and closed it.
The local network namespace had only loopback and local routes. No external DNS,
TLS or provider traffic was possible during these tests.

All changed permissions/ownership applied solely to newly created temporary
copies/directories and synthetic fixtures. Existing permissions, production users,
services, wallets, state and release paths were not changed.

### Identities

- `E/harness-source.sha256` (the five new harness/plan files):
  **`8b1e25994fce27d676faf5f83d079b45791aa3aa91f2e944778e4bbafb961aa0`**.
- Frozen blocked `E/external-session/run-spec.json`:
  **`2fef59e4b5a35a23de503f70f94e49ba794430e28841a20db354bac0df1926fc`**.
- `E/supervisor-dependencies.json`:
  **`206830fa49c07de68ea1b03f8207f2ab7c68470ecbd693c09a8ddef27cb8755c`**.
  This records accepted Python/runtime members and observed supervisor utility/ELF
  dependencies. Utility identities were collected **after** local execution; they
  are not falsely described as a pre-exec frozen host inventory.
- Every local attempt retains `jail-identity.json`, `harness-identity.json`, command,
  process, isolation and result records. The final successful four code-file hashes
  still match current code. The plan/report are documentation added afterward.
- `E/qualification-files.sha256` individually inventories all new repository files
  and the external bootstrap script. `E/SHA256SUMS` inventories each retained evidence
  file, excluding only itself. None of these identities is accepted R, C or D.

## 3. Local verification and exact attempt ledger

Only the new synthetic local checks and cleanup probe were executed. Each attempt
used an exclusive evidence directory; failed attempts remain retained. Local process
IDs below are historical host PIDs, not presently running processes.

| Directory under E | Host PID/PGID | Child exit | Result | Elapsed seconds |
|---|---:|---:|---|---:|
| `local-1` | 1642144 | 1 | Confinement established; copied ELF loader lacked execute permission, so Node did not start. Temporary-copy packaging error. | 0.2849065773189068 |
| `local-2` | 1642292 | 1 | 15/16 local checks; FD-lifecycle assertion inspected number 5 after later I/O could reuse it. Test error, not accepted loader failure. | 1.453472058288753 |
| `local-final` | 1642983 | 0 | **16/16**, no timeout; final exact code. | 2.940516202710569 |
| `cleanup-probe` | 1643069 | -9 | Intentional two-second external timeout; escaped-session descendant created and retired; supervisor exit **0**. | 2.057102889753878 |

All four records show **zero real-provider requests**, empty residual PID lists and
removed temporary roots. The cleanup probe establishes whole-namespace retirement,
not merely killing the initial process group. Final local evidence records zero
remaining sockets. Its synthetic server received **30** requests across independent
negative/positive test cases; the complete synthetic planned read path accounted
for **19**, including nine history calls. These are **not** external session calls.

The 16 final checks cover private FD loading/close and exact clean bytes; zero-dispatch
closed-method denial (including submission/bundles/simulation); total and per-method
exhaustion; request-byte denial; aggregate admission exhaustion; per-response and
remaining-aggregate stream bounds; absolute and in-flight deadlines; terminal no-retry
behavior; credential-echo rejection; generic HTTP 401/429 failure; exclusive retention;
the complete planned synthetic read sequence; and owned socket cleanup.

Two attempted terminal invocations were refused **before execution** by an embedded-null
lifecycle-guard tool error. The same authorized local Python scripts were subsequently
executed through the execution tool. This was not an RPC retry or a canonical attempt.
No accepted source defect was demonstrated or repaired. No successful final test
selection was repeated after its result.

## 4. Frozen external allocation and actual call ledger

`E/external-session/run-spec.json` was written in an exclusive directory with the
non-secret authorization, fixed endpoint/capability, exact public addresses, parameter
source/derivation, method maxima, limits and explicit missing process identity.
**No external process was created**; its host PID/starttime/namespace are null, not
invented. The record is a blocked prospective specification, not a dispatched run.

| Method | Frozen maximum | Actual external calls |
|---|---:|---:|
| `getGenesisHash` | 1 | 0 |
| `getSlot` | 1 | 0 |
| `getBlock` | 1 | 0 |
| `getMultipleAccounts` | 2 | 0 |
| `getAccountInfo` | 1 | 0 |
| `getTokenAccountsByOwner` | 2 | 0 |
| `getSignaturesForAddress` | 15 | 0 |
| `getLatestBlockhash` | 1 | 0 |
| `getBlockHeight` | 1 | 0 |
| `getTransaction` | 0 | 0 |
| `getFeeForMessage` | 0 | 0 |
| `simulateTransaction` | 0 | 0 |
| All other methods, including every submission/bundle method | 0 | 0 |
| **Total** | **25** | **0** |

The plan retains tighter readiness limits: **5,000 ms/request**, **1 MiB/request and
response**, **16 MiB aggregate admitted response bodies**, **55-second inner sequence**,
and a required **60-second external launch/cleanup bound**. The latter is a prospective
live requirement, not a claim that a live launcher was exercised. Four scan pages per
history lane plus one repeated head are maxima, not targets; no pagination cap was
raised. No transport retry, fallback, auth negotiation or session restart is allowed.

Actual external ledger: **zero admitted bytes, zero simulations, zero signatures,
zero broadcasts, no partial external request and no external process to clean up**.
There was no authentication attempt, so even credential validity is unknown. No
account entitlement, current quota, credit balance or monetary-cost claim was invented;
a custodian must identify any tighter account restriction before dispatch.

## 5. Provider findings and unresolved contracts

### Observed PASS — local only

The executed local confinement, allowlist, budget, failure-screening, exact clean
byte-retention and cleanup checks above passed. They do not establish Helius behavior.

### Observed provider refusal/incompatibility

**None observed: no provider request was dispatched.** Synthetic 401/429/echo
responses are local fixtures, not observations about this account or Helius.

The unchanged accepted exchange collapses non-200, network/TLS and screening failures
to fixed `PRIVATE_RPC_UNAVAILABLE`. A future missing clean response must therefore
be reported as an unresolved transport/screening refusal, not guessed as a particular
HTTP/auth/rate-limit cause. Stopping on all such failures is conservative; logging
raw headers, causes or rejected echoes to improve diagnosis is not authorized.

### Not tested

- Actual query authentication; mainnet genesis/finalized anchor; DNS/TLS/CA behavior.
- Real account reads, classic/Token-2022 population, context floors and exact
  cross-call watermark feasibility. Equal observed watermarks would not prove atomicity.
- Actual bounded history exhaustion/head stability, setup-body support or completeness.
  The inspected committed final-proof contracts provide setup schema/time and digest
  requirements, not admitted authentic setup transaction references for this harness.
- Dynamic tick arrays, source-supported binary route/account decoding and current quote.
- Exact-message fee, representative unsigned legacy construction and simulation.
  `orca-message-boundary-v1.mjs:19–36` requires a validated mandate and exact quote/fee
  inputs. This task did not create a mandate or repurpose its synthetic fixtures as
  real authority. No representative message was constructed; consequently **there
  are no unsigned request bytes, blockhash/message identities or simulation response
  bytes to report**. No no-op was substituted.
- The Helius simulation documentation discrepancy remains completely unresolved.
  Future admissible simulation must retain `commitment:'finalized'`, `sigVerify:false`,
  `replaceRecentBlockhash:false` and the required `minContextSlot`; distinguish accepted
  parameters, `err:null` execution success with required fields, program/economic
  refusal, transport failure and not tested. This plan allocates zero simulation calls
  rather than pretending these prerequisites were satisfied.

### Provider/host assumptions requiring confirmation

A sample cannot prove universal history completeness or omission-free owner scans,
provider-side query-secret/access-log retention, real broadcast `maxRetries` behavior,
account entitlement, production host isolation or future readiness. The local chroot
proof does not establish an installed production controller/executor/custodian setup.
The live network and private-descriptor custody binding remains genuine unfinished
work, not a flag to remove from the tested loopback runner.

Seven-day eligibility was not certified or bypassed. No eligible anchor, mandate,
control decision or trading authority was manufactured. Public observations, had
there been any, would not authorize wallet changes or future episode admission.

## 6. Individually inventoried new files

No existing repository file was modified. New files are:

| Repository-relative file | Purpose |
|---|---|
| `engine/qualification/helius-provider/bounded.mjs` | Private FD loader and bounded, durable, closed-method read session using accepted exchange bytes. |
| `engine/qualification/helius-provider/probe.mjs` | Non-authoritative fixed-public-address read sampling, dependent floors, bounded histories and explicit unsupported findings. |
| `engine/qualification/helius-provider/local.mjs` | Synthetic-only loopback checks; no signing or external dispatch. |
| `engine/qualification/helius-provider/supervise.py` | Temporary local confinement, separate runtime-copy identity, hard deadline and owned cleanup. **No live launch mode.** |
| `engine/qualification/helius-provider/RUN-PLAN.md` | Exact blocked-run plan, maxima, derivations and missing prerequisites; not a production command. |
| `engine/docs/v1.3-private-helius-provider-qualification-closeout.md` | This closeout, outside accepted source/R preimages. |

Additional external source artifact:
`/root/artifact-private-helius-provider-qualification-bootstrap.py`, a read-only
baseline/preservation recorder, SHA-256
`490499baf78bcc2e80f2478959ebb52e811841730652d0d7e6b1be0a1d711af6`.
All generated local evidence, specification, ledgers, identity files and copied
closeout are individually listed in `E/SHA256SUMS`. Historical evidence remains in
its original directories. Final checks include accepted source/runtime preservation,
unchanged docs/plans/history metadata, current code equality to the final local
selection, new-file whitespace and a synthetic credential/target scan of final local
evidence. Tracked/index diffs remain empty; six new files plus the two unrelated
plans remain untracked for the user's Git control.

## 7. Smallest concrete next action and STOP

**Obtain the custodian's non-secret confirmation of the existing provider credential
location/delivery-to-FD-5 mapping and any tighter account quota. Do not supply the key
in chat.** If no such delivery exists, explicitly authorize the smallest qualification-only
custodian/network binding rather than pretending the local runner is live-ready.
The authentic public setup references and admissible representative-builder inputs
must also be resolved before expanding the present zero-body/zero-simulation allocation.

No production C/D, activation, Git mutation, delegation, installation, provisioning,
funding, real credential read, signing, broadcasting, canonical test or subsequent
repair/review cycle occurred. Changes are uncommitted and unpushed. **STOP after
this closeout and its preservation/inventory checks; no external run is retried or
performed merely to clear a status label.**

---

## 8. Qualification continuation — local GREEN, authentic setup still missing

This appendix preserves the complete original blocked closeout above. It records
the subsequently authorized implementation and explicitly renewed consent for the
pending corrected local-only verification. It does not renew the external session
allowance. Continuation evidence (**C**):
`/root/artifact-private-helius-provider-qualification-continuation`.

### Actual resumed verification

Exact pending command:
`/usr/bin/python3 -B engine/qualification/helius-provider/local-successor.py /root/artifact-private-helius-provider-qualification-continuation/local-2`.
This is the qualification fixture driver, **not `npm run test`**.

The ordinary terminal tool again failed before execution with its lifecycle-guard
`embedded null byte` error. This was a tool failure, not an approval refusal. The
previously pending execution-tool request was submitted through its normal controls
under the user's renewed consent and completed with **exit 0**. The expired request,
earlier pause, original failed local attempt and new successful execution are kept
distinct in `C/continuation-history.json`; no expired request is relabeled executed.

| Evidence under C | Actual result |
|---|---|
| `local-1/local-summary.json` | Original failed successor attempt retained. Custody/construction checks reached the TLS test; new temporary resolver files were root-only and unreadable after dropping privileges. |
| `local-2/local-summary.json` | Nine synthetic custody checks passed; confined worker exit 0, released after isolation verification, no timeout, no residual processes, temporary root removed. |
| `local-2/confined/evidence/successor-local-results.json` | Nine checks passed: supported quote/message construction, fee contradiction, context-floor contradiction, invalid tick, simulated program refusal, replacement rejection, pre-dispatch submission denial, exact allocation, fixed-hostname synthetic TLS through the private-network relay. |
| `cleanup-probe/result.json` | Intentional external timeout; child exit -9, isolation established, no residual processes, temporary root removed. |
| `cleanup-probe/evidence/escaped.json` | Descendant created a separate session (namespace PID/SID 9); it was retired with the owned PID namespace. |
| `entry-syntax.json` | New live worker entry and successor probe pass syntax checks using the accepted inventory-resolved Node. This is not end-to-end live composition coverage. |

The resolver correction applies mode **0444 only to newly created temporary jail
configuration files**. Existing host file permissions were not changed. All code
listed in the successful local run's source identity still matches its tested bytes.
The final worker entry and successor plan were added separately afterward, and are
not falsely included in that earlier test coverage.

The TLS fixture uses a disposable self-signed test certificate/key generated by the
existing OpenSSL fixture pattern. This is not a wallet or human/agent control key.
No wallet/control signatures or transaction signatures were created. All constructed
transaction signatures remain zero. The single received TLS fixture method was
`getGenesisHash`; it was local synthetic traffic, not a provider observation.

### Custody and confinement now implemented as qualification candidates

`custody.py` implements the exact authorized `/root/.openclaw/.env` named-variable
read with no-follow/type/owner/mode/link/size/change checks, literal-data parsing,
closed sanitized failures, private canonical FD-5 delivery and temporary cleanup.
Metadata observed for the actual source: regular file, UID/GID 0, mode 0600, one
link. **The actual credential was not read or tested.** Synthetic extraction and
delivery passed; the real read remains delayed until authentic public prerequisites
permit a real qualification launch.

The new broker, relay and worker run unprivileged in a private PID/mount/chroot
domain, with empty groups, zero capabilities, no_new_privs, zero core limits and
seccomp. The worker and relay retain a loopback-only private network namespace.
The broker is credential-blind and transfers connected fixed-target sockets; TLS
terminates only in the accepted exchange and endpoint. Worker keeps only controlled
0–2/5; broker/relay keep explicit transport capabilities, not credential FD 5.
No host signer/state or host process namespace is mounted. Parent records verified
isolation and process identity before releasing the worker.

Local GREEN proves the tested confinement/relay/TLS fixture and cleanup behavior,
not real external DNS, public-CA interoperability, account entitlement or installed
production-host qualification. The outer supervisor's process timeout is 60 seconds;
a strict whole-live-session-through-evidence-cleanup wall-clock guarantee has not
been established by the local fixture, and remains a release gate rather than an
assumed guarantee.

### Authentic setup recovery — concrete blocker

Bounded metadata search under `/root/artifact-*` (depth at most three), followed by
candidate public reports and explicit public references, recovered the September 5
r13 **setup procedure**, not the executed wallet setup evidence. Archive member
names were inspected first; only named public procedure members were read, without
extracting private material. All six embedded procedure-member hashes verified.
Observed procedure archive SHA-256:
`f8da43e28ce5f9d0bdd0372ce9eee56f343ff990e58877847e26c4fc567b3868`.
No independent recorded archive digest was recovered, so this is an observed byte
identity, not independently authenticated archive provenance.

The procedure explicitly names Wiggles public evidence under
`$HOME/.artifact-calibration-local/v1/public/final-proof-wallet/`. The corresponding
`/root` public directory is absent on this host. No secret sibling or wallet key
was searched or opened. Exact missing artifacts are the executed `setup-freeze.json`,
public wallet manifest, ATA/funding finalized records and signatures, their referenced
evidence manifests, and the actual execution-evidence archive. See
`C/setup-recovery.json`. Calibration bodies and synthetic digests remain separate;
the procedure archive hash was not substituted as a real setup hash.

Without these records a truthful complete public construction descriptor cannot
be frozen. The final worker's real-input/setup/probe composition also remains
unexercised. No `READY.json`, authentic `public-input.json`, live process or live
session reservation was created. These are concrete remaining gates, not a request
to invent a mandate or establish eligibility from current RPC observations.

### Successor allocation, identities and preservation

`RUN-PLAN-successor.md` and `C/successor-session/run-spec.json` explicitly succeed
the original specification without modifying it or creating another allowance.
The new allocation is **29 maximum requests**, including **two setup bodies, one
exact-message fee and one unsigned acquisition simulation**. All original method
maxima remain otherwise unchanged; the route multiple-account call now includes
dynamic tick arrays. Limits remain 5,000 ms/request, 1 MiB/request/response, 16 MiB
aggregate admitted responses, 55-second inner sequence and 60-second outer process
timeout. Every failure consumes its reservation; zero retries/fallback/restart.

Simulation parameters remain finalized, `sigVerify:false`,
`replaceRecentBlockhash:false`, base64 and the exact fee-response context floor.
The accepted quote and unsigned builder are reused without readiness admission,
authority state or an alternate swap builder. The local positive case is explicitly
synthetic, not a fresh final-wallet quote, fee or provider simulation.

- `C/harness-identities.json` SHA-256:
  `831f3d1c244b644f8eac4a90f401cfbc9aea2386fa54b2805ab576db890e02a7`.
- Successor specification SHA-256:
  `c841b23b4cdbddcb704d2c790baafeeae49b377cc878026da548ed22f636a9da`.
- Per-run `jail-identity.json` and `harness-identity.json` separately identify copied
  dependencies and qualification code; none is relabeled accepted production R.
- `C/baseline.json` and `C/baseline-after-local.json` agree: all **253 accepted
  source members and 5,394 runtime members** match the original identities.
- `C/predecessor-hashes.json` binds the unchanged original evidence, plan and
  harness. This report's original prefix is preserved byte-for-byte.

**Actual external totals: 0 RPC requests, 0 provider simulations, 0 admitted provider
bytes, 0 real credential reads, 0 transaction signatures and 0 broadcasts.** The
single authorized provider session is still unused. No actual provider PASS or
refusal is reported. The original NOT_DISPATCHED specification/ledger remain intact.

### Individually identified continuation files

All paths below are under `engine/qualification/helius-provider/` unless stated.

| File | Continuation change |
|---|---|
| `custody.py` | New exact-source credential delivery and sanitized failure handling. |
| `launch-successor.py` | New confined broker/relay/worker launcher candidate; temporary resolver-permission correction. |
| `bounded-successor.mjs` | Separate 29-call closed allocation; original bounded module preserved. |
| `construction.mjs` | New qualification-only accepted quote/build, exact fee, zero-signature simulation path. |
| `probe-successor.mjs` | Separate read probe with setup-body and construction integration. |
| `worker-successor.mjs` | New final worker entry; separately syntax checked, full live composition unexercised. |
| `synthetic-public-input.mjs` | Local-only public fixture data; no private seeds or signing helpers copied. |
| `local-successor.mjs` | Synthetic construction, refusal, quota and TLS checks. |
| `local-successor.py` | Synthetic custody checks and disposable TLS fixture driver. |
| `RUN-PLAN-successor.md` | Separate revised allocation, relationship, evidence gates and limitations. |
| `engine/docs/v1.3-private-helius-provider-qualification-closeout.md` | This append-only continuation. |

No canonical suite, production activation/provisioning, install, delegation, wallet
change, real signing, broadcast or Git mutation occurred. Changes remain uncommitted
and unpushed. Stop on the authentic public-evidence blocker; no exploratory provider
session or canonical rerun may substitute for it.

---

## 9. Complete-worker continuation — local composition GREEN; session unused

This continuation preserves §§1–8 verbatim. Actual HEAD remains
`9c52f30b23e63bf46e32cb5a71611c476c94d279` on `main`. No tracked/index changes,
commit, push, installation, activation, provisioning or delegation occurred.
Both unrelated plans and historical evidence are preserved. New evidence (**W**):
`/root/artifact-private-helius-provider-qualification/worker-continuation/`.
This is the same remaining provider-session allowance, not a restart allowance.

### A. Actual complete worker, not component-only or syntax coverage

`worker-local.py` now invokes the actual `worker-successor.mjs` entry through
`launch-successor.py` in a separately named **worker-local** mode. That mode fixes
the broker destination to the numeric-loopback synthetic TLS server; it cannot
read the real credential. The private FD-5 synthetic capability, accepted fixed-host
query exchange, public-input loader, setup-body checks, accepted route decoder,
quote and unsigned builder, exact fee corroboration, bounded probe, byte retention
and namespace cleanup all execute. The fixture preparer finishes before the actual
worker exec; it does not replace the worker with a mocked orchestration function.

Synthetic setup records and their actual local byte hashes are retained under each
case. The descriptor's archive field is explicitly labeled a **synthetic bundle
identity, not an authentic setup archive**. Retained calibration route bytes are
used only as fixture data. Fee results, setup signature strings and simulation
responses are synthetic. No constructed descriptor is authenticated permission,
an admitted episode or eligibility evidence. The genuine worker/probe/construction
source was exercised unchanged; none of its assertions was weakened.

Final command, executed once on the repaired launcher:
`/usr/bin/python3 -B engine/qualification/helius-provider/worker-local.py /root/artifact-private-helius-provider-qualification/worker-continuation/worker-final all`.
Driver exit **0**, **four complete-entry cases passed**:

| Case | Worker exit | Local TLS requests | Observed result |
|---|---:|---:|---|
| `complete` | 0 | 23 | Full read/history/setup/construction/fee path; one zero-signature unsigned simulation succeeds. |
| `input-extra` | 1 | 0 | Closed public-input schema refuses before RPC dispatch. |
| `setup-contradiction` | 1 | 16 | First mismatching setup signature body stops the sequence; no construction or simulation. |
| `simulation-refusal` | 1 | 23 | Program refusal retained; sequence stops, no retry or replacement. |

The final cases total **62 local fixture requests**, including two local synthetic
simulations in separate tests. They consume **zero external allowance**. Every
observed body is compared byte-for-byte with the retained request/response files;
noncanonical response whitespace is intentionally preserved. Reservation/completion
hashes and lengths, per-method counts, admitted-byte totals, and closed ledgers agree.
The positive wire equals the exact retained message prefixed by one all-zero
signature; the required finalized, `sigVerify:false`, `replaceRecentBlockhash:false`
and fee-context-floor parameters are asserted at the synthetic server.

All four workers were released only after isolation verification. All returned
empty residual-process lists and removed temporary jails. Disposable TLS material
and the server were cleaned up; retained worker evidence contains neither the
synthetic credential nor credential-bearing query targets. No wallet/control
signature was produced. The TLS certificate fixture is not a wallet/control key.

### B. Genuine failure, bounded fix and preserved attempt history

`W/worker-1` returned driver exit **1**: positive, malformed-input and program-refusal
cases passed, but setup-contradiction stopped during isolation verification before
worker release. Its retained `relay-isolation.json` is **zero bytes**. The parent
checked final-name existence while `base.put` had opened but not finished writing
the report. This was a qualification-launcher publication race, not a provider
refusal or a production query-binding defect. The initial attempt's **46 local
requests** and all partial records remain retained.

The sole existing-code fix publishes each isolation report by an exclusive hard
link only after writing/fsyncing a temporary report, then removes the temporary
name and fsyncs its directory. No incomplete final-name report is exposed.
`isolation-publication-local.py` executes the actual publication AST with only
the public evidence path redirected to a temporary directory and an observer at
the open-before-write boundary. Exact reconstructed predecessor source hash is
verified against the failed run before the RED probe:

- Pre-fix launcher: `dc1bb0230249684a1fa077c86e8950ffc95f38e8b041ada4d63761c502cb3557`.
  `publication-red-exact/result.json`: exit **1**, final name visible during
  incomplete write — reproduced RED.
- Final launcher: `79620f4f6d8e52bf2ce1ebe9bbe1185cb5e838fe58022a6c334a87f0411faddd`.
  `publication-green-exact/result.json`: exit **0**, final name absent during
  incomplete write and complete afterward.

The first reconstruction-check attempt had an extra newline and failed its source
hash assertion before the race probe. It is not counted as race RED. The separate
regression driver was corrected and verified; it is not in the worker executable
closure. A readback of the original empty report also failed JSON parsing before
its proposed diagnostic subprocess could run. `W/execution-history.json` separates
these events. Terminal lifecycle-guard `embedded null byte` errors occurred before
execution; the same local/inspection actions then used the execution tool's normal
controls. These were tool errors, not approval denials. Earlier TLS-fixture failure,
resolver-permission correction and expired approval records remain unchanged.

Because the launcher changed, its separate cleanup probe was exercised on final
bytes: `W/cleanup-final/result.json`, supervisor exit **0**, intentional two-second
timeout, child exit **-9**, escaped-session descendant retained in `escaped.json`,
no residual processes and temporary root removed. Earlier successful component
checks and canonical tests were not rerun. The final complete-worker selection
was not repeated after GREEN.

### C. Exact recovered public references and remaining absence

The identified public source is the existing procedure archive:
`/root/artifact-final-proof-wallet-wiggles-setup-procedure-20260905-r13.tar.gz`.
Archive member names were inspected before reading the named public members.
Under its `artifact-final-proof-wallet-wiggles-setup-procedure-20260905-r13/` prefix:

- `RUNBOOK.md:5` names host **`wiggles`** and the existing operator-home hierarchy.
- `RUNBOOK.md:37–39` names exactly
  **`$HOME/.artifact-calibration-local/v1/public/final-proof-wallet/`**.
- `wiggles-final-proof-wallet-setup.mjs:61–70` derives HOME with `homedir()` and
  defines the exact public filenames below; `:152–153` checks the Wiggles hostname
  and invoking user's UID.

The historical context is **the account that executed setup on Wiggles**, not an
assumed root account on this host. Neither identified public source states that
account's username or resolved absolute home. **Those values remain unrecovered;
no `/home/<guessed-user>` or `/root` historical location is asserted.** The inspected
procedure also does not name the execution-archive basename. The procedure archive
is present; it is not the missing public execution-evidence archive.

Missing from the inspected local evidence are the authentic originals:

1. `wallet.json` — final-wallet public manifest.
2. `setup-preflight.json` — source-bound observed rent/fee/opening contract.
3. `ata-creation-finalized.json` — finalized ATA-creation record and its exact signature.
4. `funding-finalized.json` — finalized funding record and its exact signature.
5. `setup-freeze.json` — `ARTIFACT_FINAL_PROOF_SETUP_FREEZE_V2` and original setup boundary.
6. Referenced `preflight-<timestamp>/`, `ata-creation-<timestamp>/`,
   `funding-<timestamp>/`, `setup-freeze-<timestamp>/` public evidence directories,
   each original `evidence-manifest.json` and every listed raw-response/retention member.
7. The original **public execution-evidence archive and its independently retained
   checksum/reference**. Its literal basename/path and the two actual setup
   signature values were not recovered and must not be invented.

The previous depth-three discovery and absence of the explicitly mapped
`/root/.artifact-calibration-local/v1/public/final-proof-wallet/` were not repeated.
The new bounded metadata search covered only matching filenames at depths **4–6**
under **38 Artifact roots**, with secret/key/store/dependency directories pruned:
**20,355 entries**, below its 100,000-entry ceiling, **zero candidates**. No identified
deeper local copy was available to inspect. This is scoped non-recovery, not global
proof of absence. No secret stores, wallet-key files or raw session logs were read.

`W/public-setup-location.json` retains exact member/line references and scope;
SHA-256 `5e3bbd91e7deb88e6c9c9ec489f1663847f43b453428a4b1e552c772fdba1df8`.
The observed procedure-archive hash remains
`f8da43e28ce5f9d0bdd0372ce9eee56f343ff990e58877847e26c4fc567b3868`, not an
independently authenticated execution-archive identity or usable setup digest.

### D. Final applicability, external accounting and stop

`W/source-applicability.json` SHA-256:
`387785eefc2616d4a8b638782439000f5c6df679c1dbcc5d002b9c92eb0f8505`.
All **253 accepted source members** and **5,394 runtime members** still match the
user-pinned manifest/R, including resolved runtime paths, modes, lengths and hashes.
All **4,550 copied dependency records** for the final worker match current source.
The launcher, worker, fixture preparer and integration driver match the tested
snapshot. Only the separately executed publication-regression driver changed
after that snapshot; it is neither imported nor copied into the worker jail.
Accepted source/runtime and production live-entry refusal gates remain unchanged.

`W/worker-final/summary.json` SHA-256:
`37bdecdeaa2dc9f1d349442f6b8ba57412939c2111cc64bc1625343f6d415abe`.
`W/cleanup-final/result.json` SHA-256:
`334f5d4e2aecf22a0d089aa13f1061b5407a850908e029e1f8b1f3826a1abc26`.
Per-case exact evidence identities are in the applicability record. Final process
checks found no surviving matching identity among **16 retained process records**;
no indiscriminate process termination was performed.

**Actual external results: NOT_DISPATCHED; 0 application RPC requests, 0 simulations,
0 admitted response bytes, 0 real credential reads, 0 signatures, 0 broadcasts.**
`W/external-status.json` rehashes both original and successor specifications/ledgers.
They are unchanged; no READY record, authentic public input or live session
reservation was created. The existing successor maximum remains **29 requests /
one unsigned simulation**, within the original 40/two ceilings. No allocation or
session allowance was replenished, transferred or consumed by synthetic traffic.

Local composition GREEN does not establish public DNS/CA/provider behavior or make
the live launch releasable. The previously disclosed hard **whole-live-session**
bound through preparation, evidence retention and cleanup remains a release gate:
the current process timer starts after preparation and does not itself enforce
a deadline on parent cleanup. Successful measured local cleanup is not claimed
as that stronger guarantee. Missing authentic setup alone already prohibits real
dispatch, so no credential read or speculative provider sample was used to proceed.

Individually changed files in this continuation:

| File | Change |
|---|---|
| `engine/qualification/helius-provider/launch-successor.py` | Synthetic complete-worker launcher mode and atomic isolation-report publication repair. |
| `engine/qualification/helius-provider/worker-fixture-input.mjs` | New explicitly synthetic public setup/input preparer, no signing. |
| `engine/qualification/helius-provider/worker-local.py` | New complete-worker local TLS integration and exact evidence/cleanup assertions. |
| `engine/qualification/helius-provider/isolation-publication-local.py` | New deterministic exact-predecessor/current publication regression. |
| `engine/docs/v1.3-private-helius-provider-qualification-closeout.md` | This append-only continuation. |

Other qualification code and both run plans remain unchanged. External-only
inspection/evidence helpers and all command/result records are inventoried in
`W/SHA256SUMS`; no prior inventory is rewritten.

**Smallest next action:** from the **existing setup operator account on `wiggles`**,
provide the original **public-only** subtree
`$HOME/.artifact-calibration-local/v1/public/final-proof-wallet/`, including the five
named JSON records and their referenced evidence members, plus the original public
execution archive/checksum and the non-secret resolved HOME/account context. Do not
include secret siblings, regenerate setup, fund anything or substitute current RPC
history. This is a concrete artifact request, not a request for a key or new session.
After authentic input recovery, the remaining whole-session release gate must also
be satisfied before using the existing allowance. **STOP here; no canonical test,
provider dispatch, new review/repair cycle or subsequent slice is initiated.**

## 10. Bounded outer-deadline repair and recovered-public-input continuation

This section supersedes only §9's remaining launcher deadline gap and its statement
that the five public setup records/signatures were unavailable. All previous
sections, failed attempts, passing worker evidence, run plans and external ledgers
remain historical records, unchanged. No provider qualification result is added.

Evidence root `D` is
`/root/artifact-private-helius-provider-qualification/deadline-and-recovered-setup/`.
HEAD remains `9c52f30b23e63bf46e32cb5a71611c476c94d279`; changes remain uncommitted.

### A. Exact timing boundary and ownership

`launch-successor.py:run()` takes one monotonic timestamp before preparation and
calls the new qualification-only `deadline-supervisor.py`. The frozen outer limit
remains **60 seconds**. Its fixed absolute work cutoff is start+50, management
cutoff start+55, and final cutoff start+60. The last ten seconds are reserved
**inside**, not added to, that limit. The cleanup-only synthetic fixture retains
its two-second limit, with proportional cutoffs at +1 and +1.5 seconds. No phase,
worker, recovery helper or evidence writer receives a reset deadline.

Preparation, input/credential custody gates, worker execution, evidence copying,
process inspection, removal and result-file writes now execute beneath an owned
outer PID/mount namespace. The external parent does no evidence filesystem I/O in
its timer loop. It registers namespace-init pidfds before releasing management,
uses nonblocking bounded control messages and pidfd readiness, and preserves the
existing inner worker namespace, process-group, UID, FD and confinement machinery.
The namespace-init/process-group ownership covers descendants that call setsid.
An unreaped wrapper PID also protects the pre-registration process-group kill
from PID reuse. The external observer is outside the supervised namespaces.

Absolute work-deadline checks precede preparation's continuation, custody and
release. The child independently checks that same cutoff before accepting release
and before exec: a late release-file publication cannot authorize a late worker
start. A release-attempt progress message is conservative (`released:null`) until
publication is acknowledged; an interrupted publication is not reported as proven
NOT_DISPATCHED or permitted to restart. Existing durable live reservation behavior
is unchanged. `release_status:RESERVED_POSSIBLY_RELEASED` is the retained reservation
event; the separate `released` boolean/null carries the later observed state.

At management cutoff the observer signals the owned namespace init and wrapper,
then uses only the remaining original budget for a separately supervised recovery
and evidence writer. Public partial evidence is copied before workspace removal.
Recovery is not attempted as confirmed cleanup while namespace retirement is
unconfirmed. No unbounded waitpid, evidence copy, fsync or rmtree runs in the
external observer. Outer-boundary and inner results are distinct from the final
returned observation and `outer-result.json`; a stalled writer may prevent that
last file from being durably published, which is reported, not hidden.

`cleanup_confirmed`, `namespace_retirement_confirmed`,
`supervisor_domains_retired`, `temporary_root_removed` and
`outer_record_retained` are separate facts. An incomplete observation or stalled
finalization yields `OUTER_RETENTION_OR_CLEANUP_UNCONFIRMED`, not a successful
cleanup claim. Partial-evidence locations are retained for explicit follow-up;
there is no automatic retry, new allowance, or follow-up cleanup authorization.

**Limits:** this is deadline-driven Linux supervision, not hard-real-time scheduling
or an unconditional ability to kill uninterruptible kernel work. SIGKILL can remain
pending; filesystem durability, namespace retirement or removal can remain
unconfirmed. The observer does not wait indefinitely to convert uncertainty into
success. Kernel/scheduler stalls of the observer itself cannot be bounded by this
userspace mechanism. Caller/interpreter bootstrap and delivery of the returned
result are not claimed to be an independently supervised OS execution environment.

### B. Focused RED and final GREEN

`D/launch-before.py` preserves the exact predecessor. `preparation-red-command.json`
retains exit **1** and `PREPARATION_ESCAPED_FROZEN_OUTER_DEADLINE`: the three-second
synthetic preparation delay returned after **3.007507 seconds** despite the frozen
two-second fixture limit. It read no real credential and released no worker.

Final verification ran against stable source hashes; **all seven commands exited
0**, with no canonical suite or provider calls:

| Final case | Observed seconds | Result |
|---|---:|---|
| Preparation stalled beyond limit | 1.529146 | External cutoff; no release; confirmed cleanup. |
| Preparation returns after work cutoff | 1.287798 | Stops before command/worker creation; confirmed cleanup. |
| Actual cleanup worker plus setsid descendant | 1.083386 | Inner worker exit -9; namespace and cleanup confirmed. |
| Evidence-copy delay | 1.982851 | Bounded return; partial evidence preserved; cleanup/retention unconfirmed. |
| Removal delay | 1.981778 | Bounded return; cleanup/retention unconfirmed. |
| Final result-retention delay | 1.981775 | Bounded return; no false retained-result/cleanup claim. |
| Normal complete worker | 10.880876 | 23 synthetic requests, unsigned simulation success, exact evidence, all domains retired and cleanup confirmed. |

`D/final-focused-summary.json`, each `final-*-command.json`/`final-*-result.json`,
and `D/final-normal/complete/integration-result.json` contain the actual results.
The last two stalled-finalization cases conservatively returned
`supervisor_domains_retired:false`; their cleanup assertion remains false.
The deliberately delayed synthetic filesystem hooks are restored for separate
test teardown, explicitly labelled **not** successful within-deadline qualification
cleanup. Public partial fixture evidence was retained before that teardown.

The initial escaped-descendant test with full Node preparation failed to reach
the escape step inside the shortened two-second fixture. That failure remains in
`deadline-escaped-result.json`. The corrected fixture prepares only the minimal
Python cleanup-probe jail while retaining the real child, FD, privilege and
namespace path. The independent normal complete-worker case still uses full
preparation and the unchanged 60-second outer limit. No assertion was weakened.
Earlier successful intermediate runs were superseded only after concrete observer
retirement/release-accounting changes; no successful final rerun was performed.

`D/final-source/` preserves all 20 exact qualification source/run-plan files.
Final changed source hashes:

| File | SHA-256 |
|---|---|
| `launch-successor.py` | `e6735e81f1b87cff353d31da4c7969d7b3d932d00450f8c915e00da4b619aab4` |
| `deadline-supervisor.py` | `54af47d27f00a2dd4445e87e1d132d77f166bc14b057e71a1931b42dc12c9b7d` |
| `deadline-local.py` | `7258bd386ed50ebb58a5180922a20269cc98564a701cbc3015da43d8e6282993` |

### C. Recovered public records, without archive-identity substitution

The supplied public transfer at
`/home/ops/artifact-setup-public.YepQ0nCP/final-proof-wallet-public.tar.gz` matches
the supplied SHA-256
`eae219e7c7da6f9760145025f2a361c5565848d2d930c8a9619cee64c31d47fa`.
Its 668 safe regular-file/directory members were extracted into the new
`D/public-transfer/` directory; archive code was not executed. Supplied origin is
Wiggles, operator `ricemachine`, public root
`/home/ricemachine/.artifact-calibration-local/v1/public/final-proof-wallet/`.
This checksum establishes the received transfer's identity, not an independently
retained historical execution-archive identity.

`D/public-validation.json` records **1,304 passed internal consistency checks**,
including 293 manifest entries, directory memberships/checksums and 277 raw-response
retention records. It checks the five top-level record links, fixed wallet/ATAs,
opening amounts, original boundary, signed setup-wire hashes and signature bytes,
retained finalized slot/time/fee/success bodies, and classic-token layouts.
This is retained-evidence validation, not fresh chain verification or a claim of
provider completeness/cryptographic archive provenance. Historical failed or
incomplete attempt directories remain preserved, not promoted to finalized evidence.
Validator development failures are retained in `public-validation-attempt-1..4.json`;
the final attempt 5 exits 0. These corrected validator assumptions about membership,
canonical effects hashing, base64 transaction encoding and funding account scope;
no transferred evidence bytes were edited.

Recovered exact identities:

* Setup freeze: `c53deaebbc6f7db5463a273f6e316faa82288298e39e809fa6481df2e78f4ced`.
* Freeze evidence manifest: `6541008a222ed0aafaf6044aba557901094fdcce26b6b16f0d9e093d00dcc91a`.
* Preflight: `4073cffb0033b27890a8d1468c83cee699b673460e2815a350bf67fb50243ed1`.
* Original latest setup/funding block time: `1788611228`.
* ATA signature: `3fBHWizxAKzRdamG8QzFEvt9io6zvpUhVbC2Pq4dXuX5oqepWMHTcFMmkdkHVwtJNntv4sVzETmqDsPsaS9QsdqH`.
* Funding signature: `Y39KGmwuMGBexEZkAzcZHGGFpfZwxiVjSHRXyeWpHEdm2hiNDBwuhguuWfRFSmfPyp4c46boXuWfuM6wPMggnrR`.

The historical execution archive/checksum requested in §9.C item 7 remains
unestablished. `mandate-v1.mjs:54,99–103` requires the setup archive/freeze/evidence
digest fields; the rehearsal pack §custodian guidance (line 96) requires actual
recovered independently retained archive identities, not synthetic constants.
No new transfer hash was silently assigned to `setup_authority.setup_archive_sha256`,
and no complete live public input/READY file was fabricated. This is the exact
remaining provenance input, not a renewed request for the recovered five files.

### D. Preservation and stop

`D/final-applicability.json` SHA-256 is
`698007473a314f68ae66a5d86964c10cd23bd8ccd0058f06153cfd91cbbeb45e`.
All 253 accepted source members and 5,394 runtime members remain byte/mode/identity
matched to the original pinned source manifest and R. All 369 baseline historical
members and all 824 prior worker-evidence files remain unchanged, including the
previous 823-member SHA256SUMS inventory. The unrelated plans remain unchanged.
Additional final preservation checks and this continuation's exact inventory are
retained under `D`; no previous inventory was rewritten.

Individual repository files changed by this repair only:

1. `engine/qualification/helius-provider/launch-successor.py` — externally supervised
   management, fixed absolute phase/release checks and bounded recovery callbacks.
2. `engine/qualification/helius-provider/deadline-supervisor.py` — new external owner.
3. `engine/qualification/helius-provider/deadline-local.py` — new synthetic regressions.
4. `engine/docs/v1.3-private-helius-provider-qualification-closeout.md` — this append.

Both external ledgers remain **NOT_DISPATCHED**. Actual totals remain **zero real
provider requests, zero real simulations, zero real credential reads, zero signing
and zero broadcasts**. The single external allowance remains unused. No canonical
tests, production changes, activation, installation, Git mutation or further review
cycle occurred. The local deadline repair is complete; **STOP at this bounded
repair and the explicitly unresolved historical archive-provenance input**.

## 11. Resumed qualification — exact archive-identity resolution, zero dispatch

The user expressly resumed the original qualification after §10, with the same
unused allowance, no successful-check/canonical rerun and no repair/review cycle.
This continuation makes **no source changes**. New preparation evidence is under
`/root/artifact-private-helius-provider-qualification/archive-identity-continuation/`
(abbreviated `I` below). All prior closeout text and inventories remain unchanged.

### Exact remaining field and applicable contract

The required field is
**`public-input.json:descriptor.setup_authority.setup_archive_sha256`**.

The real qualification entry checks the descriptor at
`engine/qualification/helius-provider/worker-successor.mjs:13`.
`executor-mandate-profile-v1.mjs:37–39` routes this final-wallet profile to
`validateBoundedAgentMandateV1`. In
`engine/src/verification-scope-v1-3/final-proof-agent/mandate-v1.mjs`, line 54
includes the archive field in the exact setup shape; lines 76–77 and 99–103
require a 64-character lowercase hexadecimal digest. Lines 192–195 and 200–212
bind it into the descriptor identity. The schema alone checks shape/identity, not
historical authenticity. `RUN-PLAN-successor.md:7–9,21–25,68–76` requires authentic
setup provenance and a truthful descriptor, and prohibits using the procedure
archive as the executed-setup archive. The resumed user instruction independently
prohibits transfer-checksum substitution and invented provenance.

**Correction/precision to §10:** the qualification runtime does **not** open an
original archive, consume an archive pathname, or independently reconstruct an
archive from this scalar. An independently retained original digest with an
authenticated record/custodian reference binding it to this executed setup is
sufficient identity evidence for this field; another transfer of the original
archive bytes is **not separately required by that input contract**. Original
bytes are one way to check a claimed digest, not an additional runtime input.
The rehearsal-pack production preparation guidance at line 96 is not converted
into an extra qualification-byte requirement.

### Bounded recovered-record lookup

Only the already recovered public subtree was inspected: **652 regular files** at
`D/public-transfer/final-proof-wallet/`, where `D` is §10's evidence root. Structured
JSON keys/string values and filenames were checked for archive fields or explicit
`.tar`/`.tgz` references; the targeted content lookup also covered those terms.
**No original archive digest field or explicit archive reference was found.**
Unlabelled hashes of transactions, effects, response bodies and manifest members
were not relabelled as archive identities. The recovered freeze and evidence-manifest
digests remain usable for their own fields, not for the missing archive field.

No original execution-archive filename or storage location is established.
The known public-record origin is
`/home/ricemachine/.artifact-calibration-local/v1/public/final-proof-wallet/` on
Wiggles; that is **not evidence of an archive pathname**. The new transfer and old
procedure archive are not substituted. No broader filesystem/history search,
provider lookup or new assessment was performed.

### Other authorized preparation completed

* `I/prepared-input-parts.json` retains the recovered setup signatures, freeze and
  manifest hashes, original boundary and opening constraints, and all unchanged
  fixed-profile constructor arguments. The single missing archive field is
  **omitted**, not filled with a placeholder. This is explicitly incomplete
  preparation, not a schema-valid descriptor/public input. No mandate ID/digest,
  live authority, READY or runnable public-input file was manufactured.
* `I/recovered-public-inventory.json` binds all 652 recovered files and the exact
  archive-reference lookup scope/results.
* `I/preparation-pins.json` rebinds the unchanged 20 qualified source/run-plan files,
  accepted source manifest/R, retained 7/7/public-validation results, public files,
  successor allocation and both original NOT_DISPATCHED ledgers. This was identity
  verification, not reexecution of successful local checks.
* `I/launch-dependencies.json` binds 4,550 previously exercised copied-source
  dependency records plus the Python/unshare launch executables. All 253 accepted
  source members and 5,394 accepted runtime members were rehashed with required
  identity/mode/length checks. Exact credential-source parent/file metadata remains
  suitable; its contents and named assignment were **not read** before the gate.
* `I/archive-identity-resolution.json` records the source-contract chain, required
  evidence type, unknown original filename/location and precise blocker. A final
  inventory preserves these new preparation artifacts without changing an old one.

The stricter allocation remains **29 requests, one unsigned simulation, zero
retries**, with **60 seconds total** and §10's absolute +50/+55/+60 cutoffs.
Signer inaccessibility and the existing credential-to-FD-5 path are unchanged.
The eventual final construction/release must still admit the authentic missing
identity; a matching hexadecimal shape by itself is not that admission.

**Actual result: NOT_DISPATCHED.** Both original ledgers are unchanged and unused;
READY, live public-input and single-session reservation remain absent. Actual
provider requests, simulations, credential reads, signatures and broadcasts are
all **zero**. No allowance was renewed, consumed or replenished. No canonical or
successful local check was rerun; no production edit, activation, signing,
broadcasting, funding, Git mutation or subsequent repair/review cycle occurred.

**Sole remaining input:** an authenticated independently recorded SHA-256 identity
of the original executed-setup archive, explicitly linked to this setup, for
`descriptor.setup_authority.setup_archive_sha256`. Original bytes are not demanded
in addition if that recorded identity/provenance is supplied. **STOP at this exact
blocker; do not ask again for the already recovered five public setup records.**

## 12. Recovered provenance V1 — retained record, blocked builder mapping

The user authorized qualification-only recovered provenance and confirmed the
acquisition location: Wiggles, account `ricemachine`, directory
`/home/ricemachine/.artifact-calibration-local/v1/public/final-proof-wallet/`.
This is **location provenance only**, not attestation that bytes are unchanged
since September 5 or authentication of an original execution archive.
The user reports **scoped non-recovery** from the bounded Wiggles search: only
September 5 constituent manifests and the new September 11 transfer were found.
No global absence is claimed and no historical search was repeated.

### Versioned retained record

`/root/artifact-private-helius-provider-qualification/recovered-provenance-v1/provenance.json`
uses the explicit schema `ARTIFACT_QUALIFICATION_RECOVERED_PUBLIC_PROVENANCE_V1`.
Its SHA-256 is
`ddf6f419d83227806f0b64e02cd334709ded5d56888a82437b01cf08cc9dc58d`.
It separates:

* Exact identities of all 652 recovered public members and the existing internal
  validation result, without converting internal consistency into authentication.
* The retained original setup transaction signatures, slots, times, signed-byte
  identities, evidence manifests, freeze identity and original freeze boundary.
  These are retained finalized-evidence observations, not fresh provider claims.
* The new transfer at
  `/home/ops/artifact-setup-public.YepQ0nCP/final-proof-wallet-public.tar.gz`, with
  SHA-256 `eae219e7c7da6f9760145025f2a361c5565848d2d930c8a9619cee64c31d47fa`.
* Original execution archive: **UNKNOWN**, with null digest/filename/location.
* Qualification-only purpose: **no trading authority or eligibility approval**;
  existing production setup-provenance admission is not satisfied by this exception.

Fresh verification of this record's transfer/member binding matched every one of
the 652 regular archive members byte-for-byte to the recovered subtree and prior
member inventory. That identity check enforced safe paths, file/directory-only
types and uniqueness; it was not an adversarial test suite. The original 1,304-check public validation and
7/7 deadline verification were retained, not rerun. This is a retained provenance
artifact, **not an implemented/admitted new worker-input API**.

### Builder trace and why a sidecar does not supply a truthful mapping

`construction.mjs:33` validates the descriptor, and `:65–68` passes it unchanged as
`mandate` to `buildOrcaMessageBoundaryV1`. The accepted builder at
`orca-message-boundary-v1.mjs:24` unconditionally invokes the executor mandate
validator. `executor-mandate-profile-v1.mjs:37–39` routes the final-proof wallet
profile to the fixed mandate schema. Its only alternate profile is a different
fixed disposable wallet; even that branch maps back through the same setup-field
validation at `:54–56`. It is not a recovered-evidence qualification profile.

`mandate-v1.mjs:9,14,54,88–103` fixes the version/profile and closed setup shape,
and requires the historical `setup_archive_sha256` digest. There is no tagged
UNKNOWN/recovered-provenance variant. Adding a provenance field, replacing the
required scalar with UNKNOWN/null, or supplying a new mandate version is outside
that accepted input contract. A transfer hash **or provenance-record hash** would
satisfy hexadecimal syntax but not the historical meaning of that field.

The archive field does not enter the Solana instruction/account/message assembly
at `orca-message-boundary-v1.mjs:25–67`. It does enter the full mandate identity
(`mandate-v1.mjs:192–195,200–212`), and the returned plan retains `mandate_digest`
and the full construction `input_digest` at builder `:68–74`. Thus a sidecar
disclaimer alone would leave a purported fixed-profile mandate with a nonhistorical
value in its historical archive field. No such descriptor was manufactured.

### Exact minimal contract change required — not performed

Provide a separately versioned **qualification-only unsigned-construction input
boundary** which binds a `qualification_provenance_digest` to recovered provenance
with original archive identity UNKNOWN, validates the required construction
constraints, and feeds the pure message compiler **without pretending to be the
existing final-proof mandate**. Its plan identity must also remain explicitly
qualification-only. Preserve the existing production mandate admission and builder
wrapper unchanged; do not make historical provenance optional in production.
The current accepted pure builder exposes no such boundary. Introducing one (or
introducing a separate qualification compiler rather than reusing this entrypoint)
is a construction-contract change beyond the authorized input/provenance-only
mapping. No accepted code or verified deadline code was changed to implement it.

Following the user's explicit **stop if truthful reuse is impossible** branch,
no new worker-input adapter was installed. The requested complete-worker new-input
tests (including mismatched members, contradictory setup and unsupported versions)
were **not run**: there is no admitted new-input implementation to test. No claim
of new worker GREEN or completed provider qualification is made. No unrelated
successful checks, canonical suite or subsequent repair/review cycle was run.

### Actual external outcome

**NOT_DISPATCHED.** The same single allowance remains unused. Provider requests,
unsigned provider simulations, real credential reads, retries, signing and
broadcasts are all zero. No READY/public-input/reservation was created. Both
historical ledgers, accepted source/runtime, prior evidence, and the deadline
mechanism are preserved. New retained artifacts are confined to
`recovered-provenance-v1/`; this append-only closeout is the sole repository edit.
**STOP at the minimal construction-input contract change above.** This is not a
request to search again for the historical archive or recreate setup.

## 13. Qualification-only construction implemented; single external attempt stopped

The user explicitly authorized §12's separately versioned construction boundary
and continuation of the same single session. This implementation preserves all
accepted production source/runtime and the verified launcher/deadline mechanism.
No shared-production extraction was necessary; **no canonical suite was run**.

Evidence root for this continuation:
`/root/artifact-private-helius-provider-qualification/qualification-construction-v1/`
(abbreviated `Q` in this section).

### Implemented contract and reuse

* `ARTIFACT_QUALIFICATION_INPUT_V1` is a closed envelope containing the explicit
  `qualification_provenance_sha256`, exact provenance bytes, retained validation
  bytes and the complete recovered-member byte set. Admission pins §12's exact
  provenance digest, checks its supported version, qualification-only purpose and
  UNKNOWN historical archive, and validates every member against its pinned
  identity. The immutable source record closes all its nested fields by exact
  byte identity; this is deliberately admission of this specific recovered record,
  not a generic trust-any-self-hashed-provenance API. The internal-validation record
  is also identity-bound. Setup records, signatures, freeze/opening boundaries and
  fixed wallet relationships are reconciled before session/exchange creation.
* Admission returns an immutable, in-memory registered
  `ARTIFACT_QUALIFICATION_CONTEXT_V1`. A reconstructed/lookalike context is rejected
  before any orchestration provider call. It contains no production mandate,
  human/agent authorization identity or historical-archive replacement field.
* `ARTIFACT_QUALIFICATION_CONSTRUCTION_INPUT_V1` closes every top-level field and
  requires exact fixed network, wallet, assets, route, economic/opening, age,
  transaction-shape and rebroadcast constraints. Construction is acquisition-only,
  ordinal 1, 5,000,000 raw USDC input and exactly 5,000 lamports fee. It checks u64
  quote/minimum quantities, the unchanged 50-bps slippage relationship, canonical
  blockhash and bounded integer pool/tick inputs. Current pool/tick decoding,
  coherence checks, quotation, context floors, exact-message fee corroboration and
  expiry check remain in the existing qualification orchestration.
* Reuse is through the **already installed and accepted-runtime-pinned** Orca
  `getSwapInstructionDataEncoder` and Web3 instruction/message serialization,
  alongside the existing `swapQuoteByInputToken` path. No swap algorithm or data
  encoder is duplicated. The adapter fixes the classic Whirlpool acquisition
  account topology; it is not a general transaction builder. No accepted source
  function is patched, dynamically rewritten, made optional or given a mode flag.
* `ARTIFACT_QUALIFICATION_UNSIGNED_PLAN_V1` has distinct
  `qualification_input_sha256`, `qualification_plan_sha256` and
  `qualification_provenance_sha256` identities. It has no `mandate_digest` and no
  purported production mandate. The resulting wire is checked for exactly one
  all-zero signature. Simulation retains finalized commitment, `sigVerify:false`,
  `replaceRecentBlockhash:false` and the exact fee-response context floor.

The recovered provenance's old blocked-admission observation remains preserved as
historical evidence about the previous builder. This new contract does not rewrite
that record, turn its location-only confirmation into September 5 byte authenticity,
or reinterpret a field in the production mandate. Original execution-archive
identity remains **UNKNOWN**. Scoped non-recovery remains scoped; no search repeated.

### Individual changed files

All paths below are under `engine/qualification/helius-provider/`:

| File | Change |
|---|---|
| `qualification-contract-v1.mjs` | New pure provenance admission, registered context and unsigned-message contract. |
| `qualification-scope-v1.mjs` | New immutable fixed qualification constraints, without mandate/authority identities. |
| `qualification-contract.test.mjs` | New focused contract, adversarial admission and byte-equivalence tests. |
| `qualification-worker-local.py` | New complete-worker fixture matrix for this input. |
| `construction.mjs` | Uses registered qualification context and new unsigned plan; preserves quote/fee/simulation path. |
| `probe-successor.mjs` | Reads the verified setup time directly from qualification context. |
| `worker-successor.mjs` | Admits the new envelope before exchange/session creation; retains qualification admission. |
| `worker-fixture-input.mjs` | Transfers explicit new fixture input; no synthetic historical-archive mandate construction. |
| `worker-local.py` | Adds the expected-timeout assertion branch needed for blocked-input coverage. |

This closeout is the additional repository file changed, by appending §13 only.
`Q/changed-files.json` records each qualification file's full SHA-256. Exact final
bytes of all 24 qualification files are retained in `Q/frozen-source/`.

### Focused RED → GREEN and final applicability

* `contract-red.json`: expected missing-boundary-module RED, exit 1.
* `contract-green-initial.json`: initial recovered-input/equivalence GREEN, exit 0.
* `worker-red-command.json` and `worker-red/`: old worker rejects the new input;
  worker exit 1, zero synthetic requests, cleanup confirmed. Preserved unchanged.
* `context-red.json`: a forged context reached one injected call before admission
  checking; expected zero-call assertion failed. The new registered-context guard
  repairs this path. No external provider was used in that reproduction.
* `contract-final-command.json`: **7 tests passed, 0 failed, 0 skipped**, exit 0.
  Covers changed/mismatched members and validation bytes, duplicate/unknown members,
  contradictory setup with stale and recomputed provenance hashes, unsupported
  input/provenance versions, unknown nested fields, accessor nonexecution, fixed
  scopes, economic/fee/tick/blockhash contradictions, forged contexts and rejection
  of qualification envelopes/contexts/inputs/plans by production mandate/builder
  entrypoints. Equivalent valid synthetic construction inputs produce identical
  message bytes, including negative/shifted/maximum-range tick cases, while their
  qualification and production identities remain distinct. Output is detached/frozen.
* `worker-final/`: complete flow passed with **23 synthetic requests**, unsigned
  simulation success and confirmed cleanup, returning in **9.316 seconds**.
  Member mismatch, contradictory retained setup, unsupported provenance version
  and malformed input each rejected with **zero requests**. Provider setup-body
  contradiction stopped after **16**; simulation program refusal stopped after
  **23** with refusal evidence. Exact request/response bytes and ledger identities
  were checked by the complete-entry fixture. All seven of these cases passed.
* The eighth case initially failed its intended-timeout assertion: the newly made
  FIFO was masked to owner-only permission by the test umask, so the unprivileged
  preparer refused it instead of blocking. That failed attempt and the exit-1
  aggregate command remain retained. The **only subsequent source change** was
  explicit chmod in the `blocked-input` fixture branch. No launcher, deadline,
  builder or worker code changed. No successful unaffected selection was rerun.
* `blocked-input-green/`: the targeted corrected case passed, exit 0, zero requests,
  timeout at **50.361 seconds**, confirmed cleanup/namespace retirement and removed
  temporary root. The original 60-second boundary and +50/+55 reservations remain.
  The aggregate final coverage is **8/8 scenarios across these retained runs**, not
  a claim that the earlier eight-case command exited 0.

`Q/final-applicability.json` proves unchanged final production/qualification code
across the successful selections and checks the exact sole test-fixture delta by
reconstructing its predecessor hash. `Q/qualification-source.sha256` binds all 24
final qualification source/test/plan files; inventory SHA-256:
`47fbd587bbd35e6a407119e1822dbdbd2d9467afea04e3053c155271cd792f56`.
Local copied dependencies were rehashed before release, and the actual external
jail's full source/dependency identities matched that qualified closure afterward.

The original 7/7 deadline suite, public-validation results, earlier failures and
prior closeout/evidence inventories were preserved, not rerun or replaced. The
existing Node experimental-loader warning in focused host tests is retained; no
package installation or production-loader change was made to suppress it.

### Frozen release and exactly one external launch

The original and successor NOT_DISPATCHED ledgers were verified unused immediately
before preparation of this release and retained unchanged as historical records.
`Q/session-spec.json` separately binds their identities, the new qualification
provenance contract and the **same single allowance**. It does not renew or expand
the session. Its SHA-256 is
`d7ae85aab63c695f01f4d15e7db59a00b06cd73673936140703d5781baa59508`.

Frozen public input SHA-256:
`e664e82aaa7ce338cc70ebccf9147e266679ee09c81ab5cdf9680138148c9806`.
Final applicability SHA-256:
`84baeafc08bfcbb7b6ad37658f3b373702b7c1bf7b8969a39867a6033636baec`.
READY SHA-256:
`dff309ca5109961b42795358000e019d1db9c6a29f3614dc07a6eba453a0b808`.
READY's provenance gate is explicitly scoped to **recovered qualification
provenance only**, with production setup-provenance admission false.

`Q/external-dispatch-intent.json` records a single no-restart launch before action.
The unchanged launcher then created its exclusive
`/root/artifact-private-helius-provider-qualification-continuation/single-session-reservation.json`
as **CONSUMED_POSSIBLY_DISPATCHED** before credential delivery. Limits remained
29 calls, one unsigned simulation, zero retries, 5,000 ms/request, 1 MiB individual
and 16 MiB aggregate response limits, 55-second inner maximum and 60-second outer
deadline with absolute +50/+55/+60 cutoffs. Endpoint, custody and signer confinement
were unchanged. No signing/submission/activation capability was added.

### Actual external observation and stop

The single real launch is retained at `Q/external-session/`:

* **One reserved/attempted `getGenesisHash` client exchange**, with exact 96-byte
  request retained and request SHA-256
  `5a350404d0c600e3837985d14c714109e25f137d0887a457ba47d3205a609d6b`.
* Disposition **TRANSPORT_SCREENING_OR_LOCAL_STOP**. **Zero admitted response
  bodies/bytes**, no passed provider checks and no simulation. Remote receipt,
  HTTP status and underlying cause are not established by retained evidence.
  This is not a proven provider-specific incompatibility or a successful genesis
  observation; it is the precise terminal transport/screening/local boundary.
* Worker exit **1**, no outer timeout. Launcher CLI exit **0** is supervisor return,
  not qualification success. Persisted pre-finalization outer elapsed time is
  **9.850612 seconds**; the terminal-return observation is **9.864801 seconds**.
  Cleanup, namespace retirement and temporary-root removal are confirmed; returned
  CLI evidence also confirms supervisor-domain retirement. No residual PIDs reported.
* **One authorized real credential-source read**, delivered through private FD 5;
  **zero retries, simulations, wallet/control signatures, broadcasts or follow-up
  diagnostic calls**. No request was repeated or session restarted. The allowance
  is **consumed**, notwithstanding the retained historical zero ledgers.

The exact closed external ledger SHA-256 is
`41af8e605864be1ab6f6806cd4b8e93de572ec507c23a1843d8d570697edac35`.
`Q/external-outcome.json` and `external-evidence-inventory.json` bind accounting
and clean retained artifacts. A report-assembly KeyError about a late CLI-only
supervisor field is retained separately; it caused no source change, provider
action or retest. The later field is recorded as an observed terminal result,
not fabricated into the earlier immutable outer-result file.

**Outcome: provider behavior qualification NOT ESTABLISHED; stopped with partial
evidence at the first exchange.** No subsequent repair/review cycle, provider
diagnosis, Git mutation, provisioning, installation, funding or activation follows.
**Production setup-provenance admission remains unresolved under its existing
contract. This qualification input is not production provenance admission,
authenticated approval, readiness evidence, trading authority or signing authority.**

## 14. First-exchange diagnostic continuation — runtime readiness blocked

Separate diagnostic evidence is retained under
`/root/artifact-private-helius-provider-qualification/first-exchange-diagnostic-v1/`
(`D` below). The original §13 qualification allowance remains consumed. This
continuation does not reopen it or authorize full provider qualification.

Recovery established that `D/local-initial-clean-command.json` completed with
exit 0: one synthetic local `getGenesisHash`, all nine checks passed, 101 admitted
bytes, zero real-provider requests/credential reads and confirmed cleanup. Its
command-record SHA-256 is
`8631f50e74ebd12afe5e1e93830153c2464c9b4240ce8ae8808ebb7f38dc2e33`.
The retained exact request/response identities match the closed local ledger.
This passing case was preserved, not rerun. It is not provider evidence.

The missing subsequent terminal result concerned a combined local Node TLS-root
inspection and `ss -lnu`, not an RPC invocation. Its original execution/result
remains unclassified. Under explicit renewed permission, the two inspections
were executed separately, each with a five-second subprocess timeout, with full
output retained in `D/tls-inspection.json` and `D/udp-inspection.json`. Both exited
0 without timeout. Node reported 145 bundled/default CA roots; the host's configured
127.0.0.53 DNS stub had a UDP listener. These observations do not establish public
TLS success or DNS resolution inside the final jail.

The first remaining fixture, `untrusted`, was attempted once and stopped before
release: launcher `QUALIFICATION_LAUNCH_STOP`, worker exit null, no observations,
zero fixture requests, zero real-provider requests and zero credential reads.
Cleanup and namespace retirement were confirmed. The fixture driver exited 1;
this is a preparation failure, not a demonstrated TLS rejection. Preserve
`D/local-untrusted-command.json` and `D/local-untrusted/` as failed readiness
evidence. The other five unexecuted classification cases remain pending.

A full baseline check after that fixture attempt found **57 runtime inventory
entries (56 distinct resolved files) no longer matching accepted bytes**, including
Python, its modules, glibc and the dynamic loader. The runtime inventory file itself
still has the accepted R digest; **all 253 accepted source members match**. The
first mismatching shared library in the inspected preparation order is
`runtime/shared/0000/ld-linux-x86-64.so.2`; `supervise.py` checks its digest before
copying it. This establishes a concrete preparation blocker, not the cause of
§13's historical external failure. No raw launcher exception was retained, so
the precise thrown assertion is a source-based deduction rather than a captured
stack trace. The runtime-change origin was not investigated or attributed.

`D/readiness-baseline-check.json` retains every expected/observed identity;
SHA-256 `315ad703e6b11046ee95fdafabf838d8a340a5b9e7a6ba0916d9f2d049a1f7d5`.
The complete runtime check should have preceded the new fixture: the earlier
preservation check covered retained files, not every installed runtime member.
Historical local GREEN remains valid for its recorded bytes, not the changed host.

**No external diagnostic launch occurred.** No diagnostic READY, reservation or
external-session directory was created. No further fixture was run after this
blocker. No runtime pin, launcher, accepted source or historical evidence was
changed to obtain GREEN. The original 2,960-file recovery preservation map matched
immediately before this append; no temporary diagnostic/jail roots remained.

Smallest next action: restore the exact accepted runtime through a separately
authorized environment-recovery procedure, or explicitly authorize a distinct
runtime requalification. Then resume only the remaining/failed diagnostic gates.
Do not spend the separate single external diagnostic allowance until those gates
pass. No canonical suite, signing, simulation, broadcast, activation or Git mutation
was performed. Stopped at runtime readiness; no provider diagnosis is claimed.

## 15. Qualification-runtime successor and single diagnostic — HTTP 401

Under separate bounded runtime-successor authorization, `D/runtime-successor-v1/`
(`S`) preserves a qualification-only runtime; no accepted production pin was
overwritten. The existing inventory mechanism produced 5,394 members with no
added/removed members and exactly 57 changed entries (56 resolved files).
Runtime identity: `a07f1bd10f71521b5ce95cd9273367ade083741d1bd96b3b3071d1a9a38c6ebd`.
The 52-member auxiliary launcher/source closure has identity
`67c9853a2a096f149790b6863b2bdfa7cc71657da68ba7bd121159b1f62368e1`.
`S/runtime-delta.json` retains complete before/after entries. The copied harness
diff changes only `supervise.py`'s HERE, RUNTIME and R; repository harness and
accepted source remain unchanged. This is **not renewed production acceptance**.

Package logs record unattended upgrades on 2026-09-11, with timestamps as logged:
Python 3.12.3-1ubuntu0.15 to 3.12.3-1ubuntu0.17, 06:02:45–06:03:00;
glibc 2.39-0ubuntu8.8 to 2.39-0ubuntu8.9, 06:03:13–06:03:31.
The changed files belong to libc6, python3.12-minimal, libpython3.12-minimal,
libpython3.12-stdlib and libpython3.12t64. All 56 match installed package
checksums; `dpkg --verify` for all five owning packages exited 0 without findings.
No unexplained difference remains in this delta under those local package
records. This is not independent package-signature revalidation and does not
attribute §13's historical failure to runtime drift. Package/update records are
retained individually in S; no packages or host update settings were changed.

Changed Python/loader/glibc dependencies required fresh affected-path coverage:
all seven diagnostic cases passed (clean, untrusted TLS, HTTP 401, credential
echo screening, stalled response, oversize response and disconnect); the current
complete qualification worker passed with 23 synthetic exchanges, exact retained
wire evidence and unsigned construction; its blocked-input deadline case passed.
All six deadline cases passed: delayed preparation, late preparation, escaped
descendant, stalled evidence copy, cleanup and retention. The last three correctly
report unconfirmed in-deadline cleanup; separately labeled synthetic teardown is
not counted as within-deadline cleanup. Every run has pre/post runtime and source
identity checks. Isolation evidence verifies UID/GID 65534, zero capabilities,
no_new_privs, full inherited high-FD closure and private worker/relay networking.
No canonical suite or unchanged historical success was rerun for a badge.

One failed aggregate driver is retained in `S/custody-complete-command.json`:
its nine named-data/capability custody checks passed, then the obsolete
`local-successor.mjs` construction fixture stopped before any synthetic request.
That fixture passes a production mandate where the current construction requires
a qualification context. It was not repaired or rerun; the current complete-worker
fixture supplies the applicable composition evidence. No production or diagnostic
implementation repair was needed. The independent exact-byte read-only review is
retained in `S/independent-review.txt`; its supplied-map completeness prerequisite
was checked against every runtime and auxiliary-closure member before READY.

The original diagnostic allowance was rechecked immediately before dispatch and
reserved durably, separately from the already consumed §13 qualification allowance.
`D/READY.json` binds 5,449 source/runtime/evidence entries; identity
`76790a46cc289c4378524258da00dfd26f88c62671679644476a9fd0e7472f63`.
The once-only wrapper retains intent and process start identity before waiting;
the launcher reserves before the unchanged named credential-to-FD5 custody path.

**Directly observed diagnostic result: HTTP 401 after successful verified TLS.**
The ordered records show worker release/capability load/transport invocation,
worker loopback connection, broker DNS completion, upstream TCP connection,
descriptor transfer, TLS_VERIFIED=1, HTTP_STATUS=401, HEADERS_REJECTED and
WORKER_STOP. The closed diagnostic ledger consumed exactly one `getGenesisHash`
with parameters `[]`, zero retries and zero admitted bytes. No response body,
headers, provider prose, credential or credential hash was retained. Worker exit
was 1; supervisor CLI exit was 0, which is not provider success. Whole-launch
elapsed time was 10.38 seconds within the unchanged 60-second bound; request
timeout remained 5 seconds. Cleanup, namespace retirement, temporary-root removal
and absence of owned residual processes were confirmed. Pre/post identities match.

`D/diagnostic-outcome.json` identity:
`3eed25b14bcc497819f83b298729ac6517ade4f2cb757c5d1ef0820b69a88b47`.
The 648-member final evidence map `D/diagnostic-final-evidence-sha256.json` has
identity `790ba26c4a59dc77fcb6d2871c9fdbd2635126ce9c35e00e93a42b9a3784fb32`.
All original retained records, accepted inventory/source manifest, repository
harness and unrelated plans were verified unchanged before this append.

**Both allowances remain consumed; no further external request is authorized.**
HTTP 401 locates this attempt's refusal at HTTP admission; it does not establish
whether the key is invalid, revoked, missing an entitlement or otherwise rejected,
nor prove the historical attempt had the same cause. The smallest next action is
separate operator-side credential/entitlement investigation and explicit new
authorization before any further external request. No real simulation, signing,
broadcast, full provider session, activation, installation or Git mutation followed.

## 16. Dedicated full qualification — consumed once, stopped at route construction

This appendix records the subsequently authorized full session. Earlier statements
of unused allowances, old credential sources and blocked construction are historical,
not current launch instructions. None of their reservations or evidence was replaced.

### Recovery and dedicated diagnostic checkpoint

HEAD remains `9c52f30b23e63bf46e32cb5a71611c476c94d279`, branch `main`.
No applicable ancestor/repository instruction files were found in the inspected
locations. No surviving qualification worker appeared in the recovered process
snapshot. Both unrelated September 2 plans remain unchanged. The 253 accepted
production members still match manifest identity
`094bedf06a1d8c41b207731afe268b80cc45e049496c9ee21c7b4b5d6a6b27ad`.

The retained operator statement in
`dedicated-credential-handoff-v2/diagnostic-session/operator-authorization.json`
explicitly confirms both success messages in the same entry invocation. It was
recovered without opening the credential or inferring entry from file existence.
The separate dedicated diagnostic admitted the expected mainnet genesis after
HTTP 200, one request, worker/launcher exit 0 and confirmed cleanup. That was
diagnostic success only. Its allowance, the earlier HTTP-401 diagnostic allowance,
and the original qualification allowance are all separately consumed and preserved.

### Narrow integration and evidence reuse

New evidence root (**F**):
`/root/artifact-private-helius-provider-qualification/full-dedicated-v1/`.

Only these repository files changed in this continuation:

| File | Change |
|---|---|
| `engine/qualification/helius-provider/full-launch.py` | Additive complete-worker launcher, fixed dedicated custodian and operator-entry gate, separate durable reservation, qualified runtime binding. Historical full and diagnostic launchers unchanged. |
| `engine/qualification/helius-provider/full-worker-local.py` | Actual complete-worker success/refusal coverage through synthetic dedicated-source traversal and private FD5; checks query-key delivery without retaining the target. |
| `engine/docs/v1.3-private-helius-provider-qualification-closeout.md` | Append this section only. |

The new launcher preserves the full worker, broker/relay caps, exact accepted
exchange, TLS verification, screening, confinement and deadline owner. Qualification
runtime R remains
`a07f1bd10f71521b5ce95cd9273367ade083741d1bd96b3b3071d1a9a38c6ebd`,
with all 5,394 members verified before/after. This is not renewed production
acceptance. The only real credential source is the dedicated fixed file through
`artifact-custody.py` and private FD5; no environment/old-source fallback exists.

Both changed-composition checks passed on the final frozen bytes:
`F/local-complete/` and `F/local-simulation-refusal/`. Each used 23 synthetic
requests through the actual complete worker, with exact request/response identity
checks, closed accounting, unsigned wire verification, dedicated-reader observation
and confirmed cleanup. Simulation refusal correctly produced worker exit 1 and a
passing refusal test. Filesystem-root redirection, synthetic public input and local
TLS are explicit test instrumentation; the reader/delivery/worker were not replaced.
Both test commands exited 0. There were zero real credential reads/external calls
during local verification. No canonical suite or general review cycle was run.

`F/readiness-coverage.json` binds the unchanged deadline, custody, contract and
transport evidence to final source/runtime, plus these two new composition checks.
AST comparisons verify the inherited supervision, preparation, child, bridge and
FD-closure functions and the dedicated gate/delivery. This is targeted applicability
verification, not a new independent-review claim. The obsolete failed aggregate
remains failed historical evidence; it was not rerun or relabeled GREEN.

### Frozen authorization and identities

The recovered method maxima and parameter derivations were frozen before dispatch
in `F/session-spec.json`, retaining their existing order and stop conditions. Its
explicit overrides identify the new single allowance, dedicated delivery and
qualification-only provenance; historical plan prose does not grant authority.
Original executed-setup archive identity remains **UNKNOWN**. Recovered public input
identity is `e664e82aaa7ce338cc70ebccf9147e266679ee09c81ab5cdf9680138148c9806`.
No production mandate or setup-archive identity was substituted.

* Final candidate source map: `F/candidate-source.json`, SHA-256
  `ed333b19b5ade7e7bdf023806df5c19d667fd0f8ceeb1e2bf4eae2d39d09b166`.
* Method/session specification SHA-256:
  `076d9b0e77b082d4b54c1b5935c954f45d0b85ef3095639b6c929186741f98c1`.
* READY, covering 5,549 files, SHA-256:
  `890debb87db8cc2d0f9026739f263743bc1821d13a27223c495e1ae4eae98bfb`.
* Verified terminal outcome `F/outcome.json`, SHA-256:
  `04907af67279f63c0f35e805ddd92143d36b667cde74447470e61bbf97124088`.

Exclusive dispatch intent and process identity are retained. The launcher durably
created `F/single-session-reservation.json` before real credential reading. Limits
remained 29 calls, at most one unsigned simulation, 5 seconds/request, 1 MiB each
request/response and 16 MiB aggregate admitted responses, 55-second inner sequence,
and 60-second outer launch with +50/+55/+60 work/management/outer cutoffs. No retries,
redirects, alternate provider, alternate credentials or replacement session occurred.

### Actual method accounting and terminal result

| Method | Frozen maximum | Actual |
|---|---:|---:|
| `getGenesisHash` | 1 | 1 |
| `getSlot` | 1 | 1 |
| `getBlock` | 1 | 1 |
| `getMultipleAccounts` | 2 | 2 |
| `getTokenAccountsByOwner` | 2 | 2 |
| `getSignaturesForAddress` | 15 | 9 |
| `getTransaction` | 2 | 2 |
| `getAccountInfo` | 1 | 1 |
| `getLatestBlockhash` | 1 | 0 |
| `getFeeForMessage` | 1 | 0 |
| `getBlockHeight` | 1 | 0 |
| `simulateTransaction` | 1 | 0 |
| **Total** | **29** | **19** |

All 19 requests have exact admissible response bytes, reservations and completions;
all response/request IDs, lengths and SHA-256 identities were independently
recomputed from retained files. **57,086 admitted bytes**, ledger closed. There was
no observed JSON-RPC refusal or unadmitted transport result in this session.

**Passed samples:** authenticated mainnet genesis; fresh finalized anchor; two
classic owner accounts and empty Token-2022 enumeration at equal opening watermarks;
all three histories explicitly exhausted with stable repeated heads; both recovered
setup transaction bodies. Equality of watermarks is not atomicity, and bounded
history sampling is not universal completeness.

**Failure boundary:** after route `getMultipleAccounts` response 19, before
`getLatestBlockhash`. At context slot **446109468**, the requested oracle
`CrkkeqLUo7n6gvzoYMPZ7CHjie1Zua2CHUPe2DFh8mmR` is explicitly **null**.
`construction.mjs:55` requires a nonnull account owned by the Whirlpool program.
This is an independently sufficient contradiction of the frozen construction
contract. The worker retains a generic stop, not a thrown exception location;
the null oracle is not claimed to be the exclusive or first thrown cause. No
repair, alternate account, no-op message or extra observation was substituted.

**Simulation: NOT DISPATCHED.** No quote, unsigned message, exact-message fee,
blockhash, expiry-height or simulation artifact was produced. Missing observations
remain unknown, not filled with historical or synthetic values. Route construction
and full provider qualification are **not established**.

Worker exit **1**, launcher CLI exit **0**; the latter is supervisor completion,
not qualification success. Observed whole-launch return: **15.204 seconds**, no
timeout. Cleanup, private namespace retirement, supervisor-domain retirement and
temporary-root removal were confirmed. A post-session namespace/owner-starttime
check found no owned residual process. Exact source/runtime identities remained
stable. All 1,405 selected historical/plan preservation entries matched afterward.

### Remaining gates before any supervised agent episode

1. Resolve the route/oracle contract incompatibility under separately authorized
   bounded work; this session does not authorize repair or another provider call.
   Any future qualification needs new explicit external authorization and frozen
   plan/readiness. No unused portion of this consumed allowance is reusable.
2. Establish current route/quote, exact supported message/fee/expiry and unsigned
   simulation under that contract. Simulation parameter support and execution
   success remain unqualified; this diagnostic/read evidence cannot substitute.
3. Resolve production setup-provenance admission under its actual contract. The
   recovered qualification provenance and UNKNOWN original archive do not admit
   a production mandate, eligibility, seven-day age decision or trading authority.
4. Establish the accepted production runtime/release and production host/custodian,
   isolation, control/revocation and evidence/replay gates separately. This runtime
   successor and temporary qualification jail do not renew production acceptance.
5. Obtain fresh supervised-episode authorization and satisfy its existing readiness,
   exact-message and execution limits. Wallet/control signing, broadcasting and
   activation were not exercised or authorized here. Provider history/omission
   guarantees, secret-retention policy and broadcast-retry behavior remain outside
   these samples.

### Exact proposed Git file list for the accumulated checkpoint

The following 41 repository files only; the list is also retained exactly in
`F/proposed-git-files.json`. This includes preserved uncommitted qualification work,
not just the three files changed in this continuation. Credential **source code**
below is not credential data. No private operational evidence or unrelated plan is
included. No staging, commit, reset, clean, checkout, push or other Git mutation ran.

```text
engine/docs/v1.3-private-helius-provider-qualification-closeout.md
engine/qualification/helius-provider/ARTIFACT-CREDENTIAL.md
engine/qualification/helius-provider/RUN-PLAN-successor.md
engine/qualification/helius-provider/RUN-PLAN.md
engine/qualification/helius-provider/artifact-custody-local.py
engine/qualification/helius-provider/artifact-custody.py
engine/qualification/helius-provider/artifact-delivery-local.py
engine/qualification/helius-provider/bounded-successor.mjs
engine/qualification/helius-provider/bounded.mjs
engine/qualification/helius-provider/construction.mjs
engine/qualification/helius-provider/credential-entry-local.py
engine/qualification/helius-provider/credential-entry.py
engine/qualification/helius-provider/credential-publication-local.py
engine/qualification/helius-provider/custody.py
engine/qualification/helius-provider/deadline-local.py
engine/qualification/helius-provider/deadline-supervisor.py
engine/qualification/helius-provider/diagnostic-bounded.mjs
engine/qualification/helius-provider/diagnostic-custody-local.py
engine/qualification/helius-provider/diagnostic-exchange.mjs
engine/qualification/helius-provider/diagnostic-launch.py
engine/qualification/helius-provider/diagnostic-local.py
engine/qualification/helius-provider/diagnostic-stages.mjs
engine/qualification/helius-provider/diagnostic-worker.mjs
engine/qualification/helius-provider/full-launch.py
engine/qualification/helius-provider/full-worker-local.py
engine/qualification/helius-provider/isolation-publication-local.py
engine/qualification/helius-provider/launch-successor.py
engine/qualification/helius-provider/local-successor.mjs
engine/qualification/helius-provider/local-successor.py
engine/qualification/helius-provider/local.mjs
engine/qualification/helius-provider/probe-successor.mjs
engine/qualification/helius-provider/probe.mjs
engine/qualification/helius-provider/qualification-contract-v1.mjs
engine/qualification/helius-provider/qualification-contract.test.mjs
engine/qualification/helius-provider/qualification-scope-v1.mjs
engine/qualification/helius-provider/qualification-worker-local.py
engine/qualification/helius-provider/supervise.py
engine/qualification/helius-provider/synthetic-public-input.mjs
engine/qualification/helius-provider/worker-fixture-input.mjs
engine/qualification/helius-provider/worker-local.py
engine/qualification/helius-provider/worker-successor.mjs
```

Final source hygiene found one nonfunctional warning in the new frozen
`full-launch.py:382`: an extra blank line at EOF. It is retained rather than changing
the exercised bytes after dispatch. The initial private report recorder rejected
Git's combined diff/check exit 3; that recorder-only failure is retained in
`F/finalization-initial-failure.json`. Its status handling was corrected without
changing qualification source or repeating tests/provider execution. Tracked/index
diffs remain empty; the exact proposed list and final evidence hashes are verified
by `F/final-checks.json` and `F/final-evidence-sha256.json`.

**STOP. The fourth external allowance is consumed. No further provider action,
automatic repair, simulation, signing, broadcast or activation follows this closeout.**
