# Construct supervised Orca adapter slice — closeout

## Result

Approved option 1 is recorded transparently. The external-head design is preserved as deferred. Added an offline-testable exact-message and signed-wire adapter composed behind authenticated Wiggles ingress. The actual live factory remains mechanically disabled.

Final focused verification: 7 passed, 0 failed, 0 skipped. One canonical offline regression: 298 tests, 295 passed, 0 failed, 3 intentional skips, exit 0. No delegated review or replacement review was performed.

## Exact files changed in this pass

Modified existing files, individually:
- `engine/docs/v1.3-bounded-agent-final-proof-offline-layer.md`: approved supervised/trusted-administration scope; accurate adapter and remaining-gate description.
- `engine/docs/v1.3-bounded-agent-rollback-deployment-decision.md`: explicitly superseding scope annotation; historical external-authority proposal retained as deferred.
- `engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`: preserved prior coverage and added this slice's source/docs.

Added:
- `engine/docs/v1.3-construct-supervised-release-checklist.md`
- `engine/src/verification-scope-v1-3/final-proof-agent/orca-message-boundary-v1.mjs`
- `engine/src/verification-scope-v1-3/final-proof-agent/orca-message-boundary-v1.test.mjs`
- `engine/src/verification-scope-v1-3/final-proof-agent/orca-signed-wire-boundary-v1.mjs`
- `engine/src/verification-scope-v1-3/final-proof-agent/orca-signing-composition-v1.mjs`
- `engine/src/verification-scope-v1-3/final-proof-agent/orca-signing-composition-v1.test.mjs`
- This report: `engine/docs/v1.3-construct-orca-adapter-closeout.md`

No pre-existing implementation or test file was edited. The two pre-existing untracked `.hermes/plans/` files were left untouched. Branch remained `main`; Git was only inspected, never mutated. No package/dependency changes.

## Implemented guarantees and boundaries

- Frozen mandate validation fixes wallet, route/program/pool, vaults, token accounts/assets, transaction profile, and ordinal. Construction follows the inspected acquisition/disposal calibration executors' one-instruction legacy layout, tick-array PDA rules, swap discriminator, exact-input flag, direction, and price limits. Live calibration runners were neither imported nor executed.
- Explicit raw quantities and quote/minimum values are bounded; minimum output obeys the mandate's slippage constraint. Exact-message fee must match the mandate and the challenge-bound message hash. No missing production values are substituted.
- The semantic prepared commitment includes the complete immutable plan and serialized message. Signed output must equal that exact message and have a valid signature from its required signer. Every serialized byte is covered by alteration tests; a copied/self-authored plan is not a branded plan.
- The composed path takes disposal quantity and acquisition evidence from executor-owned durable state and reconciles the authenticated admission/challenge. The pure construction utility alone does not confer quantity authority.
- Authenticated decisions, durable reservation, preparation, and `KEY_LOAD_STARTED_AMBIGUOUS` precede the injected signer. The private plan is consumed even if later work fails. Signing uncertainty and altered-wire failure cannot generate replacement signing after normal authority reopen.
- Wire verification precedes exclusive ordinal-specific retention; file and directory are fsynced, then verified through a no-follow metadata/readback path. Existing authenticated control-plane logic independently rereads/verifies the wire and commits `SIGNED_INTENT_DURABLE` before returning an intent. The new API does not expose wire or add a submission transport.
- Source capture and signer injection are trusted executor wiring, not Hermes channel parameters. Exports and injection are not OS isolation.

Positive crypto/retention tests use the existing published fixed test-key seed and a test message, not the final-proof wallet. Final-proof-shaped composition tests use inert callbacks that expose checkpoint order and fail on uncertainty/altered bytes. They do not prove a successful operational wallet-signing round trip. Existing control-plane tests separately exercise durable signed-intent completion and revocation races. No frozen wallet identity was relaxed to make a test sign successfully.

## Commands and captured results

Working directory: `/root/artifact/trade-artifact`.

Inspection included `pwd`, `git status --short`, `git branch --show-current`, targeted instruction/source reads, package-script inspection, dependency resolution, and manifest hashing. Initial manifest matched the supplied `ad5272772634ab49511429de887f379f8510c39d1ba0e204c54ba3a465a08ea2`. No applicable instructions were found in checked ancestors or relevant repository directories.

Development commands, in order (test paths share `engine/src/verification-scope-v1-3/final-proof-agent/`):
- `node --test .../orca-message-boundary-v1.test.mjs`: expected RED, exit 1, 1 failed because the new mapping module was absent.
- Same command after implementation: exit 0, 1 passed.
- Same command after retention test addition: expected RED, exit 1, 2 passed/1 failed because the new retention module was absent.
- Same command after retention implementation: exit 0, 3 passed.
- `node --test .../orca-signing-composition-v1.test.mjs`: expected RED, exit 1, 2 failed because the new composition module was absent.
- Same command after implementation: exit 0, 2 passed.
- `node --test engine/src/verification-scope-v1-3/final-proof-agent/orca-message-boundary-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/orca-signing-composition-v1.test.mjs`: final focused run, exit 0, 7 passed/0 failed/0 skipped.
- `npm run test`: one final canonical run inside a checksum-bracketing Python subprocess, npm exit 0; 298 tests, 295 passed, 0 failed, 3 skips. Actual script: `node --test engine/src/verification-scope-v1-3/*.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/*.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/reused/*.test.mjs`.
- `git diff --check`: exit 0, no diagnostics. New-source/docs trailing-whitespace scan also found none.

The actual root script and invoked paths were inspected before execution. They route to maintained offline tests; transport tests inject inert implementations. No live runner, key loader, minting script, wallet connection, or broadcast was invoked. The engine package's placeholder test command was not run or changed. Node emitted the existing experimental SQLite warning.

Canonical full output is retained locally at `/root/.hermes/cache/terminal-output/out-1788798983-1504523-f8d0.log`. Truncated display output was inspected from that retained log; the suite was not repeated.

Intentional skips remain:
- matrix 18: import/live seam preserves route and excludes old one-send transport authority
- RED live-source operator contract consistently describes two rebroadcasts and three sends
- live integration preserves definitive terminal failure and expiry dispositions

Included acceptance checks passed without separate duplicate runs: reviewed scheduler/error-validator hashes; exactly three identical-byte sends in fixed windows; historical JUP/RAY receipt/package/member identities; frozen controlled mainnet Orca reconstruction; production economic bridge; four normal/revoked acquisition/disposal FINALIZED → SIGKILL → reopen cases. Protected economics, candidate selection, immutable claims, and scheduler implementations were unchanged.

## Frozen acceptance bytes

Manifest: `engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`

SHA-256: `387bfdd343e17080197481d1a2b1f53cc10bfcb8fdd5d1f7465712a0aa830d42`

- Preserved all 148 original member paths; added 7 source/documentation members, for 155 total.
- Regenerated sorted repository-relative `sha256  path` rows. Among original manifested members, only the authorized offline-layer documentation changed.
- Local static/dynamic literal import closure check: no missing local edges. New modules introduce no runtime-read authority fixture assets. Prior authority assets and package/dependency manifests remain covered.
- All 31 manifested test files exactly match the expanded canonical test set.
- Pre/post canonical member mismatches: 0. Manifest bytes unchanged across acceptance and subsequent read-only verification.
- This after-the-fact report is excluded from the source manifest to avoid self-reference. It does not alter tested source bytes or expand their claims.

## Remaining blockers and approved limitation

There is no remaining test failure. This is not live-release approval:

- Operational executor-owned readiness/build capture remains unavailable. The new code binds supplied quote, pool tick, blockhash, and exact-message fee inputs; it does not independently attest their truth/freshness. A real capture port must validate raw finalized pool/tick/vault/token ownership, construct the quote, establish opening balances/strict age, and retain fee/context evidence under frozen budgets. A challenge digest is not an authority substitute.
- Real signer composition, submission handoff integration, deployment UID/account/permission confinement, identities/human authorization, execution window/RPC budgets, and source-authenticated transmission/reconstruction/demo evidence remain gates in the release checklist. No OS confinement or complete live signer acceptance is claimed.
- Option 1 explicitly excludes automatic detection of an administrator restoring an older internally valid snapshot. External-head infrastructure is deferred, not implemented. Trusted administration and supervision must stop on a known restore, reprovisioning, state loss, or ambiguous recovery. No automatic reenrollment/reset; the system cannot detect every restore.
- Earliest proof-opening candidate `1789216029` remains only a candidate. Later runtime must independently prove the strict seven-day predicate from finalized chain evidence.
- Synthetic fixtures do not establish live onchain occurrence. The canonical run reconstructed retained calibration evidence offline, not a new live transaction.

No real credentials, enrollment, final-proof wallet/ATA activity, key loading, production signing, RPC calls, broadcasts, funding, scheduler activation, infrastructure changes, delegation, or Git mutations occurred. Stop after this slice; no next readiness task has begun.
