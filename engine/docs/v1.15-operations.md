# Artifact v1.15 Operations

Status: **RELEASE_READY_WITH_NONBLOCKING_LIMITATIONS** under the amended Slice 12 provider-modernization gate. The live authorization is consumed; no further provider run is required or authorized for closeout.

## Production controlled-live flow

The v1.15 operator accepts the closed CLI flag set, including explicit `--max-exact-fallback-transactions 0..8`, and constructs `wallet_wide_acquisition_request_v2`. It creates the fixed-origin Helius full-transaction port v2, binds one detached budget set and one acquisition-wide monotonic deadline, and runs `acquireWalletHistoryV2`.

The acquisition flow is:

1. validate Solana mainnet-beta identity;
2. derive a finalized anchor;
3. page finalized `getSignaturesForAddress` as the sole completeness authority and prove the lower bound;
4. perform the latest-state recheck;
5. page fixed-origin `getTransactionsForAddress` with `transactionDetails: "full"`, `commitment: "finalized"`, `encoding: "json"`, `maxSupportedTransactionVersion: 0`, `tokenAccounts: "none"`, `status: "any"`, `sortOrder: "desc"`, and page size: 100;
6. validate/detach/project full transactions and reconcile them to canonical signatures;
7. use finalized `getTransaction` only for an explicitly budgeted missing-signature-only fallback;
8. reuse unchanged v1 acquisition-result, evidence-bundle, candidate-set, digest-only selection, Slice 7 dry-run, receipt, and package boundaries.

Production has no dependency on the legacy Enhanced adapter/projector. Those modules are frozen offline oracle infrastructure only.

## Controlled-live report

Reports use `artifact_v1.15_controlled_live_validation_v1`. PASS reports include bounded canonical-source counts, all five disposition counts, canonical object digests, candidate counts, retry/timeout counts, and v2 telemetry:

- `full_transaction_pages_examined`;
- `full_transaction_entries_examined`;
- `full_transactions_reconciled`;
- `exact_fallback_transactions_requested`;
- `exact_fallback_transactions_reconciled`.

SAFE_FAILURE reports retain the closed base report and fixed `error_code`. When `failure_diagnostic` is present, it is restricted to `controlled_live_failure_diagnostic_v1` with fixed `stage`, `operation`, primary `reason`, and, when more than one fixed wallet-wide class applies, sorted unique `underlying_reasons`. v1.15 stages add `full_transaction_history`, `full_transaction_projection`, and `exact_transaction_fallback`; operations add `full_transaction_address_history`, `full_transaction_validation`, `full_transaction_projection`, and `exact_transaction_fallback`; reasons are the fixed full-page/token/duplicate/order/shape/signature/projector/result classes plus `provider_value_unsafe`, `multiple_unresolved_classes`, and the defensive fallback tuple.

Raw bodies, URLs, headers, API keys, pagination tokens, provider prose, stacks, local paths, and retry internals are excluded. Reports must use a unique nonexistent path under `/tmp`, outside the repository, exclusive `wx` creation, canonical JSON, fsync, and mode `0600`.

## Release-gate interpretation

The stable positive provider-modernization control is the reviewed Slice 7 cross-method capture plus deterministic v2 replay. All five retained transactions were acquired through both approved full-transaction methods and matched canonically. The retained JUP and RAY buy/sell pairs separately remain exact across the v2 acquisition, evidence, candidate, Slice 7, receipt, package, and member boundaries; the fifth close-account fixture remains an intentional fail-closed control. The retained Enhanced path is an offline identity oracle only and is absent from the production controlled-live dependency closure.

The final latest-state JUP whole-wallet run returned `wallet_wide_impact_unresolved` at `wallet_wide_classification` / `transaction_classification`, with primary diagnostic reason `multiple_unresolved_classes` and fixed underlying reasons `unknown_token_scope`, `unmatched_wallet_instruction`, and `unsupported_nested_instruction_shape`. This result is accepted only as a semantic fail-closed refusal after the provider-modernization stages completed. It is not a provider-modernization PASS and does not reconstruct or prove the absence of economic effect in the unsupported transactions.

No authoritative acquisition result, evidence bundle, or candidate set was issued. Slice 7 dry-run was not reached. Therefore no live JUP receipt/package comparison was available. Omitted PASS-only telemetry is unavailable, not zero.

This exception does not accept provider authentication, malformed/unsafe schema, canonical completeness, pagination, duplicate conflict, reconciliation, source mismatch, cap, deadline, timeout, fallback, capability, report-schema, or privacy failures. Any such result remains release-blocking.

## Offline verification and closeout

Use direct `node --test` commands and the maintained offline regression. Do not invoke the root npm test wrapper. No deterministic command authorizes a live/provider call.

The completed pre-live compatibility binding and consumed run are recorded in `engine/docs/v1.15-provider-compatibility.md`. Do not reinterpret the amended gate as permission for another live run.

Run the maintained direct-Node regression, exact retained JUP/RAY identity gates, provider/full-transaction fixture gates, privacy/capability/dependency checks, syntax checks, and `git diff --check` after final documentation edits. Do not invoke the root npm test wrapper. No package-store production write, publication, upload, signing, minting, deployment, commit, tag, or push is permitted without separate authorization.