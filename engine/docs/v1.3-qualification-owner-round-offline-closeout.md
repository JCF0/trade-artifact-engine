# Complete-round qualification owner resampling — offline closeout

## Disposition

Implemented and locally verified under the explicitly revised allocation. **STOP
before any provider session.** No real credential access, RPC, signing, activation,
canonical suite, general review cycle or Git mutation occurred. Six previous
external allowances remain consumed. The previous contract-decision document and
all prior source/evidence remain unchanged; its allocation blocker was resolved by
the user's subsequent explicit authorization, not by relaxing equality.

Private root (E):
`/root/artifact-private-helius-provider-qualification/owner-round-v1/`

## Correction

New qualification files under `engine/qualification/helius-provider/`:

- `bounded-owner-round-v1.mjs`: session version
  `ARTIFACT_QUALIFICATION_OWNER_ROUND_SESSION_V1`; logical direct/route maximum3,
  owner maximum4; other logical maxima unchanged, shared lag allowance2, global
  actual dispatch ceiling29. Adds pre-restart remaining-capacity check and lossless
  rentEpoch parsing using the unchanged accepted source-token reviver.
- `owner-round-v1.mjs`: at most two sequential complete rounds. Each reacquires
  direct wallet/ATAs and both owner populations. Shapes, floors, population,
  ownership and full parsed-account equality are checked before allowing a retry
  for valid watermark mismatch. Changed direct contents across rounds stop.
  Second-round floor is the greatest prior observed slot; all three admitted
  observations must equal within that round. Invalidity/conflicts cannot trigger
  resampling. Durable reservations, completion and hash-bound admission identify
  exact ordinal ranges; failed-round evidence is retained but excluded.
- `probe-owner-round-v1.mjs`: uses that admitted opening round for all downstream
  floors; original history/setup constraints and actual construction remain intact.
- `worker-owner-round-v1.mjs`: default complete-worker CLI, fixed input admission,
  original absolute deadline, and explicit session/round policy evidence.
- `owner-round-worker-local.py`: complete-composition synthetic TLS fixture.

No accepted production file or prior executable was changed. Equal watermarks are
necessary sampling checks, not atomicity or combined COMPLETE/EXACT authority.
Provider-attested completeness and controlled-capture requirements still apply in
production. Qualification provenance does not admit production setup provenance;
original execution archive identity remains UNKNOWN.

## Final composition and focused results

The actual versioned default CLI was exercised through the dedicated synthetic
custodian, FD5, confined broker/relay/worker, existing SDK construction and original
deadline supervisor. Fixtures have loopback-only namespaces and no external routes.
All17 targeted cases pass; expected negative worker exits are1, fixture exits0.

| Case | Actual requests | Shared additional attempts | Result |
|---|---:|---:|---|
| No mismatch | 23 | 0 | Unsigned simulation success |
| First mismatch, valid complete second round | 26 | 0 | Unsigned simulation success |
| Restart plus owner/fee context lag | 28 | 2 | Unsigned simulation success |
| Persistent mismatch | 9 | 0 | Stop, no admission |
| Cross-round mixing temptation | 9 | 0 | Stop, no admission |
| First-round token bytes conflict | 6 | 0 | Stop, no restart |
| First-round lamports conflict | 6 | 0 | Stop, no restart |
| u64 rentEpoch conflict | 6 | 0 | Losslessly distinguished; stop |
| Changed direct state in second round | 7 | 0 | Stop before owner reads |
| Second-round token bytes conflict | 9 | 0 | Stop, no admission |
| Below-floor result | 5 | 0 | Stop, no restart |
| Invalid owner response | 6 | 0 | Stop, no restart |
| Third lag refusal in second round | 10 | 2 | Shared allowance stop |
| Full history after restart | 29 | 0 | Budget stop before request30 |
| Restart cannot leave shortest downstream capacity | 6 | 0 | Pre-restart stop |
| Insufficient deadline for lag backoff | 1 | 0 | Deadline stop, no retry |
| Simulation -32016 after restart | 26 | 0 | No simulation retry |

The last two budget/deadline boundary fixtures use the actual worker's exported
`run()` with tighter limits (25 calls /1,000ms), not a substituted probe. All other
cases use the default CLI and normal limits. Every pass verifies exact server vs
retained request/response bytes, ordinal and identity accounting, retry templates/
IDs/backoffs, admitted bytes, no extra simulation, cleanup and namespace retirement.

Retained unsuccessful attempts: fixture Content-Type omission caused screening
at1; corrected predecessor RED failed the expected recovery at6; first candidate
missed the rentEpoch parser in its jail and exited before requests. That closure
was corrected without changing production code. Parallel-load deadline testing
first hit transport timeout at1; identical frozen bytes tested alone reached the
intended deadline refusal at1. None of these is a provider result. No successful
case was rerun. Both candidate revisions and all failure evidence remain retained.

Historical12 context-lag cases and27 oracle tests remain historical. Only unchanged
call-loop/custody/transport/supervisor/constructor mechanisms reuse their coverage;
prior owner-mismatch behavior is superseded. No broad rerun was performed.

## Allocation, freeze and dispatch gate

For successful history length H=9..15, restart R=0..1 and extra lag attempts L=0..2,
actual requests =14+H+3R+L. With a restart, success requires H+L<=12; with both lag
attempts H<=10. The observed28-call success uses shortest history. Maximum history
plus restart would require32 (34 with two lag attempts) and must refuse at29.
The pre-restart20-call capacity check tests feasibility, not guaranteed success.
Individual logical maxima cannot override the global physical cap. Shared -32016
retries can add physical attempts to a method but not new logical operations.

All prior time/byte limits remain: request5s, 1MiB request/response,16MiB aggregate,
session55s bounded by original work50s/management55s/outer60s. No clock/allowance
reset at a new round. No omitted history/setup/construction step to force green.

Actual non-fixture launch wiring is in `E/harness/full-launch.py` and
`E/dispatch-once.py`; it selects the new worker, an eight-module live qualification
closure and unchanged custody/deadline components. Six fixture override forms
were refused. Missing external authorization and16 malformed/mismatched grant
shapes were refused locally. The actual wrapper was exercised without a grant
and refused before any intent, reservation, credential read or external dispatch.

Final bindings: `E/session-spec.json`, `E/candidate-source-v2.json`,
`E/tested-candidate.tar.gz`, `E/READY.json`, `E/readiness-pin.json`,
`E/coverage.json`, and `E/final-verification.json`. The final verifier rechecks
frozen archive bytes against tested source maps, complete readiness bindings,
prior evidence and runtime identities, without rerunning successful tests.

**Remaining prerequisites:** a fresh explicit external-session and dedicated
credential-delivery grant bound to final READY/spec hashes; an applicable operator
credential-entry confirmation (no current credential availability was inspected);
unchanged readiness/runtime/source/evidence identities; unused new one-shot state.
Only after those gates may `E/dispatch-once.py` be invoked once. Full closed grant
schema, exact limits, sequencing and refusal behavior are in `E/dispatch-plan.md`.
Freeze/readiness is neither external authorization nor production readiness.
