# Artifact v1.15 — Full-Transaction Provider Modernization

Status: **RELEASE_READY_WITH_NONBLOCKING_LIMITATIONS** after Slice 12 documentation closeout and the amended provider-modernization release gate. The release remains uncommitted, untagged, and undistributed pending explicit Git/tag authorization.

## Changed

- The production controlled-live operator now constructs `wallet_wide_acquisition_request_v2`, creates provider port v2, and runs the full-transaction acquisition/projector path.
- Bulk enrichment is fixed to `https://mainnet.helius-rpc.com/` and `getTransactionsForAddress` with `transactionDetails: "full"`, `commitment: "finalized"`, `encoding: "json"`, `maxSupportedTransactionVersion: 0`, `tokenAccounts: "none"`, `status: "any"`, `sortOrder: "desc"`, and page size: 100 with opaque pagination.
- Finalized `getTransaction` is available only as an explicitly budgeted missing-signature-only fallback.
- Controlled-live reports use `artifact_v1.15_controlled_live_validation_v1` and expose full-transaction page/entry/reconciled counts plus exact-fallback requested/reconciled counts.
- The fixed diagnostic vocabulary covers full-transaction history, projection, and fallback failures while retaining `provider_value_unsafe` precedence.
- `engine/docs/v1.15-provider-compatibility.md` preserves the dated, commit-bound pre-live STOP checklist and records the completed, consumed Slice 11 evidence under the amended release gate.
- Historical chronology: the Slice 9 draft compatibility review was bound to `aee2e8250ae2ae5ab42a8a75038b9b3f321dc7e9`; the later pre-Slice-11 constrained rebind superseded it with reviewed code baseline `8f108c07a814223e5e8af66fcbe5949371cd335b` before the consumed live control.

## Preserved

- Finalized `getSignaturesForAddress` remains the sole completeness authority.
- All v1 authoritative acquisition, candidate, Slice 7, receipt, and package schemas remain unchanged.
- Exact JUP receipt/package identities remain `5fb5732d248af4e8f9214a3b074c3bf711a776e8445bf14eae735ddf02a0bbca` / `5b8d2241a70eb68b4bc1b43f3d471dbd677b6d89ba47dc0569f7af7d34e71278`.
- Exact RAY receipt/package identities remain `4d33969c45a041837070dbc83730862325ff989772712aae285384d4570e4341` / `25e6820d0ac45e8347375eadd824fde2c6ec528b56b637a0144c013da33d5fa2`.
- Report output remains outside the repository at a unique nonexistent temporary path, exclusive-create, canonical, fsynced, and mode `0600`.
- Raw bodies, URLs, headers, API keys, pagination tokens, provider prose, stacks, paths, and retry internals remain excluded.
- No package-store production write, publication, upload, signing, minting, or deployment capability was added.

## Compatibility and status

Enhanced Transactions is currently documented as a legacy product in maintenance mode. Production v1.15 no longer reaches its adapter or projector; those remain frozen offline oracle infrastructure only. Provider compatibility must be rechecked from official documentation before live operation, and any drift means STOP.

## Stable positive modernization control

The stable positive control is the reviewed Slice 7 cross-method capture plus deterministic v2 replay. All five retained finalized full transactions were acquired once through full-mode `getTransactionsForAddress` and once through finalized `getTransaction`, passed individual-versus-bulk canonical equality, and were admitted as non-authoritative provider-attested fixtures. Exact retained JUP and RAY transactions replay byte-identically through the v2 acquisition result, evidence bundle, candidate set, digest-only selection, Slice 7 request/result, receipt, package, and all five package members.

The production v1.15 controlled-live dependency closure contains no legacy Enhanced adapter or projector. Finalized `getSignaturesForAddress` remains the sole completeness authority; bulk full transactions are validated enrichment and exact fallback remains bounded, missing-signature-only recovery.

## Final controlled-live evidence and amended gate

The final separately authorized latest-state JUP whole-wallet control returned the semantic SAFE_FAILURE `wallet_wide_impact_unresolved` at fixed stage `wallet_wide_classification` and operation `transaction_classification`. Its primary diagnostic reason was `multiple_unresolved_classes`; its fixed underlying reasons were `unknown_token_scope`, `unmatched_wallet_instruction`, and `unsupported_nested_instruction_shape`.

This is an accepted conservative refusal under the amended v1.15 provider-modernization gate. It is **not** a provider-modernization PASS, not a reconstruction of the unsupported transactions, and not evidence that those transactions had no economic effect. The sequential production path reached wallet-wide classification only after finalized canonical pagination, latest-state proof, full-transaction retrieval and validation, and canonical provider reconciliation completed. The unresolved in-window wallet semantics then correctly prevented authoritative issuance.

No authoritative acquisition result was issued. No evidence bundle or candidate set was issued. Slice 7 dry-run was not reached, and no live whole-wallet comparison against the frozen JUP receipt or package identity was available. PASS-only page, transaction, retry, timeout, fallback, disposition, and candidate telemetry omitted by the safe-failure report is unavailable and is not represented as zero.

The exception is narrow. Provider authentication, malformed or unsafe schema, canonical completeness, pagination, duplicate conflict, reconciliation, source mismatch, cap, deadline, timeout, fallback, capability, report-schema, or privacy failures remain release-blocking. No further JUP/provider rerun is required or authorized for v1.15 closeout.

The final sanitized report remained outside Git, canonical and closed-schema, with mode `0600`, size 1,091 bytes, and SHA-256 `c540fb132acdc247d4779624dc8b73c298b8401dea853027fc526aecd703c75f`.

Artifact v1.15 replaces production legacy Enhanced-history enrichment with validated finalized full transactions while preserving finalized canonical signature history as the sole completeness authority and preserving exact JUP/RAY proof identities.

The system deterministically reconstructs supported results from provider-attested on-chain evidence and exposes the evidence boundaries and limitations required to reproduce its result.

This release classification does not authorize commit, tag, push, publication, upload, signing, minting, deployment, or distribution.