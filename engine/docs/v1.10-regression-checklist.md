# Artifact v1.10 Regression Checklist

Run before any manual Direct Upload review.

## Commands

```bash
node engine/src/public-demo/cli.mjs --dry-run
node engine/src/public-demo/cli.mjs --write --out engine/data/public-demo --visibility unlisted --wallet-display truncated --force
node engine/src/public-demo/predeploy-check.mjs --root engine/data/public-demo
node engine/src/public-demo/leak-check.test.mjs
node engine/src/public-demo/site-bundle.test.mjs
node engine/src/public-demo/predeploy-check.test.mjs
node engine/src/run-v110-regression.mjs
node engine/src/run-v19-regression.mjs
```

## Expected Results

- Public-demo dry run reports 14 files and `leak_check: pass`.
- Predeploy checker reports `ok: true`.
- v1.10 regression reports 4 suites run and 0 failed.
- v1.9 regression reports 34 suites run and 0 failed.
- Generated bundle remains byte-stable across regeneration.
- No Git, Cloudflare, browser automation, deployment, DNS, mainnet, upload, mint, signing, account, database, or live network action occurs.

## Manual Hosted Review

After manual Direct Upload, check the default `pages.dev` URL:

- `/index.html` loads.
- Board shows only selected JUP and RAY entries.
- Receipt links open both static proof pages.
- Both `proof.json` files load.
- Both verifier JSON files load.
- Invalid paths show `404.html`.
- `robots.txt` returns `User-agent: *` and `Disallow: /`.
- Response headers include the configured CSP and `X-Robots-Tag: noindex, nofollow`.
- Browser developer tools show no scripts and no network calls beyond static file loads.
- Wallets remain truncated.
- Coverage Statements and raw quote/no-USD disclosures remain visible.

## Rollback

Use the Cloudflare Pages deployment list to restore the prior Direct Upload deployment. Do not change DNS or add a custom domain during v1.10 review.