# Additive history admission and further terminal-source incompatibility

## Subsequent user-requested canonical verification

After the partial-result report below, the user explicitly requested `npm run test` immediately. That command ran **once**, with a 600-second timeout, and completed with **553 tests, 550 passed, zero failed, three skipped, exit 0**. Wrapper elapsed time was 577.5661250464618 seconds. No code repair or successful rerun was needed.

The canonical allowance is now **consumed**. Statements below that it was unused describe the earlier stop, not current status. Full TAP, command, exit and source snapshot are retained under `/root/artifact-supervised-integration.uzy_y343/canonical-requested.{tap,result.json,sources.json}`. TAP SHA-256: `8c0096e83317df70f4d95c4a44e69c498b543769f4ab8d772ca8d55085a18280`.

The 202-member candidate manifest SHA-256 was `0686676e2cbf4b299079624d0a09fe559da9e6eff3ddab1a30b1b7dbab5a767d` both before and after; all members verified and source bytes stayed unchanged during execution. `git diff --check` passed. The historical predecessor manifest remains unchanged. This is passing canonical verification of the current partial implementation, **not acceptance of the incomplete supervised integration**, and does not resolve the terminal-source incompatibility. Only this documentation addendum was added after the run; tested source was not edited.

## Disposition

The original retained-history counterexample now passes an additive replay path with its raw page SHA-256 unchanged. Focused history regressions and historical replay regressions pass. **The complete supervised production integration is not implemented or accepted.** Continuation exposed a further source/terminal-closure contract incompatibility described below. No source completeness or economic rule was weakened to bypass it.

Predecessor HEAD: `aa752943f17c24705a3763cbc1f3df90ef4256cd`. Predecessor manifest SHA-256: `da60b0bc18331cc6ae6d6cac4fc9877a103d55348e5d8373d654da4815f58d65`, 200 members. This remains the historical manifest, not a claim that the modified working tree matches it. The previous blocker document and preparation pack are preserved as historical work.

Evidence root for this continuation: `/root/artifact-supervised-integration.uzy_y343/`.

## Precise additive contract

`artifact_final_episode_replay_v3` adds one mandatory `history.admission` member reference. Historical v1/v2 descriptor fields, behavior and result shapes remain unchanged. Unknown versions still fail. No historical fixture was regenerated.

The new referenced object is closed to `{version, lanes}`, with version `artifact_supervised_history_v1`. Exactly three ordered lanes identify the mandate's wallet, JUP account and USDC account. Each lane is closed to `{address, pages, repeated_head}`; each page/head pair is closed to `{request, response}`, referring to original manifest-bound members.

Independent replay performs these checks in `supervised-history-v1.mjs`:

1. Validate the existing fixed-lookback acquisition request and signed human mandate authorization. Derive the cutoff using `deriveOldestAllowedTimestampV1` and the retained finalized anchor. Require strict separation from the frozen latest setup time. No caller-provided exclusion list, row classification, timestamp override or `until` selector is accepted.
2. Bind each raw signature-page request to the exact address, finalized commitment, limit 100, retained anchor context floor and expected `before` cursor. Check response/request IDs. Require a nonempty initial history, contiguous cursors, a terminal empty page, no intervening empty pages, and a matching repeated head for every address. The wallet lane must reference exactly the descriptor's original history page members.
3. Parse original page members without rewriting them. Reject duplicate signatures within an address, ascending slots/times, future/unknown times and contradictory cross-address overlaps. Legitimate overlaps are corroboration, not duplicate economic transactions. Setup histories may differ by address; latest setup corroboration is aggregate, as in readiness.
4. Account for each primary-page row by member and row index. Rows at or above the cutoff are `REQUIRED_EPISODE_TRANSACTION`. Older rows must also be no later than the immutable latest setup time, and are classified `OUTSIDE_WINDOW_SETUP_HISTORY`. An older post-setup transaction cannot be hidden by moving the economic window. The aggregate history must corroborate the frozen setup time.
5. Require exact signature/slot/time/execution-state correspondence between **each lane's in-window population** and the already independently normalized/reconciled episode bodies. Both fixed direct swaps reference all three monitored addresses. Unknown activity, missing bodies or contradictory bodies fail; the old overbroad whole-page equality is not simply removed.
6. Feed the original wallet pages through the existing wallet-history acquisition rules. Only the independently checked in-window population supplies the authoritative transaction transcript. The resulting admission view is included in the v3 release result and its digest.

Older rows are not asserted irrelevant to all proofs. Original authenticated readiness/setup/owner-enumeration checks in `retained-final-control-v1.mjs` still run. The admission result explicitly says `ORIGINAL_AUTHENTICATED_READINESS_STILL_REQUIRED`. Under the current readiness contract, setup corroboration uses the full finalized histories and frozen setup time; it does not authorize fabricating or omitting a body where another governing contract requires one. Existing economic evaluation, fee binding, control authentication, explicit selection and demonstration gating remain unchanged.

This is an administrator-qualified retained-evidence contract, not independent authentication of provider truth. The tests synthesize the **additional** request/pagination/head carriers explicitly; they do not claim that those missing carriers were present in the original compatibility probe. The original counterexample page itself is unchanged.

## Regression evidence

The prior compatibility probe asserted rejection and passed. It was not RED for supported admission.

This continuation's `history-red.tap` instead asserts successful v3 replay and fails before implementation with `REPLAY_SOURCE_ADMISSION_UNAVAILABLE`: **1 test, zero passed, 1 failed, exit 1**. `history-first-green-attempt.tap` records the repaired positive path: **1 passed, exit 0**.

The positive regression independently pins the raw three-row page to SHA-256 `7f079f0fd63d1c68b0b78aa5403201503ead07f13ba8ffd7d351ddece59dae13`, the actual retained counterexample at `/root/artifact-supervised-contract-probe.xea2yuko/original-history-package/history.json`. It requires verified replay, authenticated control, all nine primary row appearances accounted for across three address lanes, and three older-row classifications without changing the raw bytes.

`history-expanded-red.tap` caught an implementation overrestriction: requiring every address to contain the latest setup timestamp. **14 tests, 12 passed, 2 failed including the parent subtest, exit 1.** The fix uses aggregate latest-setup corroboration, matching the existing readiness rule.

`history-repair-green.tap`: **14 tests, 14 passed, zero failed/skipped, exit 0**. Covered cases: checksum-pinned counterexample, older-row accounting, missing required body, producer exclusions, cutoff equality, null/future time, missing exhaustion, wrong cursor, absent floor, changed repeated head, duplicate row, inconsistent finalized body and legitimate overlap with distinct older address histories. TAP SHA-256: `1e381913dd4f2091b6ac307a1e471a1f8b28ef0371e15dcd300213e2af4975ed`.

An intermediate six-file focused command timed out at **180 seconds**. It included the new history test plus the five historical files below. Its original wrapper buffered TAP and failed to persist the partial buffer on timeout. That output is unavailable; this is an **incomplete/unclassifiable attempt**, not GREEN. The wrapper traceback was returned in the session. A subsequent process check found no surviving Node test children. The retaining runner was corrected to stream TAP to an exclusive file and persist timeout status and source hashes.

A bounded replacement historical-only selection ran with `--test-concurrency=1` and a 570-second inner timeout / 600-second terminal timeout. `historical-replay-focused.tap`: **60 tests, 60 passed, zero failed/skipped, exit 0**, elapsed 217.62929631676525 seconds. Exact selection:

- `final-episode-release-v1.test.mjs`
- `final-episode-integration-v1.test.mjs`
- `final-episode-fee-binding-v1.test.mjs`
- `final-episode-outcomes-v1.test.mjs`
- `retained-episode-package-v1.test.mjs`

This includes existing positive replay/selection, historical calibration identity checks, exact-byte fresh-process replay, fee repair and incomplete/revoked/failed outcome tests. TAP SHA-256: `b76f9fad07580d8577b37c7087063256283b7bcab016fbc96bc5e3724cf107c0`. The actual command arrays and source snapshots are retained in the corresponding `.result.json` and `.sources.json` files. No successful selection was rerun without changed code or another concrete reason.

**Canonical `npm run test` invocation count remains zero.** These focused gates are not complete integration acceptance and do not consume the reserved canonical invocation.

## Further incompatibility encountered during integration tracing

The complete admitted wallet history after disposal includes the acquisition and disposal while both remain inside the existing fixed-lookback window. The trusted terminal economic-source callback, however, requires a **one-transaction source-authoritative context**:

- `wiggles-terminal-closure-v1.mjs:24–27`: independently validate the context, then require `rows.length === 1`.
- `:39`: disposal requires an opening-basis reference to the durable acquisition evidence.
- `:43–44`: disposal requires a context opening quantity equal to acquired JUP, and an ending quantity of zero.
- `wallet-acquisition/request-contract.mjs:85–88`: the existing request is fixed-lookback/latest-state with an initial cursor of null; it is not an ordinal-scoped acquisition-to-disposal interval.
- `fixtures/bounded-agent-offline-v1.mjs:84–102`: the accepted disposal-only synthetic source explicitly slices away acquisition before constructing both its history and authoritative transcript. It is synthetic scaffolding, not a complete-history production adapter.

The history repair must not exclude acquisition as an old setup row: it is a required in-window transaction. Merely selecting disposal in terminal closure is also insufficient because the whole-episode opening balance is zero, not the retained acquisition quantity. Mutating a source context, forging a different acquisition result or inventing a shorter window would bypass its independent authority.

### Executed counterexample

`terminal-source-contract.test.mjs` in the evidence root uses the **existing accepted whole-episode synthetic fixture**. It first independently validates its source-bound context, establishes two in-window transactions, then passes that context into the actual `closeTrustedTerminalSourceV1` function. It requires the exact rejection at line 26, before any retained evidence or authority mutation.

`terminal-source-contract.tap`: **1 passed, zero failed/skipped, exit 0**. TAP SHA-256: `4676aa0545cb8179cc9db9bee243a85ed7b8390d5ee23932a80e95261337263f`. The full context and observed stack are retained in `terminal-source-context.json` and `terminal-source-observation.json`.

This is a **successful incompatibility probe**, not a RED→GREEN repair or a durable-runtime exercise. The authority in the probe is a read-only fixture state provider; no production disposal was attempted. The fixture used here is distinct from the signed retained-page fixture used by the history regressions. The result demonstrates the actual consumer rejection of an independently valid complete source, not a completed composition/exporter path.

### Required next contract

A separately specified source-bound terminal leg projection must retain the complete admitted history and required bodies, select only the durable ordinal's exact signed intent for closure, and bind disposal opening inventory to the actual retained acquisition evidence and correctly timed owner enumeration. Whole-episode source identity, projection identity, per-leg boundary/basis and raw member correspondence must be independently checked. No caller-supplied quantity, invented narrower window or unverified population slice is acceptable.

That is an additional change to the terminal source/leg-boundary contract, not another relaxation of the history equality. This continuation stops at this concrete incompatibility rather than changing economic/boundary semantics implicitly.

## Changed-file inventory and remaining work

- Modified `engine/src/verification-scope-v1-3/final-proof-agent/final-episode-release-v1.mjs`: explicit v3 dispatch and checked history admission; old versions retain their previous branches and result shape.
- Added `engine/src/verification-scope-v1-3/final-proof-agent/supervised-history-v1.mjs`: closed source-derived admission.
- Added `engine/src/verification-scope-v1-3/final-proof-agent/supervised-history-v1.test.mjs`: focused repair and adversarial regressions.
- Added this document.

No fixture, package script, live entry point, signing/runtime/scheduler/terminal implementation or accepted manifest file is modified. The two plans, preparation pack and prior blocker are preserved. No commits or pushes were made.

Still **not implemented/accepted**: terminal leg projection; private supervised composition and closed extended configuration; bounded transport/finalized-source acquisition; unsigned exact-message simulation; additive production submission profile; actual evidence exporter; composition→export→fresh-process reconstruction; complete requested adversarial outcome coverage; final release manifest and canonical acceptance. Existing historical fresh-process tests are not a substitute for actual-exporter acceptance.

The predecessor manifest remains unchanged on disk. An external **candidate, non-accepted 202-member working-source manifest** and source archive are retained with final preservation evidence; they cover the predecessor source set with its one intended modification plus the two new source/test files. There is no final accepted release identity yet. Do not treat the old manifest as a checksum claim over this modified tree.

No real keys/credentials, external network/RPC, broadcasts, installation, deployment, delegation, services/account changes or Git mutations were used. Both live entry points remain mechanically disabled. Offline validation does not establish OS isolation, provider qualification or live readiness.
