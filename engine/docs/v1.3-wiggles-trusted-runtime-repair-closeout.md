# Wiggles trusted runtime — bounded two-finding repair closeout

## Disposition

**PASS_OFFLINE_REPAIRS / LIVE_MECHANICALLY_BLOCKED.** Repaired only B1 and B2 from `v1.3-wiggles-trusted-runtime-readonly-review.md`, with executed targeted RED→GREEN evidence for each before final acceptance. The historical review and original closeout are preserved unchanged; this is an implementation closeout, not a replacement independent review.

Final focused acceptance: **123 tests, 123 passed, zero failed/skipped; exit 0**.

Final canonical acceptance: **422 tests, 419 passed, zero failed, three unchanged intentional skips; exit 0**. Exactly one canonical invocation followed focused GREEN. No successful acceptance rerun was made to obtain an automated verification label.

Only disposable test keys and synthetic transport were exercised. No live RPC, real credentials/key access, production-wallet signing, submission, service launch, infrastructure change, installation, delegation, Git mutation, commit or push occurred. No additional review cycle, architecture expansion, automatic administrator-snapshot rollback detection or later live gate was initiated.

## Exact repository files changed in this repair pass

1. `engine/src/verification-scope-v1-3/final-proof-agent/wiggles-trusted-runtime-v1.mjs` — verified existing-evidence file fsync before acceptance; reader-owned allocation cleanup across exceptional exits; non-dispatching mutable-array erasure.
2. `engine/src/verification-scope-v1-3/final-proof-agent/wiggles-trusted-runtime-v1.test.mjs` — appended three evidence-recovery cases and nine key-buffer exception cases; preserved all pre-existing cases/assertions.
3. `engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256` — replaced only those two member digests; no membership, ordering, formatting or other digest changes.
4. `engine/docs/v1.3-wiggles-trusted-runtime-repair-closeout.md` — this new closeout, outside the source manifest to avoid self-reference.

All other manifested files, including capture/rentEpoch code, SQLite authority, authentication/admission/signing composition, production validation, deployment proposal, live entry, dependencies and package scripts, match the reviewed baseline. The prior dirty/untracked work was not staged or reverted. Evidence and the acceptance capture runner are retained outside the repository.

## B1 — Readiness-evidence durability after fsync failure / EEXIST

### Executed reproduction before repair

Added a real-runtime test matrix with the existing synthetic transport/clock and a disposable state root. Each case writes the complete first raw evidence record, injects its file fsync failure, verifies that bytes remain, closes/reopens the runtime and repeats capture of the identical record.

The initial implementation produced:

- `recover`: **FAIL**, `existing evidence must be file-fsynced before acceptance`, actual fsync count **1**, expected **2**.
- `repeat-fsync-failure`: **FAIL**, `Missing expected rejection.` The old EEXIST branch never attempted the second file fsync, so the injected repeat failure could not block readiness.
- `mismatch`: **PASS**, preserving the existing exact-byte rejection.

Targeted RED: **3 tests, 1 passed, 2 failed; exit 1**.

### Minimal repair and GREEN

`wiggles-trusted-runtime-v1.mjs:28-38,92-120` now lets the private descriptor read establish durability when needed. On EEXIST, it checks descriptor ownership/type/mode/link/size, fsyncs that existing file, performs bounded exact readback and only then fsyncs the containing directory. Newly created files retain their existing write/file-fsync path. Any file fsync or readback failure still aborts capture; no challenge/decision permission is reconstructed.

Targeted GREEN: **3 tests, 3 passed; exit 0**. The tests prove the existing file is actually fsynced, repeated fsync failure refuses readiness, recovery remains exact-byte-bound across ordinary reopen, file durability precedes directory durability, and no key load or ordinal consumption occurs in these capture-only cases.

Regression locations: `wiggles-trusted-runtime-v1.test.mjs:353-415`.

## B2 — Secret-bearing allocation cleanup across exceptions

### Executed reproduction before repair

Added tests that retain the actual Buffer passed to the key-file read, not a copied buffer or merely the array reaching the signer. The nine cases cover:

1. Partial read followed by read exception.
2. Post-read fstat exception.
3. Post-read lstat exception.
4. Close exception cancelling a completed read's return.
5. Read exception followed by close exception.
6. Short read with a throwing replacement cleanup method.
7. Post-read metadata mismatch with a throwing replacement cleanup method.
8. Signer exception with throwing cleanup methods on owned allocations.
9. Successful actual disposable-key signing with throwing cleanup methods on owned allocations.

Targeted RED: **9 tests, 0 passed, 9 failed; exit 1**. Eight cases failed `key-read allocation must be zeroized`; the otherwise successful signing case failed with `injected cleanup method failure`.

### Minimal repair and GREEN

`wiggles-trusted-runtime-v1.mjs:23-58` keeps the reader responsible for its Buffer until descriptor close succeeds. An outer catch covers read/metadata/close failures, including a close exception cancelling a pending return, and clears the exact allocation before propagating failure.

`clearMutable` uses indexed writes to owned Buffers/typed arrays/plain JSON arrays rather than dispatching replaceable `.fill` methods. The same helper clears the reader Buffer, parsed number array, signing secret and derived secret in the signer's existing `finally` (`wiggles-trusted-runtime-v1.mjs:86-89`). Throwing replacement cleanup methods cannot interrupt erasure of the current or subsequent allocation.

Targeted GREEN: **9 tests, 9 passed; exit 0**. Tests require non-vacuous observation of the reader allocation; for cases reaching signing they additionally require observation and zeroization of the parsed array, signer secret and derived secret. Pre-sign IO failures perform zero signing. Failed paths preserve `KEY_LOAD_STARTED_AMBIGUOUS`, expose no durable wire, and cannot load/sign again on ordinary reopen. The successful disposable case completes SIGNED_INTENT_DURABLE and cannot obtain replacement signing after reopen.

Regression locations: `wiggles-trusted-runtime-v1.test.mjs:248-351`.

The guarantee is for these application-owned mutable allocations under the existing trusted-runtime scope. It does not claim erasure of immutable JSON strings, VM/crypto-internal copies, resistance to malicious same-UID code, or completion of JavaScript cleanup after process termination. The close-failure probes close the disposable descriptor before throwing, avoiding intentional descriptor leakage while reproducing cancelled-return/cleanup ordering.

## Exact executed gates and process results

All commands ran from `/root/artifact/trade-artifact` through the retained capture wrapper:

`python3 /root/artifact-wiggles-runtime-repair-evidence.xazg9db8/run-gate.py <label> <command>`

The wrapper runs the indicated command once, records its actual return code and complete combined TAP, and compares pre/post source snapshots. No application test command was replaced with fabricated output.

### Targeted RED→GREEN commands

B1, labels `b1-red` then `b1-green`:

`node --test --test-name-pattern='^readiness evidence EEXIST recovery ' engine/src/verification-scope-v1-3/final-proof-agent/wiggles-trusted-runtime-v1.test.mjs`

B2, labels `b2-red` then `b2-green`:

`node --test --test-name-pattern='^key buffer exception ' engine/src/verification-scope-v1-3/final-proof-agent/wiggles-trusted-runtime-v1.test.mjs`

### Agreed focused command, label `focused`

`node --test engine/src/verification-scope-v1-3/final-proof-agent/wiggles-trusted-runtime-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/wiggles-production-configuration-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/wiggles-decision-channel-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/orca-signing-composition-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/orca-message-boundary-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/orca-readiness-capture-v1.test.mjs`

### Canonical command, label `canonical`

`npm run test`

The unchanged root script invokes the maintained verification-scope, final-proof-agent and reused Node test globs. The engine subpackage's placeholder test script was not used. The canonical tool allowance was 600 seconds, with a 580-second inner process guard; execution completed normally without timeout.

| Gate | Tests | Passed | Failed | Skipped | Exit | Recorded wall seconds |
|---|---:|---:|---:|---:|---:|---:|
| B1 RED | 3 | 1 | 2 | 0 | 1 | 5.969739798922092 |
| B1 GREEN | 3 | 3 | 0 | 0 | 0 | 3.1399346948601305 |
| B2 RED | 9 | 0 | 9 | 0 | 1 | 6.825310110114515 |
| B2 GREEN | 9 | 9 | 0 | 0 | 0 | 6.267337072174996 |
| Focused | 123 | 123 | 0 | 0 | 0 | 36.09936516918242 |
| Canonical | 422 | 419 | 0 | 3 | 0 | 215.7201806907542 |

Focused TAP duration: **35945.970718 ms**. Canonical TAP duration: **215219.990915 ms**. Both report zero cancelled/todo tests. Node's experimental SQLite warning remains informational and was retained in the logs.

Unchanged intentional canonical skips:

- `matrix 18: import/live seam preserves route and excludes old one-send transport authority`
- `RED live-source operator contract consistently describes two rebroadcasts and three sends`
- `live integration preserves definitive terminal failure and expiry dispositions`

No test was weakened or skipped to obtain GREEN. Automated verification labels are distinct from these complete TAP summaries and process exits; no label-driven rerun occurred.

## Manifest and preservation proof

The starting identity was recovered from the retained earlier `canonical-result.json` and verified against the current baseline before edits:

**`a455d6480c7c85896ab85817dc4d64a225171cb8ee9017fe9394a8158c6fef33`**, 182 members, 23674 bytes, all members matching.

Final manifest:

**`afdeb67f2990179e0258f7c85ea3ea3e18bb87a55f64449c66d5553974384edc`**

- **182 members; 23674 bytes.**
- Exactly two existing member digests replaced, with all other **180** digests unchanged.
- No member addition/removal/reordering or incidental manifest-byte change. The final manifest was byte-compared to the baseline with exactly the two intended digest substitutions.
- Both targeted gates captured stable source snapshots for their respective RED/GREEN bytes. During development, the old manifest intentionally still named baseline digests; the wrapper allowed actual-byte differences only for the runtime and test file.
- Before final focused/canonical execution the manifest was updated and fully verified. Each acceptance command verified all 182 entries against current bytes before and after execution; the manifest byte hash also remained stable.
- Focused post-snapshot equals canonical pre-snapshot. Final post-acceptance readback again verified every member and the protected review/original closeout.
- No source/test/manifest member changed after focused acceptance.

Exact repaired member identities:

| Member | Before SHA-256 | After SHA-256 |
|---|---|---|
| `wiggles-trusted-runtime-v1.mjs` | `f9b2c55b2a896a2f145007a66a351898c457e2f076d6a7b9096aa0f460a67fdb` | `6d4c4a510a0eecc7631ae69949e8119ebe1d77014d6a4622ae2e8e2ca1ef821b` |
| `wiggles-trusted-runtime-v1.test.mjs` | `8541b1baf3bf2cbf42ec01f1b26fe5011242d91019f27cc094b875994a487c2b` | `0a1350f985a6c28f73095d1d1ca3f24305647ea297d88083eea931d26f5cc2ba` |

Protected unchanged identities include:

- Strict final-proof `mandate-v1.mjs`: `87eae4eb748be1f6c279621db92667c5aea6224a913ed8f8ebc3149d698b344c`.
- Blocked `live-executor-composition-v1.mjs`: `b1610a1e1a66a1e514dede31c5151ab25a0022ae36d77b377b3746fe27cb1263`.
- Blocked `run-wiggles-supervised-v1.mjs`: `d01087c9a30ce81305d079f4903442023d61a1373eb68368270630a158a3db4c`.

`git diff --check`, explicit untracked-source `git diff --no-index --check /dev/null <path>` checks, and `node --check` for both edited modules passed. A patch-tool coordination warning was followed by a complete manifest comparison; no unexpected byte drift was found. A read-only Git metadata command initially used the external evidence directory and reported “not a git repository”; it was corrected to target the repository explicitly. Neither diagnostic triggered an acceptance rerun or Git mutation.

## Retained evidence

Directory: **`/root/artifact-wiggles-runtime-repair-evidence.xazg9db8/`**.

It contains baseline/accepted manifest copies, six full TAP files, six process-result JSON files, and the external capture runner. Each process-result JSON retains command, actual exit code, duration, TAP hash and full pre/post member/protected-document hashes.

The **15-member** `SHA256SUMS` inventory verified completely. Its SHA-256 is:

**`bb93f6c911c1842d29a1e051022375265c3a853414daa34fb84a1344b905e2ab`**.

Acceptance evidence identities:

- `focused.tap`: `962bc5fc488ba9ec7020c44725106843dabc596459c81885eb826f735f1ad1ee`.
- `focused-result.json`: `c897392a3940e89a2f675458939370061b48d0c11c32790b5fe4e06166768efa`.
- `canonical.tap`: `f3c4175eea5d315acfa0077dbbbd9f69e48dd0b05b866e47d8cc96b2183846ef`.
- `canonical-result.json`: `5b86a7b7df6a86a004f99de5f6af0bdcd843b99469113b445a166dcc9f7850a1`.

These are local executed offline evidence, not host-deployment certification, actual principal ownership, release approval or a fresh independent review. The additional later-regression suggestions in the historical review were not silently expanded into this two-finding repair.

## Preserved authority and remaining gates

Authenticated ingress, human/agent identity binding, mandate economics, readiness freshness/revocation, one-time consumption, exact-message verification, signed-wire verification/retention/registration and no-replacement restart semantics are unchanged. The intended live entry remains mechanically disabled with no enable flag or test-mode production escape.

Actual deployment confinement, real authorization/configuration and release approval remain separate gates. Submission still requires separately reviewed exact retained-byte SUBMISSION_POSSIBLE integration, maximum three total identical-byte sends, at most two rebroadcasts, zero provider retries and no replacement construction. Ambiguous submission requires retained finalized status/expiry/absence evidence before any fresh execution authorization. Source-authoritative reconstruction, child-artifact validation and economic evidence promotion remain separate later gates.

Known authoritative-state loss/restoration requires stopping and preserving evidence, not resetting signing permission. Automatic administrator-snapshot rollback detection remains outside the approved supervised/trusted-administrator scope.

**Stop condition reached. Changes remain uncommitted and unpushed; no repairs beyond B1/B2 or next slice follows this closeout.**
