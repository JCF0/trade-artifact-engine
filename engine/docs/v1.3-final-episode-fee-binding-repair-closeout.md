# Final-episode fee-binding repair closeout

## Result: PASS for the bounded offline repair

The fee-binding finding B1 and documentation discriminator typo N1 from `v1.3-final-episode-reconstruction-release-readonly-review.md` are addressed. This is a repair acceptance record, not a replacement for that historical independent review and not a new broad review or deployment approval.

**Final source manifest SHA-256:**

`da60b0bc18331cc6ae6d6cac4fc9877a103d55348e5d8373d654da4815f58d65`

**Members: 200.** All members were verified before and after focused acceptance and before and after the single canonical acceptance. Source snapshots were identical throughout both acceptance runs. No source changed afterward.

Retained evidence: `/root/artifact-final-fee-repair.nuueiz14/` (abbreviated E below).

## Scope and baseline

The supplied accepted baseline manifest `4166e626b5e97dbd96d48b0db8f3b618a160998462f005102573574d70a72362` and all 199 unique members were verified before editing. `E/baseline.json` retains their identities plus the manifest, `.gitignore`, completion document, original closeout and independent review.

Relative to that baseline, 198 original source-manifest members are unchanged, one implementation member is changed, and one new test member is added. There are no removals. Existing modified and untracked work was preserved. No Git staging, commits, branch changes, pushes or other Git mutations occurred.

Only disposable synthetic identities and existing strictly offline fixture signing were used. No real keys or credentials were read, no live authorization was granted, and no RPC, network submission, broadcast, deployment, installation or delegation occurred. Live entry-point source remains byte-identical to baseline and disabled.

## Changed-file inventory

Paths are relative to the repository root.

1. **`engine/src/verification-scope-v1-3/final-proof-agent/retained-final-control-v1.mjs`** — one comparison and its explanatory comment at lines 200–203. SHA-256: `75b870d9424799868b94f707ef62c0f7e85fd08c6bbd3df7eb85235d00931420`.
2. **`engine/src/verification-scope-v1-3/final-proof-agent/final-episode-fee-binding-v1.test.mjs`** — new test file, 13 tests through release reconstruction, including a closed-submission hash propagation helper. SHA-256: `5b72828dff953e96348e7c4da45783f5d2d9f8c8f57955f43acda416c592b355`.
3. **`engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`** — refreshed only the changed control member and appended the new test member; final identity above.
4. **`engine/docs/v1.3-final-episode-reconstruction-release-completion.md`** — corrected `SUPERVISED_FINAL_EPISODE` to the implemented `SUPERVISED_RETAINED_FINAL_EPISODE` and added an explicit correction note. SHA-256: `3852906c3bf1bafa6dbb15d14659b3b4718e89c8d3fbcbdbdad8f3152244fe64`. This document remains outside the source manifest, as before.
5. **`engine/docs/v1.3-final-episode-fee-binding-repair-closeout.md`** — this new closeout, outside the source manifest to avoid self-referential acceptance hashes.

No existing fixture file was modified and no new fixture data file was introduced. The new test creates its synthetic evidence in temporary directories using the existing fixture builder. `git check-ignore` confirms the test is not ignored; Git status exposes it as untracked. `git diff --check` passed, and direct whitespace checks covered the new/untracked edited files as well.

## B1 repair and exact fee authority chain

The finalized transaction fee is compared using `BigInt` with the reconstructed exact-message plan fee, after terminal reconciliation has been recorded but before a finalized leg can close. A mismatch raises `FINALIZED_FEE_MISMATCH` through the existing control issue path, yielding `control.status = INVALID` and false complete-demonstration/synthetic/full-success flags. Failed execution is checked too; the comparison is not nested inside the success-only branch.

This reuses, rather than duplicates or relaxes, existing contracts:

- `orca-message-boundary-v1.mjs:25-36` validates phase/ordinal and requires the source fee to equal the applicable acquisition or disposal fee in the authenticated mandate.
- Its lines 68–74 retain that exact fee with the serialized-message digest.
- `orca-readiness-capture-v1.mjs:294-308` establishes the captured `getFeeForMessage` value and mandate agreement during retained readiness replay.
- `retained-final-control-v1.mjs:131-142` rebuilds the plan and matches captured fee-message digest, signed intent, serialized message and signed wire.
- The new comparison at `retained-final-control-v1.mjs:200-203` joins the independently reconstructed finalized fee to that chain.

Observed fees remain observed values. No economic reconstruction, quote/PnL, population, candidate selection, signing, transmission or terminal semantics were changed. Source validation already restricts raw numeric fees to the supported exact safe-integer domain; the new equality performs no floating-point rounding or decimal coercion. There is no new schema, authorized-value substitution, or source-summary trust.

## Genuine RED → GREEN

The first regression altered the disposal finalized fee from 5000 to 5001 and adjusted the wallet native debit coherently. It updated both finalized bodies and propagated unsigned retention, normalized record, manifest, checksum and completion hashes. It omitted the optional producer finalized carrier, then explicitly selected the reconstructed population member.

Before calling release reconstruction, the regression independently validates the complete rehashed submission snapshot and requires `FINALIZED_SUCCESS`. The source and terminal transaction results agree. Human authorization, both challenges/decisions/admissions and signed intents remain unchanged; binding and signed-intent submission bytes are checked unchanged. Neither transaction message nor wire is rebuilt or re-signed by the mutation helper.

The release-path assertions established verified integrity, a supported CLOSED/VERIFIED economic claim, eligible retained capture, reconciled transmission and the actual observed 5001-lamport disposal fee **before** asserting that complete-demonstration success must be false.

- `E/fee-red.tap`: the unmodified implementation returned true at that final assertion (`true !== false`, original test line 112), exit 1. This was the requested defect, not a malformed fixture, missing authentication or checksum failure.
- The minimum control change was then applied.
- `E/fee-green.tap`: the identical test passed, including explicit `FINALIZED_FEE_MISMATCH`, preserved supported economics, and both demonstration-success flags false, exit 0.

The original RED test bytes are retained as `E/red-test-source.mjs`; original implementation bytes as `E/baseline-control-source.mjs`. These copies were recovered after acceptance and verified byte-for-byte against the retained RED/baseline hashes before being saved. They are evidence copies, not executed alternative implementations.

## Finding-to-test and boundary mapping

All tests below are in the new `final-episode-fee-binding-v1.test.mjs` and ran successfully in both focused and canonical acceptance.

| Requirement | Regression coverage |
| --- | --- |
| Reviewer’s exact disposal counterexample | `fee binding rejects the fully rehashed authenticated disposal counterexample without changing economics` — lines 100–119. |
| Valid fee equality for both legs | `fee binding preserves matching authorized fees and complete synthetic success for both legs` — lines 141–153. Both observed fees equal their respective mandate fields; synthetic success true, real full success false. |
| Lower mismatch and largest admitted integer | Disposal fees 4999 and `Number.MAX_SAFE_INTEGER` — lines 158–166. Native debit remains coherent and the economic claim stays VERIFIED/CLOSED while authorization control is INVALID. |
| Preserve existing economics for zero fee | Zero-fee disposal — lines 154–157. Existing `position_economic_native_evidence_unresolved` refusal is retained rather than broadening economic semantics. |
| Acquisition mismatch, including zero and one-lamport boundaries | Acquisition-only fees 0, 4999 and 5001 — lines 168–178. Actual observed fees and supported remaining custody are retained; no claim or complete demonstration is invented; state remains resolving with the fee issue. |
| Valid acquisition-only outcome | Matching 5000-lamport acquisition — lines 179–188. Authenticated acquisition closure and actual fee survive; complete demonstration remains false. |
| Failed execution still fee-bound | Rehashed finalized failure at 5001 — lines 189–197. Finalized-failure classification and actual fee survive; control is INVALID and no economic claim is created. Existing matching-failure coverage also passed in the outcomes suite. |
| Reject unsupported numeric domains before authority | Negative, fractional and unsafe finalized fee inputs — lines 198–205. Existing source/parser refusal is required. |
| Other finalized/unresolved/revoked/result behavior remains intact | Existing integration, outcomes, release, retained package, submission/recovery/timing and legacy identity gates all passed. |

### Preserved intermediate boundary failure

`E/fee-boundaries.tap` records 12 passing and one failing test, exit 1. The new zero-fee **disposal** test initially expected an economic result, but the unchanged economic bridge rejects that input with `position_economic_native_evidence_unresolved`. The test expectation was corrected to require that existing refusal; no economic implementation was changed. The zero-fee acquisition-only test separately reaches the fee-control comparison and passes. The original boundary failure and its snapshots remain retained; it is not represented as RED evidence for a second production defect.

## Acceptance commands and results

`E/run.py` invokes commands with a 600-second subprocess timeout, retains full combined TAP, exit code, elapsed duration, exact command array and explicit selected test-file list in each `*-result.json`, and checks before/after source snapshots. Focused and canonical runs additionally verify every source-manifest member before and after execution. Evidence files use exclusive creation; successful runs were not overwritten or repeated.

Targeted invocations, each from the repository root:

- `python3 /root/artifact-final-fee-repair.nuueiz14/run.py fee-red node --test engine/src/verification-scope-v1-3/final-proof-agent/final-episode-fee-binding-v1.test.mjs`
- `python3 /root/artifact-final-fee-repair.nuueiz14/run.py fee-green node --test engine/src/verification-scope-v1-3/final-proof-agent/final-episode-fee-binding-v1.test.mjs`
- `python3 /root/artifact-final-fee-repair.nuueiz14/run.py fee-boundaries node --test engine/src/verification-scope-v1-3/final-proof-agent/final-episode-fee-binding-v1.test.mjs`

Focused acceptance invokes `node --test` with the following ordered selection (the complete exact command is retained in `E/focused-result.json`):

1. `engine/src/verification-scope-v1-3/final-proof-agent/retained-episode-package-v1.test.mjs`
2. `engine/src/verification-scope-v1-3/final-proof-agent/final-episode-release-v1.test.mjs`
3. `engine/src/verification-scope-v1-3/final-proof-agent/final-episode-integration-v1.test.mjs`
4. `engine/src/verification-scope-v1-3/final-proof-agent/final-episode-outcomes-v1.test.mjs`
5. `engine/src/verification-scope-v1-3/final-proof-agent/final-episode-fee-binding-v1.test.mjs`
6. `engine/src/verification-scope-v1-3/final-proof-agent/wiggles-submission-v1.test.mjs`
7. `engine/src/verification-scope-v1-3/final-proof-agent/wiggles-submission-recovery-v1.test.mjs`
8. `engine/src/verification-scope-v1-3/final-proof-agent/wiggles-submission-timing-v1.test.mjs`
9. `engine/src/verification-scope-v1-3/final-proof-agent/orca-readiness-capture-v1.test.mjs`
10. `engine/src/verification-scope-v1-3/final-proof-agent/orca-message-boundary-v1.test.mjs`
11. `engine/src/verification-scope-v1-3/legacy-identity-lock.test.mjs`

Exactly one new canonical invocation followed focused GREEN:

`python3 /root/artifact-final-fee-repair.nuueiz14/run.py canonical npm run test`

The maintained package test command and its expanded file selection are retained with the canonical evidence; no selection script was changed.

| Run | Tests | Pass | Fail | Skip | Exit |
| --- | ---: | ---: | ---: | ---: | ---: |
| fee-red | 1 | 0 | 1 | 0 | 1 |
| fee-green | 1 | 1 | 0 | 0 | 0 |
| fee-boundaries (intermediate expectation correction) | 13 | 12 | 1 | 0 | 1 |
| focused | 179 | 179 | 0 | 0 | 0 |
| canonical | 539 | 536 | 0 | 3 | 0 |

No cancellations. Focused elapsed 258.48 seconds; canonical elapsed 406.32 seconds, below the required 600-second limit. Existing Node experimental SQLite warnings were present; they were not failures. The three canonical skips remain the existing intentional live-seam tests. All 13 new test names appear successful canonically with no skip directives, and every focused successful test name appears successful canonically.

TAP SHA-256 values:

- fee-red: `837c008f87f56646661b54c40e4918e256bff288b7bb8d3c903d9c6bf51cc4fa`
- fee-green: `6cd068aaa16bed34ea76c1211371281dd6a5bbf20cffea607ddfa5511eb4e02b`
- fee-boundaries: `3978a797995d2799dd5c5ed082a857aabbbff25e25c62e44019ab3ee5580f169`
- focused: `68a2818aff6b23b03e205ebb27a2b250c5168f7537da069b917b18e4ca43a524`
- canonical: `ea21207972f40fce05ddbda2525a91ade3f83083cdca339c7586cc82a11ae669`

## Preservation and reproducibility

`E/acceptance-verification.json` records final hash/member checks, cross-run snapshot equality, baseline change scope, TAP digest checks, successful new-test names, focused-to-canonical inclusion, whitespace and visibility checks. `E/accepted-source.tar.gz` retains accepted source/document bytes from the acceptance snapshot; archive SHA-256: `66218e097e6d2ea162c2b2b982adc2ef3e784b17dd7ffdec52926638c507fac2`.

`E/completion-before.md` preserves the original completion document before its spelling correction. Its historical acceptance results were not rewritten. The original closeout, independent read-only review and original `/root/artifact-final-release-completion.44cthNCA/` evidence remain untouched. The failed-patch and external-checker dispositions in that review were not reopened.

## Remaining deployment gates and stop

This acceptance grants no live execution, real-wallet demonstration or deployment approval. Actual principal/authorization provenance, reviewed executor/configuration/dependency binding, supervised process/filesystem custody, durable state and revocation delivery, retained finalized source/capture completeness, execution-time original-boundary eligibility and separate deployment authorization remain operational gates.

Preserve the existing maximum three identical-byte client sends, provider retries zero, strict expiry/absence resolution and deadline rules, and prohibitions on rebuilding, requoting, refreshing blockhashes, re-signing, replacement/cleanup, unapproved Sender/tips/priority fees/Compute Budget. Trusted-administrator limitations and deferred CLI/API/deployment choices are unchanged.

No repairs beyond B1/N1, no broader review cycle, and no additional acceptance run were started. Changes are left uncommitted. Stop after this closeout and final read-only stability check.
