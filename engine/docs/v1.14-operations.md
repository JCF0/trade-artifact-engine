# Artifact v1.14 Operations

## Deterministic operation

From the repository root, run:

```text
node engine/src/run-v114-regression.mjs
```

This command is offline and direct-Node only. It requires no credentials, network, API key, live wallet, package store, or upload/sign/mint capability. It runs no npm wrapper and no controlled live validation. Its child processes receive a minimal deterministic environment with a nonexistent home location.

The runner performs four gates:

1. the safety-adapted v1.13 baseline via `node engine/src/run-v113-regression.mjs`;
2. every `engine/src/wallet-acquisition/*.test.mjs` file through Node's test runner, including retained-provider acceptance and acquisition-to-candidate integration;
3. `node --check` for every wallet-acquisition production module, test, and fixture helper; and
4. the v1.14 runner/documentation consistency self-check.

It fails on a child failure, nonzero exit, malformed or inconsistent TAP, a failed/cancelled/todo test, or any skipped wallet-acquisition/self-check test.

## Controlled live validation

The deterministic runner does not perform live validation. The first pre-hardening and distinct later post-hardening controlled live validations passed their then-current implementations, followed by the final post-remediation controlled live validation, which **PASS**ed at finalized anchor slot `437570354` and finalized anchor block time `1786013791`. That earlier result remains an accurate historical stage.

The final post-patch-3 controlled live validation **PASS**ed as the final authoritative release-gate run for one approved public Solana mainnet-beta wallet with `lookback_7d_v1`. It bound finalized anchor slot `437600788`, finalized anchor block time `1786026671`, and lower timestamp bound `1785421871`. It examined two pages, terminated with `historical_bound_reached`, observed 76 canonical signatures, excluded zero post-anchor signatures, and reconciled five in-window transactions with five Enhanced transactions.

The final partition was 1 supported, 0 unsupported, 1 ambiguous, 3 unrelated, and 0 failed. It produced one normalized event, one finding, one localized finding, one blocked summary, zero candidates and zero selectable candidates, with zero retries and zero timeouts. Acquisition, normalization, classification, pagination, historical-bound, and chain-boundary gates were proven. Capped, truncated, partial, and provider_uncertain were all false. Under patch-3 semantics, no retry attempt actually began and no HTTP attempt terminated because of its effective transport timeout. Patch 3 changed no aggregate classification relative to the prior run; the newer finalized anchor naturally changed the content-addressed digests.

Zero candidates was a valid result, not a validation failure. Live candidate resolution and Slice 7 were not exercised. Slice 7 was not invoked because there was no selectable candidate, and no receipt or package digest was issued. No package-store write, publication, upload, signing, minting, or deployment occurred. Controlled-live raw responses are not retained.

The final sanitized report remains private and outside Git. Its SHA-256 was `d2973f4f97745880d05dd75100071d22b339ac12366db798730f7b7eea8b1ef5`, its size was 1,951 bytes, and its permissions were `0600`. Credentials, URLs, headers, provider prose, response bodies, and raw provider bodies were absent. The canonical acquisition-result, evidence-bundle, and candidate-set digests were `d1ae883f9b41566fd547a79f2dfcb7894e8e7e827e7229abfba1a5c0606f93d3`, `f463e5cd140b97fba00ad25eb86244ab7bad03dd97412f70d2613aa4ca809dfd`, and `86400e78a0bc28b0367dcd4a8787abf49e58a68939590e8e0e5a449bf6084a61`, respectively.

This was the final post-patch-3 live validation. The live release gate is complete, no further live rerun is required before tagging v1.14.0, and v1.14.0 is not yet tagged. Wallet-history completeness remains provider-attested, not trustless.

### Sanitized report

A report may include only bounded derived fields such as:

- wallet;
- chain and network;
- lookback profile;
- anchor slot, time, and blockhash;
- pages and signatures examined;
- in-window source count;
- counts for all five dispositions;
- normalized event count;
- finding count;
- acquisition, evidence, and candidate digests;
- candidate and selectable counts;
- terminal reason;
- retry and timeout counts; and
- every completeness gate.

It must exclude raw bodies, URLs, headers, key origin, local paths, environment details, and provider prose.

### Repeated-run comparison

An exact offline replay must remain byte-identical. Two independent live runs may have different finalized anchors, so whole-result digests need not match. Compare their common frozen interval instead. If closed candidate-local evidence is unchanged, receipt-scoped evidence and candidate-local identity should remain stable, preserving the established receipt and package identity.

### Patch 3 operational controls

The authoritative orchestrator rejects a five-method provider object unless a registered hidden starter first binds detached immutable validated budgets. The adapter establishes exactly one acquisition-wide deadline from a monotonic `performance.now()` origin; wall-clock changes do not affect request timeout, remaining time, retry sleep, or exhaustion. Effective transport timeout remains `min(request_timeout_ms, remaining_overall_timeout_ms)`.

`retry_count` increments immediately before a retry HTTP attempt actually starts. `timeout_count` increments once only when an HTTP attempt terminates because of its effective transport timeout. A terminal retryable response, pre-attempt exhaustion, or cancelled retry sleep does not fabricate either count.

The deterministic runner invokes every discovered wallet-acquisition test file separately. It includes offline controlled-live operator tests with injected ports; those tests may create isolated temporary sanitized report fixtures. It invokes no live operator command and performs no production/package-store/publication write. The separately authorized final post-patch-3 live validation completed the live release gate; no further live rerun is required before tagging v1.14.0.
