# Supervised production integration — B1–B6 repair closeout

## Verdict: bounded offline acceptance PASS; no live release

The six review findings are repaired and covered by composed offline regressions. The complete focused selection is GREEN. Exactly one new canonical `npm run test` was invoked after focused GREEN and source freeze; it completed successfully within its authorized 1,800-second limit. No successful acceptance was repeated.

| Gate | Tests | Passed | Failed | Intentional skips | Process / runner exit | Elapsed seconds |
|---|---:|---:|---:|---:|---|---:|
| Complete focused, ownership-compatible isolation | 432 | 429 | 0 | 3 | 0 / 0 | 766.3020365387201 |
| Canonical, authorized once | 604 | 601 | 0 | 3 | 0 / 0 | 1035.5127911986783 |

Both completed runs have zero cancelled tests and zero TODOs. The focused selection includes all **35** final-proof/reused test files: the historical 32 plus three added regression files. The root package test script and canonical selection were not modified. The three existing intentional skips were not added or changed by this repair.

- Repository: `/root/artifact/trade-artifact`, branch `main`; work remains uncommitted and unpushed.
- Evidence root: **`/root/artifact-supervised-repair.gz4yFFVt/`**. Relative evidence paths below are beneath this directory.
- Final repaired manifest: **`engine/docs/v1.3-supervised-repaired-integrated-source-manifest.sha256`**.
- Final manifest SHA-256: **`d8de356f38e57403b250cff47c7890eb67401320be233c955800bd016de55fd6`**, **223 members**.
- All accepted members matched before/after focused acceptance, freeze, canonical execution, and closeout. Canonical source snapshots include those members plus the manifest itself.
- No canonical process-group survivors or canonical TAP-file holders were found after completion.

**Historical no-external-network compliance remains uncertified.** The verified isolation of these new runs does not retroactively certify, rewrite, or authorize the historical execution. No deployment, external RPC, broadcast, real-key/credential/wallet access, delegation, Git mutation, live activation, or further review cycle was performed.

## Preserved baseline and evidence

Before editing, the original integrated manifest was verified against all 220 working-tree members, the retained source archive, and the historical acceptance pre/post snapshots. The historical TAP checksum and successful process result were checked. Original work and evidence were archived without resetting the repository.

- Original manifest, preserved byte-for-byte: `engine/docs/v1.3-supervised-integrated-source-manifest.sha256`.
- Original identity: `69ff401e87184657350b73cc7f033125e5e4047d2d15b351bd9f8e3392ad6892`, 220 members.
- Retained historical regression: 589 tests, 586 passed, zero failed, three intentional skips; exit 0.
- Original evidence: `/root/artifact-supervised-complete.nv5y5yi9/acceptance-1800-once/`.
- Preservation records: `baseline-verification.json`, `baseline-worktree.json`, `baseline-git-status.txt`, `baseline-manifest.sha256`, `baseline-source.tar.gz`, and `historical-acceptance.tar.gz`.
- All 327 archived historical acceptance files still matched their originals at post-canonical verification and closeout. The original review, integration contract, rehearsal pack, historical closeout/addendum, and previous evidence were not edited.

The original manifest remains the historical baseline identity, not a claim that the repaired working tree still matches its old member hashes. Use the new repaired manifest for the accepted repair. Its delta is **211 unchanged members, nine modified members, three added test members, no removals**. The new closeout and evolving external evidence metadata are outside the source manifest to avoid self-reference.

## B1–B6 repair and regression mapping

Implementation/test paths in this section are relative to `engine/src/verification-scope-v1-3/final-proof-agent/`.

| Finding | Minimal repair | Executed composed coverage |
|---|---|---|
| **B1 — original-opening eligibility and source history** | `supervised-history-v1.mjs` no longer requires the authenticated setup cutoff to precede the economic lookback. Both admission and `supervised-finalized-source-v1.mjs` distinguish setup by the mandate's setup cutoff, independently of the unchanged source lookback. Post-setup activity outside the window still refuses. `final-episode-release-v1.mjs` gives acquisition only the economic view after complete three-lane admission has accounted for every original row. Original pages, repeated heads, exhausted pages, and independently checked setup bodies remain retained unchanged. | `supervised-repair-v1.test.mjs` exercises producer → acquisition closure → disposal closure → operational export → fresh-process replay with original setup age **604801 seconds** and **10 days**, including three distinct address-specific setup bodies inside the source lookback. One/two economic transaction populations remain separate. Exact **604800-second equality** is refused from the original finalized evidence even when wall time has advanced. The first eligible boundary succeeds. Existing history completeness/contradiction tests remain selected. |
| **B2 — actual dispatch deadlines** | `supervised-rpc-v1.mjs` checks the applicable absolute deadline and a synchronous trusted freshness predicate after durable request retention, immediately before transport, and on the response/retention path, alongside existing monotonic phase/per-call checks. Economic source awaits phase consumption and receives the runtime deadline. Simulation receives the runtime deadline and `orca-readiness-capture-v1.mjs`'s synchronous freshness check, wired by the trusted runtime. The existing asynchronous signing-freshness API remains intact. | `supervised-dispatch-deadline-v1.test.mjs` covers an already-open expired runtime, expiry during asynchronous phase consumption, expiry during request persistence, and simulation crossing challenge/runtime/monotonic freshness expiry while still inside the relative call timeout. It asserts zero late calls, retained phase consumption, ambiguous consumed signing state, no retained signed wire, and no renewed budget on replay/reopen. Existing late-response and submission-timing/recovery coverage passes unchanged. |
| **B3 — revocation reconciliation** | `supervised-exporter-v1.mjs` reconciles authenticated canonical revocation bytes with durable revocation digest, predecessor state/digest, time, episode, mandate, authorization, and human identity. Missing wrapper bytes after a committed revocation refuse with `SUPERVISED_REVOCATION_EVIDENCE_INCOMPLETE`; they cannot export as unrevoked. `retained-supervision-v1.mjs` checks exported revocation against every authenticated journal delivery, rejecting null or contradictory control. | `supervised-export-repair-v1.test.mjs` interrupts after authority commit but before wrapper retention, reopens existing state, verifies durable revocation, and requires incomplete export refusal. Fully rehashed null/contradictory control packages refuse. Disposal is revoked after dispatch, then later finalized evidence is retained and reconstructed: both economic transactions remain observed, control is revoked, and demonstration success is false. Nulling that revocation in a rehashed derived package refuses. |
| **B4 — admitted request selection** | The exporter selects by the durable ordinal's **decision ID, challenge ID, and recomputed admission digest**, with consistency among any matching records. It does not select the first/last journal request. Rejected unconsumed requests remain in the full journal. | An authenticated configured-agent decision with invalid challenge context is rejected without consuming the ordinal; a valid decision then uses the same still-unused challenge. Another rejected delivery follows. After retained acquisition completion and reopen, export/replay selects the middle admitted decision, retains all three request records, and observes exactly one simulation/signing path. |
| **B5 — idempotent revocation delivery** | The exporter accepts multiple deliveries only when their canonical authenticated bytes are identical and each reconciles to the same durable revocation. Every delivery remains in the exported journal. Reconstruction checks every delivery against the single control revocation. | Exact authenticated delivery → reopen → identical replay returns `ALREADY_REVOKED`. Both delivery records survive export and fresh-process reconstruction. Conflicting bytes still refuse. Control remains authenticated as revoked and cannot establish demonstration success. |
| **B6 — preserved unselected and selected exports** | The private exporter retains its original exclusive unselected destination and uses a distinct exclusive destination suffixed by the canonical selection digest for selected derived exports. Selection is validated against the independently reconstructed population before export. No caller/agent path field was added. | An unselected ordinal-2 export is reconstructed, the runtime is reopened, a wrong population binding is refused, and a correctly digest-bound selected export is created for the same ordinal. Both reconstruct. Every original package byte remains unchanged; all shared evidence members match byte-for-byte. Only descriptor selection and the package manifest differ. Reusing the same selected destination still fails exclusively with `EEXIST`. |

### Retained targeted RED → GREEN evidence

Every finding had a targeted behavioral RED retained before its corresponding implementation repair. Later repairs built on earlier repairs without resetting the worktree. Counts below include existing compatibility cases where explicitly selected; exact commands and full TAP are in each directory.

| Finding | RED directory: tests / passed / failed, exit | GREEN directory: tests / passed / failed, exit |
|---|---|---|
| B1 | `B1-red`: 3 / 1 / 2, exit 1 | `B1-green`: 17 / 17 / 0, exit 0 |
| B2 | `B2-red`: 6 / 1 / 5, exit 1; `B2-freshness-red`: 1 / 0 / 1, exit 1 | `B2-green`: 10 / 10 / 0, exit 0 |
| B3 | `B3-red`: 3 / 0 / 3, exit 1 | `B3-green`: 3 / 3 / 0, exit 0 |
| B4 | `B4-red`: 1 / 0 / 1, exit 1 | `B4-green`: 1 / 1 / 0, exit 0 |
| B5 | `B5-red`: 1 / 0 / 1, exit 1 | `B5-green`: 1 / 1 / 0, exit 0 |
| B6 | `B6-red`: 1 / 0 / 1, exit 1 | `B6-green`: 1 / 1 / 0, exit 0 |

These runs have no skips, cancellations, or TODOs. Their `result.json` records TAP SHA-256, bytes, exact exits, elapsed time, and source-map equality. Each retains `command.json`, `process.json`, `source-before.json`, `source-after.json`, and full `output.tap`.

Behavioral distinctions were preserved rather than manufacturing failures:

- B1 equality already refused correctly on the baseline; that passing control is retained. The two eligible in-lookback scenarios failed in composed source replay.
- Initial B2 monotonic advancement already triggered the existing relative guard. The regression was sharpened to begin near freshness expiry and cross it by less than the relative timeout; `B2-freshness-red` then demonstrated the missing freshness-at-dispatch check.
- Untampered revoked in-flight disposal already preserved the correct revoked/economic result on the baseline. Its regression's RED was the accepted **rehashed null-revocation package**, not a fabricated failure of the untampered economic observation.
- For an additional direct baseline cross-check, the final 15 repair cases were run against an external copy of the original frozen implementation, with disposable fixture instrumentation only. `all-findings-original-baseline-red-resolved-dependencies` contains **15 tests, one passed equality control, 14 genuine behavioral failures, exit 1**. All original 220 members were verified before instrumentation; all implementation members stayed at baseline hashes before/after the run. `baseline-reproduction-provenance.json` and `baseline-reproduction-source-after.json` retain that identity. The repository was not reset.
- The first external-copy attempt, `all-findings-original-baseline-red`, failed at module loading because the existing nested Orca SDK dependency directory had not been linked into the copied tree. Its three file-level failures are **harness failures, not behavioral RED**. The original evidence remains intact. Existing local dependency directories were linked; nothing was installed.

## Isolation, focused incompatibility, and canonical execution

### Enforced boundary

Before the first test, the original runner established a new user/network/PID namespace, removed capabilities, enabled `no_new_privs`, and verified locally that its network namespace differed from the parent, loopback was down, and there were no routes. A loopback-only connection probe failed; no external endpoint was contacted. `isolation-local-preflight` retains that result. All targeted RED/GREEN runs used that boundary and suppressed npm update checks.

The first complete focused run exposed an **isolation/fixture incompatibility**, not a repair-source failure:

- `focused-complete`: **432 tests, 427 passed, two failed, three intentional skips**, exit **1**, no timeout; elapsed **724.6952692428604 seconds**.
- Both failures were unchanged `sqlite-decision-authority-v1.test.mjs` ownership-fixture setup: `chown(..., 65534, 65534)` returned `EINVAL` under the single-UID user mapping. They did not reach their intended ownership assertions.
- No test, assertion, skip, package script, source member, or test selection was changed to clear this failure.

An available private network/PID namespace **without the single-UID user mapping** supports those fixtures. Its capability bounding/permitted/effective sets retain only `CHOWN`, `DAC_OVERRIDE`, and `FOWNER`; inheritable/ambient capabilities are empty. Namespace/network capabilities, including `SYS_ADMIN`, `NET_ADMIN`, and `NET_RAW`, are removed. `no_new_privs` prevents regaining removed privileges across exec. The private namespace has only down loopback and no routes. No host-wide networking, account, dependency, or service configuration was changed.

`isolation-compatible-local-preflight` verifies that boundary locally and demonstrates disposable ownership change to UID 65534 and back. `ownership-historical-green` runs the unchanged SQLite authority file: **21 tests passed, zero failed, exit 0**. The failed complete selection was then rerun under the compatible boundary, with the same 35 test files, yielding the complete focused GREEN above. This was a justified rerun of a **failed** gate, not repetition of successful acceptance.

Both runners use a new process group and private PID namespace. Intentional local timeout probes retain exit 124 / killed-child exit -9, and verify that even a descendant which started a new session no longer exists after namespace-init termination. Those are runner self-tests, not acceptance timeouts. See `runner-timeout-local-probe`, `runner-timeout-descendant-verification.json`, `runner-compatible-timeout-local-probe`, and `runner-compatible-timeout-descendant-verification.json`.

### Exact canonical invocation and controls

Invocation, exactly once: `python3 /root/artifact-supervised-repair.gz4yFFVt/run-canonical.py`.

Its sole acceptance command is **`npm run test`**, with a fixed **1800-second timeout**, cwd `/root/artifact/trade-artifact`, run through:

`/usr/bin/unshare --net --pid --fork --kill-child=KILL /usr/bin/setpriv --bounding-set=-all,+chown,+dac_override,+fowner --inh-caps=-all --ambient-caps=-all --no-new-privs /usr/bin/python3 /root/artifact-supervised-repair.gz4yFFVt/isolation-compatible.py npm run test`

The runner uses a constructed environment, not inherited credentials/configuration: fixed executable `PATH`; `HOME` and `USERPROFILE` pointing to the empty evidence home; `npm_config_update_notifier=false`, `npm_config_audit=false`, `npm_config_fund=false`; the parent namespace identity for the local assertion; and the operational export evidence directory. Exact values, runner/isolation script hashes, source-manifest identity, PID/PGID, timeout controls, and full expanded focused selection are retained in the command/process records. Fresh-process reconstruction runs inherit the isolation boundary even with their deliberately empty environment and `/tmp` cwd.

Canonical evidence: **`canonical-repaired-1800-once/`**.

- Full `output.tap`: **143717 bytes**, SHA-256 **`adb182f0493ad08650e206dcb59c4aff668430e1a705e70c6e34eb9e6f3ebbd9`**.
- `command.json`, `process.json`, `result.json`, `source-before.json`, `source-after.json` retain the complete invocation and process/source result.
- `execution-final-verification.json` verifies TAP checksum, unchanged current source, no remaining canonical process group or TAP holders, and retained operational package member hashes.
- No canonical failure, timeout, automatic retry, allowance extension, or post-acceptance source repair occurred.

## Frozen source, operational artifact, and checksums

- Repaired source archive: `repaired-frozen-source.tar.gz`, **224 files including the manifest**, SHA-256 **`ab7c917561e92b6d453a976963303261babf2e17d8278dabf719e1004720c424`**. Archive contents were checked against the repaired manifest.
- Freeze record: `repaired-freeze.json`; archive metadata: `repaired-frozen-source-archive.json`.
- Boundary checks: `pre-canonical-verification.json`, `post-canonical-verification.json`, `closeout-verification.json`.
- Whitespace/fixture visibility: `final-visibility-whitespace.json`; `git diff --check` passes, and explicit changed/new-file whitespace and ignore checks supplement tracked-diff checking.
- Per-file repository inventory: `final-changed-file-inventory.json`, distinguishing this repair's changes from preserved prior uncommitted work.
- Evidence file checksums: `evidence-files.sha256`; final checksum-verification metadata: `evidence-checksum-verification.json`. These checks do not rerun tests.

The canonical run retained a real synthetic operational export at:

`/root/artifact-supervised-repair.gz4yFFVt/canonical-repaired-1800-once/operational-exports/complete-export-Kho6tB/package`

Its manifest is **`57a71cebc8c5e7503627543350c7ee81bb3ab3c499763b3648b9c02aafc28c15`**, **295 members**. Every member was verified. The surrounding retained input/reconstruction files and both focused-run operational exports are preserved too. This is producer/exporter/reconstruction evidence using disposable offline signatures, not real transaction occurrence or a production demonstration.

## Individual changed-file inventory

The following are the files changed or added by this repair relative to the preserved session-start baseline. Paths beginning with `final-proof-agent/` below are relative to `engine/src/verification-scope-v1-3/`. Earlier uncommitted changes outside this list remain preserved; the external full inventory records their current identities without attributing them to this repair.

| Status in this repair | Individual path | Scope |
|---|---|---|
| Modified | `final-proof-agent/final-episode-release-v1.mjs` | B1 checked economic acquisition view after full history admission. |
| Modified | `final-proof-agent/fixtures/supervised-runtime-offline-v1.mjs` | Disposable original-opening/setup and journal/decision timing instrumentation. |
| Modified | `final-proof-agent/orca-readiness-capture-v1.mjs` | B2 synchronous trusted freshness-at-dispatch check. |
| Modified | `final-proof-agent/retained-supervision-v1.mjs` | B3/B5 authenticated journal/control revocation reconciliation. |
| Modified | `final-proof-agent/supervised-exporter-v1.mjs` | B3–B6 durable identity selection, revocation reconciliation, preserved derived destinations. |
| Modified | `final-proof-agent/supervised-finalized-source-v1.mjs` | B1 separate setup accounting; B2 awaited phase consumption and absolute deadline. |
| Modified | `final-proof-agent/supervised-history-v1.mjs` | B1 setup classification independent of lookback; unchanged completeness. |
| Modified | `final-proof-agent/supervised-rpc-v1.mjs` | B2 deadline/freshness enforcement around real dispatch and retained responses. |
| Modified | `final-proof-agent/wiggles-trusted-runtime-v1.mjs` | B2 trusted simulation dispatch deadline/freshness wiring. |
| Added | `final-proof-agent/supervised-repair-v1.test.mjs` | B1 composed original-opening and history regressions. |
| Added | `final-proof-agent/supervised-dispatch-deadline-v1.test.mjs` | B2 six actual-dispatch boundary regressions. |
| Added | `final-proof-agent/supervised-export-repair-v1.test.mjs` | B3–B6 operational lifecycle and fresh-process replay regressions. |
| Added | `engine/docs/v1.3-supervised-repaired-integrated-source-manifest.sha256` | Accepted repaired source identity; preserves original manifest. |
| Added | `engine/docs/v1.3-supervised-production-integration-repair-closeout.md` | This closeout, outside the source manifest. |

External runner, probe, snapshot, archive, copied-baseline, and checksum artifacts are confined to the evidence root; they are not additional production interfaces.

## Remaining limitations and stop boundary

This closes the authorized **offline repair and acceptance** scope only. Both `run-wiggles-supervised-v1.mjs` and `createArtifactFinalProofLiveExecutorV1` remain mechanically disabled and unchanged. No enable flag or alternate launch path was added. Administrator-private authority, existing economics, historical identities, one-time signing/submission semantics, and already-repaired submission timing remain unchanged. External administrator rollback infrastructure and public CLI/API decisions remain deferred.

Synthetic offline tests do not qualify real authorizations/setup archives, deployed identities, production key isolation, storage/WAL/fsync behavior, provider completeness/retries/cancellation, network/provider configuration, fresh live eligibility, or deployment/signing/broadcast approval. Those gates remain separate and unauthorized here. Historical network compliance remains uncertified despite this new enforced offline boundary.

The accepted source is frozen and verified; no repair blocker remains within this bounded offline scope. Work stops after acceptance, this closeout, and final integrity checks. All changes remain uncommitted and unpushed.
