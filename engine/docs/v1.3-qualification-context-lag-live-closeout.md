# Final context-lag qualification binding and one-session closeout

## Disposition

The separately frozen non-fixture binding was completed and locally verified.
**Exactly one newly authorized external session ran and stopped. Qualification
remains INCOMPLETE because the owner-watermark equality gate failed.**

There was no replacement session, policy repair after dispatch, signing,
broadcasting, account creation, production activation, canonical suite, general
review cycle or Git mutation. The new allowance is consumed permanently; all six
historical allowances are now consumed. Unused capacity does not renew authority.

Private evidence root (E):
`/root/artifact-private-helius-provider-qualification/full-context-lag-v1/`

The checkpoint remains `0e97f53511823d3d09fcd3ceefbba36bb0127da0`.
Previous closeouts, the offline fixture launcher/live refusal, uncommitted repairs,
prior freezes, evidence, reservations and both unrelated plans remain unchanged.

## Final binding

`E/harness/full-launch.py` is an additive copy of the previous full launcher,
not the live-refusing offline fixture launcher. Its enumerated changes are:

- New exclusive evidence/reservation root and five-prior-allowance accounting.
- Select the byte-identical tested `worker-context-lag-v1.mjs` as the actual CLI
  worker entry, with default limits and no fixture entry wrapper.
- In non-fixture preparation, remove inherited local bootstrap modules and admit
  exactly the seven-member relative qualification closure: context-lag worker and
  policy, predecessor bounded helpers, probe, repaired constructor, qualification
  input contract and scope.
- Reject nonzero fixture ports, fixture CA or fixture cases in live mode, at both
  public and managed launcher ingress. Live mode supplies no fixture route/input,
  synthetic credential delivery or test override.
- Bind the versioned policy in readiness and durably reserve the new allowance
  before the dedicated custodian reads the real credential.

Dedicated custodian, private FD5 delivery, prior operator-entry confirmation,
fixed `mainnet.helius-rpc.com:443` target, TLS hostname/certificate verification,
response screening, confinement, deadline supervisor and cleanup are reused.
The real launch reads only the dedicated credential source; no alternate source,
provider or credential entry was used. Fixture CA/key material exists only in
isolated local tests and is removed afterward.

### Frozen semantics

`ARTIFACT_QUALIFICATION_CONTEXT_LAG_SESSION_V1` permits at most **two additional
attempts across the entire session**, only after explicit correlated **-32016**
responses on permitted read-only methods. Delays are **250 ms**, then **500 ms**.
Method and parameters repeat exactly, including the context floor; only the
JSON-RPC correlation ID changes. Every attempt consumes the same **29-total**
ceiling and unchanged absolute launch/work deadline. Simulation is never retried.
Authentication, transport ambiguity, other errors and malformed/uncorrelated
responses do not qualify. The transport has zero hidden retries.

The successor specification distinguishes unchanged **logical method maxima**
from physical attempt counts. It is not a 31-request allowance. Slot floors,
opening/owner-watermark equality and below-floor rejection remain unchanged.
Bounds remain 5,000 ms/request, 60 s/launch, 50 s/work, 55 s/management,
1,048,576 bytes/request and response, and 16,777,216 aggregate response bytes.

### Verification before dispatch

- Reused the matching retained **12-case** context-lag full-worker coverage and
  **27-case** focused oracle coverage without rerunning either suite.
- Exercised the actual final launcher and its actual default CLI worker entry
  inside an isolated loopback-only network namespace, with synthetic dedicated
  custody and TLS:
  - Shared owner-read/fee lag followed by success: **25 attempts, two retries**,
    worker exit **0**, unsigned construction/fee/expiry/simulation reached.
  - Simulation returns -32016: **23 attempts, no retries**, expected worker exit
    **1**. The fixture runner passed; simulation was not repeated.
- Both fixture runners exited **0**, with cleanup verified and no real credential
  reading or external requests.
- Separately exercised actual non-fixture preparation: exactly seven qualification
  modules and no fixture route/input. Six local ingress checks rejected live fixture
  overrides before custody or dispatch.
- Verified source/runtime identities around tests and bound the full dependency and
  applicable evidence coverage: **7,727 readiness files**. No general review claimed.
- Preserved the inherited launcher's EOF blank-line hygiene warning in the separately
  copied executable bytes; readiness retains the exact diagnostic. No cosmetic
  post-freeze edit or test rerun was performed.

Frozen source-map SHA-256:
`b50ba0e8228c412c4f2eae7b718ec9f49c940321dc72dacb6d4c13f0a2211215`

Readiness SHA-256:
`cded7f5ccf4fa19132104765cc8ff2460a952502a83d96ff2db6d235c640f899`

Session-specification SHA-256:
`cbdfdb8e3c39aa83c0b66d942ac49a7d622c0f9bde5995193f67efa70dc14625`

Qualified runtime SHA-256:
`a07f1bd10f71521b5ce95cd9273367ade083741d1bd96b3b3071d1a9a38c6ebd`

Accepted production-source manifest SHA-256:
`094bedf06a1d8c41b207731afe268b80cc45e049496c9ee21c7b4b5d6a6b27ad`

## Actual external session

The one-shot dispatcher durably retained its exclusive intent. The final launcher
verified readiness and operator-entry confirmation and wrote
`single-session-reservation.json` as **CONSUMED_POSSIBLY_DISPATCHED** before the
real dedicated credential read. No restart or refund followed.

The worker retained **six exact request/response pairs**, six attempt reservations
and six completions. All responses were successful JSON-RPC result envelopes;
there were **zero RPC refusals, zero retries, zero retry reservations and zero
simulation calls**. Closed accounting totals **2,562 admitted response bytes**.

| Method | Actual attempts |
|---|---:|
| getGenesisHash | 1 |
| getSlot | 1 |
| getBlock | 1 |
| getMultipleAccounts | 1 |
| getTokenAccountsByOwner | 2 |
| getAccountInfo | 0 |
| getSignaturesForAddress | 0 |
| getTransaction | 0 |
| getLatestBlockhash | 0 |
| getFeeForMessage | 0 |
| getBlockHeight | 0 |
| simulateTransaction | 0 |
| **Total** | **6 / 29** |

Fresh observations established authenticated mainnet genesis and a finalized fresh
anchor. Opening account context was **446127931**. Both owner requests retained
`minContextSlot: 446127931`:

- Classic Token Program returned context **446127932**, two accounts.
- Token-2022 returned context **446127931**, zero accounts.

Both lane contexts met the minimum floor, but the classic lane did not equal the
opening watermark. `probe-successor.mjs:52–53` records false equality and stops;
`findings.json` is **STOPPED_PARTIAL_EVIDENCE**. The session ledger has no transport
stop reason because this was a downstream semantic gate, not an RPC refusal.

This successful-but-unequal response is not -32016 and is not retry-eligible.
There was no whole-owner-pair restart, delayed sampling, lowered floor or relaxed
equality. No backend-topology explanation is established by these observations.

### Construction, simulation and cleanup

History/setup sampling, live route sampling, quote, message, latest blockhash,
exact-message fee, expiry and simulation were **NOT REACHED**. No live quote/message/
fee/expiry identity or simulation outcome exists. Local synthetic success does not
fill those missing live stages or prove the oracle repair against this session.

- Worker exit: **1**.
- Supervisor CLI and dispatch wrapper exit: **0** — not qualification success.
- Launch elapsed: **13.849655797705054 seconds**; no timeout.
- Isolation before release, namespace retirement, supervisor-domain retirement,
  temporary-root removal and no residual owned processes were verified.

The retained final CLI result includes the supervisor's post-retirement fields;
`outer-result.json` is the earlier finishing record. Two outcome-recorder attempts
failed while selecting/comparing these records: first a missing final field, then
elapsed-time equality across the finishing interval. Both failures are retained.
Only the recorder was corrected: other shared fields must match exactly, elapsed
must advance monotonically within 60 seconds, and final cleanup/process identities
must verify. The corrected outcome verifier exited **0**. No provider or test was
rerun and no executable policy/launcher changed after its freeze.

## Final integrity and production boundary

The outcome verification rechecked exact bytes/hashes, correlated IDs, canonical
method/parameter template identities, ordinal and completion/reservation linkage,
response-byte totals, closed ledger, live module closure and owned process/namespace
absence. All **253 accepted production members**, **5,394 qualified runtime members**
and **3,811 preserved historical/source/evidence/report/plan members** remained
unchanged. The final source and all readiness-bound files also remained unchanged.

**The binding is complete; full provider qualification is not successful.** No
production gate is waived. Original executed-setup archive identity remains UNKNOWN;
production provenance admission, runtime/release, deployed isolation/custody,
control/revocation, evidence/replay and fresh episode authorization remain separate.
There is no remaining external allowance from this instruction.

New repository file this continuation:

- `engine/docs/v1.3-qualification-context-lag-live-closeout.md`

The final launcher, specification, authorization, local integration evidence,
readiness, dispatcher, reservation and external evidence are private under E.
They are not proposed Git members. No repository executable was changed this turn.
The accumulated uncommitted repair/document set, excluding both unrelated plans,
is retained in `final-verification.json`; nothing was committed or staged.

**STOP after this session and closeout. No repair-and-provider cycle begins.**
