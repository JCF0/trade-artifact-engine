# Construct Orca adapter — B1 repair addendum

## Disposition

**B1 resolved** by bounded implementation repair, RED→GREEN regression evidence and one targeted final source readback. This is not another broad review or live-release approval. The original `v1.3-construct-orca-adapter-readonly-review.md` and original closeout remain unchanged as historical evidence.

## Exact files changed

Under `engine/src/verification-scope-v1-3/final-proof-agent/`:

- `orca-message-boundary-v1.mjs:32`: replace downward division with rejection when `minimum * 10000n < quote * (10000n - BigInt(maximum_slippage_bps))`. Thus acceptance requires the exact authorized cross-product inequality, with no rounding allowance. Existing raw-input, mandate, upper-output and other validation is preserved.
- `orca-message-boundary-v1.test.mjs:42–63`: exact BigInt cross-product oracle for quote/minimum pairs `10000001/9950000` (reject), `10000001/9950001` (accept), `10000000/9950000` (accept), and `10000000/9949999` (reject). Existing divisible disposal coverage is retained.
- `orca-signing-composition-v1.test.mjs:15–107`: add BAD_SLIPPAGE to the authenticated regression harness. Construct an unsigned valid template, encode the forbidden minimum, hash those exact message bytes, then issue readiness against the complete final source. Assert source/readiness and fee-message commitments, exact slippage error, zero signer calls, and retained RESERVED stage with zero calls after ordinary reopen/replay.

Also changed:

- `engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`: refresh only the three source/test digests above; preserve all 155 member paths and all other member hashes.
- `engine/docs/v1.3-construct-orca-b1-repair-addendum.md`: this new addendum, outside the source manifest like the historical closeout/review.

No other implementation, economics, projections, production bridge, selection, claims or historical identity files were changed. Existing modified/untracked work was preserved. No Git staging, commits, pushes, resets or branch changes.

## Finite verification

Working directory: `/root/artifact/trade-artifact`.

Focused command (run once RED, once GREEN):

`node --test engine/src/verification-scope-v1-3/final-proof-agent/orca-message-boundary-v1.test.mjs engine/src/verification-scope-v1-3/final-proof-agent/orca-signing-composition-v1.test.mjs`

- RED before implementation change: exit 1; 9 tests, 7 passed, 2 failed, zero skips. Pure boundary failed with “Missing expected exception.” Authenticated BAD_SLIPPAGE passed preparation/fee binding and reached the inert signer, then returned `bounded_agent_signed_wire_boundary_invalid: signed message mismatch` instead of the expected slippage error. This establishes that the regression was not merely a stale-digest rejection.
- GREEN after the one-line repair: exit 0; 9 tests, 9 passed, zero failures/skips. BAD_SLIPPAGE requires `bounded_agent_orca_message_invalid: minimum output exceeds slippage authority` and asserts callback count exactly zero, including after reopen.
- Canonical `npm run test`: invoked exactly once after focused GREEN, within a checksum-bracketing Python subprocess; npm and wrapper exit 0. **300 tests, 297 passed, zero failed, three skipped**, zero cancelled. Existing experimental SQLite warning remains. Full output: `/root/.hermes/cache/terminal-output/out-1788836532-1504523-9d90.log`.
- `git diff --check`: no diagnostics. Additionally ran `git diff --no-index --check /dev/null <path>` for each of the three changed source/test files (currently untracked): exit 1 for file differences, empty stdout/stderr, no whitespace errors.

## Final snapshot and targeted readback

Initial manifest SHA-256: `387bfdd343e17080197481d1a2b1f53cc10bfcb8fdd5d1f7465712a0aa830d42`; all members verified before edits.

Final manifest SHA-256: **`8a2095da1bec93e2a0052902cfc8e50c0941f1d3a032bab87b08ae4ed6e8cd81`**.

All 155 members verified before and after the canonical run, with zero mismatches and unchanged manifest bytes across acceptance. Only the intended three member digests differ from the initial snapshot.

One final targeted readback inspected the inequality and both new regression regions. It confirmed BigInt-only cross-products without division, preserved validation, explicit adjacent/divisible thresholds, and correctly bound composition rejection with zero signer invocations. B1 is resolved on these accepted bytes; no separate implementation defect was encountered.

## Unchanged release boundaries

The live entry point remains mechanically disabled and unchanged. Operational readiness capture, real signer deployment, OS confinement, submission integration and live evidence remain later gates. Approved trusted administration for one supervised episode is unchanged; automatic administrator-snapshot rollback detection remains excluded and external-head infrastructure deferred.

No wallet/credential-file access, real-credential signing, RPC, broadcasts, scheduler activation, infrastructure work or delegation occurred. Existing offline tests use published test fixtures and test cryptography only. All changes remain uncommitted. No additional test/review/readiness cycle follows this addendum.
