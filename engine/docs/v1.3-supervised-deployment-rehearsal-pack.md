# Artifact supervised deployment and rehearsal pack

## 0. Disposition and checkpoint

**Preparation only. The accepted offline implementation is not an approved deployment or a live-ready executor.** The next session should first authorize a bounded production-composition build, not launch the existing executable. This pack authorizes nothing: every command below is proposed and was **not executed** in preparing it. No source/configuration edits, tests, installs, repository imports/entry-point execution, key/credential access, signing, RPC/network, service activation, deployment, delegation or Git mutations were performed.

Repository: `/root/artifact/trade-artifact`.

- HEAD: `aa752943f17c24705a3763cbc1f3df90ef4256cd` — `Complete offline final episode reconstruction and fee binding`.
- Branch: `main`; local `origin/main` has the same full commit ID. This corroborates local tracking-ref agreement; no remote contact/fetch was made. The user reports the checkpoint committed and pushed.
- Accepted source manifest: `engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256`.
- Manifest byte SHA-256: `da60b0bc18331cc6ae6d6cac4fc9877a103d55348e5d8373d654da4815f58d65`; **200 entries / 200 unique paths**, all member hashes verified at preparation preflight. Repeat this exact identity and full membership check after saving the pack.
- Retained canonical acceptance: **539 tests, 536 passed, zero failed, three intentional skips, exit 0**. This is historical acceptance, not a run performed for this pack. Source: [fee-repair closeout](v1.3-final-episode-fee-binding-repair-closeout.md):83–139. The maintained command is root `package.json:6`; it must not be run just to refresh a preparation badge.
- Initial tracked diff was empty. Initial `git status --short --untracked-files=all` contained only:

```text
?? .hermes/plans/2026-09-02_095802-controlled-route-evidence-calibration-capture.md
?? .hermes/plans/2026-09-02_145703-phase-3-calibration-wallet-and-simulation.md
```

Those plans are preserved. Their respective SHA-256 values are `f4bc20edc9d0981d86213eff090f61901d6d3e44ce292ab7089f9c0e462c01bb` and `8bdfda6c946d3b1d9e989baa591b7cf0e624925154c35ca5d1bc8a4902b4f4b8`. This new pack is outside the accepted source manifest; do not refresh that manifest for this documentation-only handoff.

No applicable `AGENTS.md`, `CLAUDE.md` or `.cursorrules` was found in checked ancestors, repository searches or the relevant engine subtree outside dependencies. README and the documents below supply repository context; README's legacy mint/API quick starts are **not** this procedure.

### Reference precedence

In source citations, **A/** means `engine/src/verification-scope-v1-3/final-proof-agent/`; **V/** means `engine/src/verification-scope-v1-3/`. Line references describe the HEAD above.

Reuse these documents rather than copying their full contracts:

1. [Final reconstruction completion](v1.3-final-episode-reconstruction-release-completion.md), especially :29–76 and :116–120, followed by the [fee-binding repair closeout](v1.3-final-episode-fee-binding-repair-closeout.md):35–47. The 199-member/526-test completion predates the accepted 200-member/539-test repair. Their “uncommitted” statements are historical, superseded by current Git state, not edited here.
2. [Trusted runtime deployment contract](v1.3-wiggles-trusted-runtime-deployment.md):23–47 for proposed custody and configuration; `engine/deployment/wiggles-supervised-v1.example.mjs` is an incomplete proposal, not installed configuration.
3. [Trusted submission timing repair](v1.3-wiggles-trusted-submission-repair-closeout.md):35–80 and :150–154 for actual effect-boundary timing and consumption.
4. [Supervised release checklist](v1.3-construct-supervised-release-checklist.md):3–11 for the approved one-episode scope, and [rollback decision](v1.3-bounded-agent-rollback-deployment-decision.md):3–9 for the explicit external-infrastructure deferral.
5. [CLI/API assessment](v1.3-cli-api-readonly-assessment.md):3–9 and :95–101: public CLI/API work remains deferred. Its :80 missing offline loader/reconstruction items are now closed by the reconstruction completion.

Older deployment/checklist statements that submission and final reconstruction are wholly unimplemented are stale at this checkpoint. `A/wiggles-trusted-runtime-v1.mjs:186–189` now exposes trusted submission/finalization in the **offline** runtime; the deployment document's :21 and :53 describe its earlier slice. They do not prove a production path exists.

## 1. What remains, separated by kind

### A. Necessary implementation / integration

| Remaining work | Source basis and bounded interpretation |
|---|---|
| Connect production validation to a fixed supervised executor, without caller-supplied privileged capabilities | `A/run-wiggles-supervised-v1.mjs:1–10` calls the refusing factory before any arguments/configuration/channels. `A/live-executor-composition-v1.mjs:5–14` has no signer dependency and always refuses. `A/wiggles-production-configuration-v1.mjs:9–27` is structural/authentication preflight only. `A/wiggles-trusted-runtime-v1.mjs:143–169` permits only the distinct disposable offline profile. Filling the five mandate values cannot unlock any of these. |
| Supply trusted production read transport, clocks, submission transport and finalized-source acquisition | Capture consumes injected `{body, signal}` transport and wall/monotonic clocks (`A/orca-readiness-capture-v1.mjs:46–67,128–171`). Submission requires injected transport/sleep/finalization source and refuses non-offline mandates (`A/wiggles-submission-v1.mjs:307–316,451–454,478–502`). `A/wiggles-terminal-closure-v1.mjs:18–44` expects a separately source-bound context, not scheduler labels. No production binding for those capabilities is released. Reuse the accepted validators and scheduler; do not introduce another execution engine. |
| Retain/export a real episode into the accepted replay format | Runtime already retains readiness records, wire, submission journals and economic contexts, but no supervised producer is wired to emit the flat `episode.json`/control/member inventory accepted by `A/retained-episode-package-v1.mjs:95–148` and `A/final-episode-release-v1.mjs:38–53,123–127`. The located v2 producer is the synthetic fixture `A/fixtures/retained-final-episode-v1.mjs:168`; it is not a live exporter. A small trusted evidence assembler must preserve original record bytes, admissions and clocks, and exact submission path→member mappings required by `A/retained-final-control-v1.mjs:26–81,163–189`. This is private operational glue, not deferred product CLI/API work. |
| Resolve simulation and submission-profile integration explicitly before activation | The checklist :61 calls for simulation evidence, but readiness's exact method list has no `simulateTransaction` (`A/orca-readiness-capture-v1.mjs:14–15`); the signer proceeds after readiness/checkpoints (`A/wiggles-trusted-runtime-v1.mjs:157–169`). Existing send requests use `skipPreflight:false`, not a separately retained pre-sign simulation. The production build must supply/review the required exact-message simulation gate or stop for an explicit scope decision; do not claim simulation is already covered. Submission and retained replay currently require `OFFLINE_INJECTED_SUBMISSION_V1`, including for prospective supervised replay (`A/retained-final-control-v1.mjs:175–188`). Define and review production compatibility honestly, preserving historical offline bytes rather than disguising a live transport as a test transport or deleting profile checks. |

**Already implemented offline:** authenticated decisions/revocation and durable consumption; exact Orca message/signature/wire binding; executor capture and freshness; bounded identical-byte transmission plus terminal verification; independent flat-package reconstruction of original eligibility, control, transmission and economics; observed finalized-fee equality to the authenticated exact-message fee, including failed execution. See completion :29–38, fee repair :35–47, and current source cited above. None needs wholesale redesign.

### B. Deployment configuration and isolation — not proved by source

- Assign actual distinct controller, runtime and administrator identities and supervisor ownership. Establish cross-UID pipe handoff and separate human-revocation delivery. No tracked broker is installed by this checkpoint. Verify denied access under the actual UIDs, not by inspecting private JS exports. Source: deployment contract :23–41; `A/wiggles-decision-channel-v1.mjs:3–7` requires a supervisor-supplied composed agent port and streams.
- Freeze trusted read-only release/dependency/Node/configuration roots, clean environment/FD table, no debugger/core dumps, no automatic restart, and runtime-only private key/state roots. Validate real storage's SQLite/WAL/fsync semantics and prevent Hermes from changing ancestors, code, keys, state or launch inputs. Source: deployment contract :29–41; `A/wiggles-trusted-runtime-v1.mjs:18–57,93–120`.
- Provision one initial authority outside runtime launch. Runtime open must refuse missing/corrupt/empty state, never initialize. Source: `A/sqlite-decision-authority-v1.mjs:1142–1158`; offline-layer document :43,58–64.
- Qualify the chosen provider and egress policy: correct network/genesis, finalized evidence, supported context floors and complete enumerations/history, bounded body size, no hidden transport retries/redirects, no credential-bearing diagnostics. Equal finalized watermarks do not establish an atomic snapshot. Source: capture :173–260 and deployment contract :37,45–47. Provider behavior, real host permissions and installed dependency integrity have not been checked here.

### C. Human authority — must not be inferred

Actual principal ownership, approved deployment/custody choices, authentic setup artifacts, approved immutable release/configuration, bounded RPC budget table and execution interval remain required. Section 5 distinguishes human choices from derived hashes and chain observations. Source: production validator :15–26; deployment contract :43–47; `A/human-authorization-v1.mjs:8–38,58–69`.

### D. Evidence available only in later supervised stages

Actual UID/FD/revocation isolation, durability on the deployment filesystem, provider conformance, original final-wallet eligibility, route/liquidity/fee/blockhash freshness, any separately required simulation, signed-wire custody, real dispatch timing, finality/failure/absence, complete source history/boundaries and independent final reconstruction must be obtained at their appropriate stage. Offline fixtures prove neither an executed transaction nor real custody/provider truth. Sources: completion :70–76; fee closeout :137–141; timing repair :74,152–154. A valid economic claim alone does not prove authenticated direction or original eligibility (`A/final-episode-release-v1.mjs:185–198`).

## 2. Smallest next build task

**Task: implement and offline-qualify the private supervised production binding and its retained-evidence handoff, keeping live entry points disabled until a separately reviewed release is approved.** This is a bounded vertical integration of existing components, not CLI/API development, a daemon, a scheduler redesign, a new economic model or external rollback infrastructure.

Deliver only the missing adapters/wiring:

1. Administrator-owned closed production configuration → strict production validator → existing-state open → private capture/signing/submission/closure composition. Fix transports, clocks, evidence sinks and source providers in trusted construction, never on the agent channel. Keep provisioning separately invoked.
2. Bounded credential-safe transport and finalized-source acquisition which captures once under explicit budgets and then replays locally for validators. Preserve exact request/response bytes before parsing, original clocks and context floors. Bind submission/finalization limits as well as capture budgets into the approved deployment/release identity; the current canonical `c.budget` hash alone binds capture configuration, not an invented all-purpose network allowance.
3. Explicit production handling of the simulation requirement and offline submission-profile restriction identified in §1A. Preserve the fixed swap topology and all accepted offline profiles. If the accepted semantics cannot admit the required production evidence without a schema/profile extension, stop and scope that additive change before activation.
4. Private supervisor wiring for readiness, one-envelope decision ingress, human revocation, ordinal-only trusted submission/finalization, and exclusive retained-package assembly. Export original capture/control and exact child evidence to v2 replay; no fixture synthesis, latest-file discovery or caller-authored economics.

**Acceptance criteria for that later authorized build:**

- Production-invalid/test/placeholder configurations refuse before authority/key/provider effects; real-production-shaped negative cases use inert signers, not the final wallet. Any disposable offline signing requires an explicit test-only exception in the new task.
- Only authenticated configured-principal decisions reach durable reservation. Replayed/conflicting decisions, revocation races, loss of outputs, key-load uncertainty, missing state and ordinary restart cannot sign replacements or replenish send budgets.
- Real production-shaped composition is exercised offline with bounded transport fixtures: raw retention, exact-message simulation if required, deadlines at the actual transport boundary, no hidden retries/redirects, cancellation/late responses and source recapture. Preserve all existing economic, fee and scheduler invariants.
- The operational producer's exact emitted bytes, not a hand-built verifier-shaped fixture, pass the existing independent loader/reconstruction in a fresh offline process. Cover complete synthetic episode, acquisition-only, refusal/revocation, finalized failure, unresolved/partial evidence and rehashed fee/control/transmission contradictions. Synthetic success must remain distinct from real success.
- Fresh dependency-closed release identity, focused RED→GREEN evidence and one newly authorized canonical run after focused GREEN; no unchanged successful reruns. Scope any independent final-byte review explicitly. Retained 539-test acceptance remains historical evidence for this predecessor.
- Default/unfinished launch still refuses. The final activation diff may replace the disabled production surface only after the complete composition and finite acceptance are reviewed and approved. Never remove the refusal first, add an enable environment variable, import the offline runtime against the final wallet, or call low-level signer/scheduler helpers as a bypass.

**Dependencies:** human authorization of this build scope and finite test/signing exception, proposed non-secret deployment/transport design, approved handling of simulation and production profile compatibility. Real principal values, real keys and an execution window are **not prerequisites for starting the offline build**. Actual live approval is a later dependency.

Launch order is therefore: **bounded build → offline acceptance/frozen release review → approved deployment/isolation rehearsal → fresh read-only live capture approval → authenticated episode/signing/broadcast approval → one supervised episode → independent retained reconstruction**. There is no currently usable launch flag or config-only route around this order.

## 3. Ordered supervised preparation and rehearsal

### Stage P — now-preparable records, no activation

1. Record the checkpoint above and assign owners for controller, trusted runtime, provisioning, supervision, revocation and evidence custody. Prepare a non-secret deployment worksheet; retain `<UNKNOWN>` values rather than guessing. Preserve one authorized episode with two bounded initiation/refusal decisions and trusted administration.
2. Recover the independently retained setup archive, setup freeze and setup evidence manifest through the custodian. Record their actual verified hashes in `mandate.setup_authority`; this task recovered the recorded time boundary, not those external artifacts or their authenticity. Do not replace setup digests with synthetic constants.
3. Define four separate destinations: `<READ_ONLY_RELEASE_ROOT>`, `<PRIVATE_EXECUTOR_STATE_ROOT>`, `<NEW_PRIVATE_CAPTURE_ROOT>`, and `<NEW_FLAT_REPLAY_ROOT>`, plus `<NEW_OFFLINE_REPORT_ROOT>` outside the flat package. Roots/parents must be explicit, non-symlinked and appropriately owned. No evidence output may overwrite a prior episode or retained failure. Never place keys/credentials in a replay package. Source: deployment :29–37; package loader :95–142.
4. Bind release bytes, dependency lockfiles/resolved installed closure, Node binary/version and fixed launcher code in `<APPROVED_EXECUTOR_RELEASE_SHA256>` using a reviewed release inventory definition. Keep approved public configuration/budgets and their identities separately recorded to avoid a self-referential release hash. Neither HEAD nor the 200-member source-manifest digest alone is the final executor release approval.

**Proposed read-only checkpoint commands — not run as rehearsal here:**

```sh
cd '<REPOSITORY_ROOT>'
git rev-parse HEAD
git status --short --branch --untracked-files=all
sha256sum engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256
sha256sum -c engine/docs/v1.3-bounded-agent-authority-source-manifest.sha256
```

For this predecessor the expected digest/count are §0. For a later changed release use its separately approved identity, not these values by rote. Any unexpected drift is STOP, not permission to regenerate checksums.

### Stage R — offline rehearsal, separate approval before local effects

**Prerequisites:** the bounded build is accepted; a supervised offline-rehearsal authorization explicitly names host, identities, temporary state/evidence roots, permitted tests and whether disposable synthetic signing is allowed. Network denied. No final-wallet key, production authority database or real authorization is available to this rehearsal.

5. Under the actual deployment identities, use harmless canary files and synthetic state to prove the permissions in deployment :29–41: Hermes cannot traverse key/state roots, alter release/config/ancestors, inspect runtime FDs/memory, invoke provisioning or acquire privileged shell rights. Runtime cannot modify the release or provisioner files. Verify supervisor-only control/revocation paths and isolated inherited FDs. Record PASS/STOP with evidence; do not use a real key to test denial.
6. Provision a fresh **synthetic** episode via the distinct provisioning API, never on runtime open. Exercise the actual supervisor handoff, one envelope then EOF, canonical/signature failures, duplicate delivery and revocation. Verify durable wire retention, failure/ambiguity handling and normal reopen using approved synthetic fault probes. No automatic restart policy and no production restore experiment.
7. Exercise producer→retained-package→independent replay with egress and signer capabilities absent from the verifier. Freeze failures as well as positives. Hash outputs and inputs; do not present synthetic observations as final-wallet eligibility.

The existing root regression command, **only if included in the later finite build/rehearsal authorization**, is:

```sh
cd '<APPROVED_BUILD_REPOSITORY_ROOT>'
npm run test
```

This is not a new acceptance request and must not be run on this unchanged checkpoint for this preparation task. New deployment probes will need their exact reviewed command list from the build deliverable; there is no installed cross-UID rehearsal runner to invoke today.

### Stage D — real deployment preparation, separate administrator approval

**Prerequisites:** final source/dependency release is frozen and approved; offline rehearsal passed; approved host/UID/group/path/FD/egress plan and stop owner exist. Provisioning and key custody are administrator operations, not Hermes operations.

8. Install only the approved immutable release and configuration under the approved owners. No package installation at launch, mutable module paths, `NODE_OPTIONS` loaders/inspectors, inherited secrets in argv/logs, core dumps or restart manager. Provision signer custody out of band under deployment :32; no request to paste any private material into chat.
9. Validate public configuration and authentic human/agent ownership. Freeze the actual mandate and obtain the human's canonical domain-separated authorization. Its statement is `AUTHORIZE_ONE_BOUNDED_AGENT_DIRECTED_TWO_SWAP_FINAL_PROOF_EPISODE`; its `not_before_unix_seconds` is the fixed earliest candidate, not a free parameter (`A/human-authorization-v1.mjs:34–36,63–69`). Reaching that time still does not authorize execution.
10. **Approval boundary: production durable-state provisioning.** Administrator verifies the exact authorization/release and that this is the sole intended new episode, then separately invokes the existing APIs through a reviewed private harness:

```text
createAuthorizedEpisodeStateV1({ mandate: <VALIDATED_MANDATE>, authorization: <VALIDATED_HUMAN_AUTHORIZATION> })
provisionCrashDurableDecisionAuthorityV1({
  state_root: <PRIVATE_EXECUTOR_STATE_ROOT>,
  initial_episode_state: <DERIVED_AUTHORIZED_STATE>,
  executor_release_sha256: <APPROVED_EXECUTOR_RELEASE_SHA256>
})
```

These are existing **library call specifications**, not executable shell commands. State derives from the authorization; do not hand-author database rows. The provisioning function closes the authority; runtime uses only `createCrashDurableDecisionAuthorityV1({state_root})`. Source: `A/retained-final-control-v1.mjs:107`; SQLite authority :1142–1158. Existing or missing-after-use state must not be deleted/reprovisioned to make this step pass.

11. Supervisor arranges fixed descriptors and human revocation access before allowing decision ingress. The deployment's current Node launch shape is below, but **at this HEAD it intentionally exits 1 with `bounded_agent_live_executor_not_released`**. It reads no configuration and is not a rehearsal or deployment command to try:

```sh
# BLOCKED AT THIS CHECKPOINT. Future reviewed launcher/FD wiring is a build deliverable.
cd '<APPROVED_READ_ONLY_RELEASE_ROOT>'
umask 0077
ulimit -c 0
env -i PATH='<APPROVED_MINIMAL_PATH>' HOME=/nonexistent NODE_OPTIONS= NODE_PATH= \
  '<PINNED_NODE_ABSOLUTE_PATH>' \
  '<APPROVED_READ_ONLY_RELEASE_ROOT>/engine/src/verification-scope-v1-3/final-proof-agent/run-wiggles-supervised-v1.mjs'
```

This shape alone neither selects a UID nor hands off FDs nor supplies configuration. The trusted supervisor must do those under its separately reviewed host mechanism. No invented `--enable`, config flag, systemd unit, broker path or installation command is supplied. **STOP until the build supplies the exact approved launcher invocation and descriptor/configuration contract.** CLI/API deferral does not excuse missing private operational wiring.

### Stage C — read-only live capture, separate finite RPC approval

**Prerequisites:** transport/capture is reviewed, destination paths are exclusive, provider completeness/context-floor assumptions admitted, and the user approves `<PROVIDER_CAPABILITY>`, exact public wallet/accounts, `<RPC_BUDGET_TABLE>`, `<CAPTURE_DEADLINE_UNIX_SECONDS>` and output root. No key loading, decision submission, signing or broadcast is included. A capture may use a separately reviewed signer-inaccessible harness; never approximate this separation with a legacy `--dry-run` command.

12. Retain exact bounded RPC requests/responses and failure records before parse, hash and fsync them; retain a sanitized capability identity, not credential URLs/headers. Qualify genesis/finality, original setup/history and current balances using §4. Verify route accounts/tick arrays, quote, exact message fee and lifetime through existing capture. Every retry must be separately represented in approved budgets. Readiness allows only a narrow configured fee `-32016` retry of zero or one; this is **not** provider send retry permission (capture :65–66,298–305).
13. Respect readiness hard ceilings, not assumed defaults: `total_calls <= 64`, `history_pages <= 4`, `max_response_bytes <= 1048576`, `freshness_seconds <= 300`, `overall_timeout_ms <= 60000`, `call_timeout_ms <= 5000` and `call_timeout_ms <= overall_timeout_ms`, with positive per-method budgets for the ten methods at capture :14–15. Choose values only through approved configuration. A rehearsal capture's challenge/quote/blockhash cannot be reused after expiry or process reopen.
14. If a separate exact-message simulation is required by the final reviewed procedure, approve its method/count/time and signed-versus-unsigned form explicitly. Unsigned simulation belongs in this no-sign phase only if the reviewed binding supports it; simulation requiring a wallet signature crosses Stage S. There is no `simulateTransaction` capability in today's readiness method list, so do not add an ad hoc RPC call or assume a successful send preflight satisfies the missing retained simulation gate. Failure, stale evidence or an unsupported simulation contract is STOP.

**Operation specifications, not available live commands:** the final private harness must fix the provider capability and use `createOrcaReadinessCaptureV1`; the composed supervisor later calls `issueReadinessChallengeV1('ACQUISITION')` or `('DISPOSAL')`. It must not accept a state/quantity/transport chosen by Hermes. Capture-only qualification and live session readiness are distinct: the latter must be fresh and executor-owned inside the signing composition. No final-wallet RPC was made in preparing this pack.

### Stage S — actual signing and broadcast, explicit approval boundaries

**Prerequisites:** approved released production composition and host, authentic immutable episode authorization, fresh original eligibility, bounded capture/submission/finalization budgets, functioning revocation/supervision, retention health and any required simulation. Obtain explicit approval for the named real operations before entering them. The one-episode authorization retains the agent's bounded decisions; it does not permit humans or scripts to inject economic choices through another interface.

15. **Before real signing:** supervisor issues the fresh acquisition challenge. Hermes receives the challenge, signs one existing canonical bounded decision using its own control identity, writes exactly that envelope and closes the decision pipe. Allowed actions remain `INITIATE_ACQUISITION` / `REFUSE_ACQUISITION`, then `INITIATE_FULL_DISPOSAL` / `REFUSE_DISPOSAL`. No paths, quantities, transport selectors or transaction bytes enter the pipe. Channel bound: 131072 bytes and at most 60000 ms; 30000 ms is a proposal, not an approved setting. Source: `A/wiggles-decision-channel-v1.mjs:6–36`; offline layer :19–28.
16. Executor authenticates configured principals before durable lookup, reconciles challenge/current state, consumes ordinal, records PREPARED then KEY_LOAD_STARTED_AMBIGUOUS, and only then loads the wallet key. Recheck private freshness and revocation after key IO/derivation. Sign once, verify exact message/signature, fsync wire and directory, independently reread and register SIGNED_INTENT_DURABLE. The agent receives only the existing small result. `NOT_SUCCESSFUL` or lost output means supervisor reconciliation, **not** “unsigned.” Source: runtime :63–89,157–189; deployment :13–19.
17. **Before real broadcast:** supervisor verifies operation approval still covers this exact retained intent, runtime deadlines/revocation and evidence health. Trusted invocation is ordinal-only `trusted.submitRetainedIntentV1(1)` (later `2`), not an agent command. Commit SUBMISSION_POSSIBLE before effects, consuming the **whole schedule**. Reopen cannot use “remaining” slots. Retained readback never grants permission to submit.
18. Use only the existing reviewed schedule: maximum **three total client sends per leg** (initial plus at most two rebroadcasts), identical signed bytes/signature, provider `maxRetries:0`, no hidden transport retry or redirect. Initial matching acknowledgment is required for rebroadcast scheduling. Half-open windows are acknowledgment-relative `[2000,3000)` and `[5000,6000)` ms; status must be null and block height live at the prescribed checks. Missing a window cancels it, never moves it. Final timing check follows durable reservations immediately before external dispatch. Source: reused scheduler :10–14,84; submission :325–328,368–447. No rebuild, requote, blockhash refresh, re-sign, replacement, cleanup, Helius Sender, tips, priority fees or Compute Budget.
19. **Finalized read/closure approval:** the approved submit operation may include its bounded terminal reads; record that scope explicitly, not as free unbounded polling. Scheduler resolution ceiling is 180000 ms, 90 opportunities and 181 calls; wrapper permits at most 188 total calls, 190000 ms and 1048576 response bytes (actual approved limits may be smaller). Deadline expiration is not chain absence. Verify exact signed wire/signature, status/transaction slot and error agreement and timing evidence. Source: scheduler :171–194; submission :307–316,478–501.
20. Before disposal readiness, separately acquire/retain the existing source-authoritative one-leg closure under its approved read budget. Only validated finalized acquisition evidence may commit ACQUISITION_EVIDENCE_CLOSED and its exact acquired JUP quantity. A terminal ACK or caller quantity cannot do so. If source capture fails, stop at an evidence blocker even if acquisition is finalized. Disposal must reference that exact durable acquisition basis, freshly capture its own readiness and dispose of the complete chain-derived amount, never a human-entered balance. Source: `A/wiggles-terminal-closure-v1.mjs:18–72`; capture :193–202,247–261.
21. The second decision has the same signing/broadcast boundaries, with approval explicitly covering ordinal 2. Acquisition `not_after` is acquisition-specific; do not invent an equivalent disposal deadline, remove the common runtime deadline, or refresh it after uncertainty. If any prerequisite fails, keep the acquisition-only/failed/revoked/unresolved record and stop. No “cleanup trade.”

### Stage E — independent result reconstruction, offline after approved evidence acquisition

22. Preserve raw roots first. Trusted assembler copies exact admitted non-secret records into a **new flat replay root**, creates sorted manifest members with path/byte count/SHA-256, and records its expected manifest identity independently. Do not point the loader at the mutable runtime tree: it contains nested submission directories and authority files outside the flat format. Loader bounds are 512 manifest members, 8 MiB/member, 64 MiB total; only flat, single-link regular files with exact inventory are admitted (`A/retained-episode-package-v1.mjs:6–8,95–142`). Fail on overflow; do not omit inconvenient evidence.
23. Use `artifact_final_episode_replay_v2` / **`SUPERVISED_RETAINED_FINAL_EPISODE`** only for custodian-qualified real evidence. Retain the original acquisition opening capture, both original challenges/decisions/admissions, authenticated mandate/authorization/configured principals/revocation, exact signed wires, submission binding/journal/timing/child trees, raw finalized bodies, full history and opening/ending owner enumerations. Keep output reports outside the flat input. Never relabel calibration or synthetic fixtures as real.
24. Start with `episode.json.selection = null` to inspect the complete reconstructed population. A later explicit selection uses actual `candidate_population_digest` and `requested_candidate_digest`; even a singleton is not automatic. Because selection is manifest-bound inside the descriptor, a selected package is a **new derived package identity**, preserving all original source bytes and the earlier unselected package; never edit the frozen original in place. Source: release :139–150,167; completion :66,118.
25. Run the existing library in a fresh verifier process with no network/key/state-write capabilities, approved read-only release/dependency inputs and the independently retained expected manifest digest. Proposed concrete invocation (not executed here; requires approved prepared input and installed pinned runtime):

```sh
cd '<APPROVED_READ_ONLY_RELEASE_ROOT>'
(
  umask 0077
  set -C
  env -i PATH='<APPROVED_MINIMAL_PATH>' HOME=/nonexistent NODE_OPTIONS= NODE_PATH= \
    '<PINNED_NODE_ABSOLUTE_PATH>' --input-type=module -e '
      import { reconstructFinalEpisodeReleaseV1 } from "./engine/src/verification-scope-v1-3/final-proof-agent/final-episode-release-v1.mjs";
      import { canonicalJson } from "./engine/src/verification-scope-v1-3/contract.mjs";
      const result = await reconstructFinalEpisodeReleaseV1({
        root: process.argv[1],
        expected_manifest_sha256: process.argv[2],
        expected_evidence_kind: process.argv[3]
      });
      process.stdout.write(canonicalJson(result));
    ' '<ABSOLUTE_FLAT_REPLAY_ROOT>' '<EXPECTED_REPLAY_MANIFEST_SHA256>' \
      '<EXACT_EVIDENCE_KIND>' > '<NEW_ABSOLUTE_REPORT_PATH>'
)
sha256sum '<ABSOLUTE_FLAT_REPLAY_ROOT>/manifest.json' '<NEW_ABSOLUTE_REPORT_PATH>'
```

Use `SYNTHETIC_FINAL_EPISODE` for the offline rehearsal and `SUPERVISED_RETAINED_FINAL_EPISODE` for the qualified real package; the expected kind must equal the manifest-bound descriptor. A clean environment is not an OS network sandbox; the supervisor must enforce the denied capabilities externally. Input/member hashing before and after replay is also required. Successful process exit or `operation.status: COMPLETED` alone is not full demonstration success.

26. Independently inspect all dimensions: integrity/source admission, original eligibility, authenticated control/revocation, transmission reconciliation, observed fees, economic availability, population/explicit selection, immutable claim and demo summary. The fee repair requires actual finalized fees to equal the rebuilt exact-message/mandate fee even on failure (`A/retained-final-control-v1.mjs:200–205`). Missing provenance must not be repaired by editing observations. Full demonstration requires both eligible legs, authenticated control at DISPOSAL_EVIDENCE_CLOSED, reconciled transmission and a selected VERIFIED/CLOSED claim (`A/final-episode-release-v1.mjs:185–196`). Preserve null claims/dependencies and failed/unresolved outcomes accurately. No upload, mint, publication or API deployment follows automatically.

## 4. Final-wallet eligibility and timing

Recorded final wallet: `5CJdSbz9d5CifzFcWL5NcbicgpSAEuDGpSZBgaLHN1tA`. The fixed JUP and USDC accounts and mainnet genesis are recorded at `A/mandate-v1.mjs:26–49`; they are not new user choices.

The recorded setup schema is `ARTIFACT_FINAL_PROOF_SETUP_FREEZE_V2`; the mandate fixes **latest setup block time `1788611228`** (`A/mandate-v1.mjs:98–117`). UTC/SGT conversions and strict-age arithmetic below were calculated locally with Python datetime/Asia/Singapore; no wallet was contacted.

| Boundary | Unix seconds | UTC | Singapore (UTC+08:00) |
|---|---:|---|---|
| Latest recorded setup | 1788611228 | 5 September 2026, 12:27:08 | 5 September 2026, 20:27:08 |
| Exactly seven days later — **ineligible equality** | 1789216028 | 12 September 2026, 12:27:08 | 12 September 2026, 20:27:08 |
| Earliest candidate opening, strict one-second margin | **1789216029** | **12 September 2026, 12:27:09** | **12 September 2026, 20:27:09** |

Predicate: **`finalized_anchor_block_time - 604800 > latest_setup_block_time`**, not `>=`. The candidate is `1788611228 + 604800 + 1`. It is an earliest possible chain boundary, not an appointment, promise, fresh authorization or proof of eligibility.

The actual acquisition capture must independently:

- Validate genesis, finalized slot/block and fresh anchor time; obtain account state at a sufficient `minContextSlot` (capture :173–202).
- Require exact initial inventory: JUP `0`, USDC `6000000`, native `820624` lamports; retain the approved per-leg fee/system-rent equation. No funding top-up, token movement or ATA repair is authorized. Source: mandate :104–110. Acquisition input remains `5000000` raw USDC with maximum 50 bps; disposal is full chain-derived acquired JUP, not a fresh budget decision (:119–146).
- Capture classic and Token-2022 owner populations at the opening context; exactly the two expected classic accounts, no Token-2022 accounts, matching raw account contents and no unsupported authority/layout. These calls establish controlled equal-watermark consistency, not atomicity (capture :203–228).
- Paginate finalized histories for **wallet plus both token accounts**, with provider-enforced `minContextSlot = opening.context.slot`, limit 100, bounded pages, unique signatures and valid descending slot/time. Require an exhausted empty page, nonempty corroborating histories, then repeat each head and require exact equality. Corroborate the frozen latest setup time. Acquisition rejects all post-setup activity; disposal permits only the retained successful acquisition signature and requires it present (capture :230–261).
- Apply the strict predicate to that original finalized anchor and retain its complete requests/responses, clocks and challenge. A later verifier replays this **original** boundary, not its own current clock (`A/retained-final-control-v1.mjs:42–84`). New activity, unavailable/truncated history, unequal owner contexts, unsupported floors or stale captures mean STOP. Elapsed wall time cannot cure them.

This pack establishes the **recorded** boundary from accepted repository source; custody/authenticity of setup archive/freeze/evidence digests remains a prerequisite. It neither verifies current wallet balances/history nor changes the wallet.

## 5. Human inputs — only genuine choices/custodian values

No private keys, seed phrases, API tokens or credential-bearing URLs should be supplied in chat or the pack. Public identity values and approvals are sufficient for planning.

| Required decision/value | When it can be prepared | What is derived rather than invented |
|---|---|---|
| Authorization for the bounded build in §2, finite tests and any disposable offline-signing exception | Now | No real execution permission is implied. |
| Actual human authorization and agent-control **public** keys; identity/custody attestation for each | Now, through their custodians | Distinct Ed25519 public keys are 64 lowercase hex in this contract, not display names or published fixture identities. No private key request. |
| Deployment host and trusted administrator/supervisor/stop owner; actual runtime/controller UIDs/groups, approved paths, FD/revocation handoff, filesystem and access policy | Proposed now; freeze/qualify before deployment | Example names/modes are requirements/proposals, not proof that accounts or permissions exist. |
| Custodian locations and approved identities of setup archive/freeze/evidence manifest | Now if already retained | Recover and verify real digests from those artifacts; do not ask the user to manufacture hashes or repeat hardcoded wallet/route/time values. |
| Provider capability/plan and allowed egress/custody; actual capture, submission, source-finalization and recovery budget settings; simulation gate disposition | Design now; approve against final adapter and execution window | Canonical capture budget-table SHA-256 is computed from the approved exact object. Schema maxima are not approved operating values; provider retries for sends stay zero. |
| Approval of final immutable executor release/dependency/launcher and public configuration identities | Only after the bounded build/freeze | Compute actual SHA-256 over the reviewed release definition. The current source manifest is not a substitute; no arbitrary “release hash” is requested. |
| Acquisition `not_after`, runtime/capture deadlines, authorization issue time and unique authorization nonce; exact human-signed authorization | At final release/execution-window planning | Mandate/authorization/episode digests derive canonically; `not_before` is fixed to 1789216029 by existing validation. No placeholder deadline may enter a live mandate. |
| Explicit deployment/provisioning, offline rehearsal, read-only live capture, real control signing/wallet signing/broadcast and any later recovery-read approvals | Before the respective stage | Each approval names exact release/episode, methods/limits/destinations and stop owner. One session can approve a precisely enumerated sequence; it must not imply automatic restart or new economic authority. |
| Explicit candidate choice if requesting an immutable result after population inspection | Only after reconstruction | Use returned population/member digests; never invent a candidate ID or select automatically. This is output selection, not authority to trade. |

These include the original **five missing live-readiness fields** (two public keys, acquisition not-after, budget-table hash, executor release hash), without confusing their hash derivation with arbitrary user input. Actual slot, block time, blockhash/last-valid-height, quote, fee, simulation result, transaction signature, acquired quantity and final economics are **execution evidence**, not human-entered substitutes.

## 6. Recovery and STOP procedure

The scope remains **one human-authorized supervised episode, trusted deployment administration, ordinary crash/restart durability**. Automatic detection of a valid administrator-restored snapshot is excluded. The deferred external generation/head design is not a release gate for this scope and is not reopened here (supervised checklist :3–11; rollback decision :3–9).

1. On any known restore, reprovisioning, authoritative-state loss, uncertain custody, conflicting identity, invalid/missing evidence, deadline failure or ambiguous execution: stop new decision ingress and signing/submission initiation. Do not delete, reset, reenroll, recreate authority, refresh the mandate, restart a consumed schedule or fabricate completion. Preserve failed outputs as well as successful ones.
2. Notify the trusted supervisor/human. Use the authenticated human-revocation path if available and separately authorized; record whether its durable result is confirmed or ambiguous. A request to revoke is not proof of committed revocation and cannot recall escaped bytes. Do not block evidence retention from already in-flight work or use an abrupt kill as proof nothing happened.
3. Preserve database **and WAL/SHM**, exact wire, readiness/control records, submission/timing/terminal evidence, approved release/configuration identities and the supervisor's observed failure boundary. Do not copy only the main SQLite file while it is active. The trusted administrator must establish a consistent stopped/quiescent evidence preservation procedure; use ordinary supported close only when safe, and retain crash files without opening them through a mutating recovery command otherwise. No restore experiment or raw state inspection by Hermes is authorized by this pack.
4. Classify conservatively:

| Last reliable evidence | Allowed conclusion / next step |
|---|---|
| No reservation proved | No authority gained from the crash. Any later action still requires original unused authorization and fresh executor validation; absence of a log is not proof of no reservation. |
| RESERVED or PREPARED | Ordinal remains consumed. No second semantic action or automatic replacement preparation. |
| KEY_LOAD_STARTED_AMBIGUOUS, no durable wire | Signing may have occurred. Stop; no key reload/re-sign/rebuild. |
| SIGNED_INTENT_DURABLE, lost channel result | Preserve and independently reread exact bytes; do not repeat the decision to get another signature. Readback is not submission permission. |
| SUBMISSION_POSSIBLE or dispatch/ACK/retention uncertainty | Whole schedule is consumed. No resumed sends after reopen and no refunded slots, including NOT_DISPATCHED_DEADLINE reservations. Exact-signature read-only reconciliation only under separately approved bounded scope. |
| Finalized success but later source/export failure | Occurrence is retained; this is an evidence-completion blocker, not an unexecuted leg. Reuse captured finality and request only genuinely missing approved reads. No replay of the trade. |
| Finalized failure, EXPIRED_ABSENT, revoked or unresolved | Preserve actual fees/effects/uncertainty and original authority. None grants another decision, transaction or budget, including replacement disposal. |

5. Before new recovery RPC, reconstruct the retained intent offline: wallet, wire/message/signature, original blockhash and last-valid-height/context, episode/release, consumed schedule and existing terminal evidence. Ask approval only for the minimum missing finalized reads, zero provider/transport retries, a fixed deadline and a new exclusive evidence root. Do not ask for a replacement signing authorization as routine recovery.
6. Terminal proof must use exact signature and retained lifetime, not local time. Existing scheduler EXPIRED_ABSENT requires a null `getSignatureStatuses` result with `searchTransactionHistory:true`, finalized `getBlockHeight` **strictly greater** than retained `last_valid_block_height` with the retained context floor, and a null finalized base64 `getTransaction` for the same signature (scheduler :179–194). These are two different exact-signature absence observations plus chain expiry, not two local timeout messages. Equality in height is not expiry. Contradictory/nonfinalized errors, missing/late bodies, a non-null transaction or incomplete timing/binding evidence remain unresolved unless independently resolved by admitted finality.
7. Finalized success/failure requires consistent finalized status, exact signed wire/signature/slot and validated transaction error/body evidence. Do not promote scheduler acknowledgments, wrapper exit codes or self-rehashed summaries into economics. Freeze the checksum-bound recovery result for independent read-only reconstruction. Any approved later read can refine knowledge of the same episode; it cannot reset authority. Known restore/state loss keeps execution stopped even if some external outcome is reconstructed.

## 7. Next-session checklist and first concrete action

1. Recheck HEAD, Git status, accepted manifest identity and all 200 members; preserve the two plans and this pack.
2. Agree the bounded **production binding + retained-evidence handoff** build scope in §2, including simulation/profile compatibility and a finite offline acceptance budget. Keep both live entry points disabled during that build.
3. Prepare public principal/custodian and deployment worksheets; recover setup artifacts. Do not access real keys or RPC to start the build.
4. After implementation acceptance, freeze/review the actual release and finish its exact private launcher/config/FD/transport contract.
5. Separately approve and perform isolated offline deployment rehearsal, then deployment/provisioning, then finite read-only live capture. Stop on any unmet gate.
6. Only with fresh finalized eligibility and exact authenticated episode approval proceed to the supervised signing/broadcast sequence; afterward independently reconstruct the frozen evidence and report all result dimensions.

**First concrete action:** request authorization for the bounded offline production-composition build described in §2, with the accepted manifest as its predecessor and live activation explicitly excluded. Do not run `run-wiggles-supervised-v1.mjs` or fill configuration expecting it to become operational. This preparation task stops after saving the pack and verifying source-manifest stability.
